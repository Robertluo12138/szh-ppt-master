# szh-ppt-master

Internal, local, clean-room skill for turning a prompt, report, or Markdown source into an editable PowerPoint deck through a controlled pipeline.

This README is for maintainers. End-user / agent behavior is described in `SKILL.md`.

## Current state

Scaffold + expanded vertical slice. The directory layout, the JSON-Schema contracts for the five core artifacts plus a per-slide **controlled render model**, a **first** template-family skeleton (`business_review`), a **deterministic render-model generator** that covers every layout reachable from the controlled text / line / shape / image_slot / kpi / table primitive set (`cover`, `section_divider`, `executive_summary`, `key_message`, `two_column`, `kpi_dashboard`, `timeline`, `agenda`, `conclusion`, `comparison_table`), a **minimal SVG preview generator + validator** built on top of the render-model output, an **expanded native editable PPTX exporter** that consumes render_models for the same ten layouts, and a **narrow local PNG / JPG / JPEG embed slice** for `image_slot` primitives (bytes copied into `ppt/media/imageN.<ext>`, per-slide `image` relationships, matching `<Default Extension="..."/>` registered in `[Content_Types].xml`), all exist. The generator-supported and exporter-supported layout sets are deliberately the same today — the exporter can consume any render_model the generator can produce. D-One image generation, full PPTX coverage (every layout, every primitive, full editability inventory, theme palette mapping, determinism inventory, layout/primitive-scope inspection of the produced PPTX), SVG / GIF / WebP media embedding, `chart_placeholder` rendering, and Qoder CLI integration are not implemented.

The `render_model` contract is the intermediate the SVG renderer and the PPTX exporter both consume directly. It locks the renderable surface to a closed set of owned primitives (`text`, `shape`, `line`, `image_slot`, `table`, `kpi`, `chart_placeholder`) with required bounds and token-only style references; arbitrary SVG-like fields are rejected at the schema level. **The repo separates two questions:** which layouts the **generator can produce end-to-end** from a slide_plan, and which layouts the **exporter can consume** from a render_model. Today both sets are the same ten — `cover`, `section_divider`, `executive_summary`, `key_message`, `two_column`, `kpi_dashboard`, `timeline`, `agenda`, `conclusion`, `comparison_table` — every layout reachable from the `text` / `line` / `shape` / `image_slot` / `kpi` / `table` primitive subset. `scripts/generate_render_models.py` reads an existing workspace and emits `render_models/*.json` for slides whose layout is in that supported set; slides with any other layout — notably those whose required slot maps to the `chart_placeholder` primitive_kind — are reported as not implemented and skipped, never claimed as success. `scripts/generate_svg_previews.py` then turns each `render_models/*.json` into a deterministic per-slide SVG under `svg_previews/`, supporting the primitive kinds the generator emits today (`text`, `line`, `shape`, `image_slot`, `kpi`, `table`); every other kind fails closed on that slide. `scripts/export_pptx.py` is driven by `deck_plan.json`: it iterates `deck_plan.slides[]` in declared order, resolves each entry to its canonical `render_models/<idx:02d>_<layout>.json`, and writes one editable `.pptx` for any render_model whose layout is in the exporter's allow-list. A `deck_plan` declaring N slides whose `render_models/` is missing one file (or carries an orphan file `deck_plan` did not name) fails closed at the 1:1 coverage gate before any output is produced. The exporter's slide-body emitter is layout-agnostic — it walks each render_model's `primitives` list — so widening the layout allow-list does not change how any single shape is rendered. The `table` primitive is exported as a native `<p:graphicFrame>` wrapping `<a:tbl>` (one `<a:gridCol>` per column, bold header row, editable `<a:txBody>` per cell), so every cell is directly editable in PowerPoint without media embedding. Unsupported primitives (`chart_placeholder`), any layout outside the exporter's allow-list, wrong output extension, malformed render_models, undeclared image refs, and unsafe manifest paths all fail closed. The contract is intentionally all-or-nothing: a render_model with an out-of-scope layout aborts the run with a per-slide error and no partial deck is written. `image_slot` primitives whose `image_manifest` entry resolves to a local PNG / JPG / JPEG file are exported as native `<p:pic>` shapes (`<p:blipFill>` + `<a:blip r:embed="rIdN"/>`); the bytes are copied into `ppt/media/imageN.<ext>` deterministically (manifest-declared order), the matching `<Default Extension="..." ContentType="image/png"/>` (or `image/jpeg`) lands in `[Content_Types].xml`, and a per-slide `image` relationship lands in `ppt/slides/_rels/slideN.xml.rels` with an internal Target like `../media/image1.png` (no `TargetMode="External"`, no `file://`, no URI scheme). All other manifest extensions (SVG, GIF, WebP, ...) still fall back to native placeholder rectangles carrying the `image_manifest` alt_text. The embed gate is fail-closed: an unsafe path / a path that resolves outside the workspace / a symlink / a missing file / a file over the 10 MiB embed cap / a magic-byte mismatch all abort the run before any output is written. SVG remains a **preview / inspection** layer — the PPTX exporter does not parse SVG, it builds native OOXML from the render_model directly. The PPTX contract validator (`scripts/validate_pptx_contract.py`) now enforces a `relationships.allow_list` over the canonical OOXML rel types the exporter emits today (including the `image` URL), a `minimal_evidence.every_slide_has_native_shape` positive gate, and `media.targets_internal` + `media.inventory` + `media.embedded_only` gates over embedded media (no external image rels, no dangling Targets, no orphan `ppt/media/` parts, every shipped extension declared in `[Content_Types].xml`, and no `<a:blip r:link="..."/>` linked-image reference anywhere in the package's content XML); full editability inventory, theme palette mapping, determinism inventory, and validator-side layout/primitive-scope inspection of the produced PPTX all remain TODO. D-One, SVG / GIF / WebP media embedding, the remaining PPTX coverage (`chart_placeholder`), and Qoder behavior remain unimplemented and are documented as such.

## Deck shape is adaptive

There is no fixed deck length and no universal slide sequence. The planner derives a `deck_brief` from the user request and the supplied source documents, and the `deck_plan` stage chooses target length, section structure, slide layouts, and density for that specific brief. Expected capacity range is **12–25 slides** as guidance; a run may produce 6, 8, 10, 15, 20, 25, or another reasonable count. Different scenarios (executive summary, product proposal, technical solution, project review, research report, training deck, strategy memo) use different structures. The `business_review` template family is the first scaffold template family, not the product's identity. The 20-page synthetic example below is one upper-range stress-test fixture, not the canonical deck.

## Layout

```
SKILL.md                # skill entry point, pipeline invariants, status
README.md               # this file
SECURITY.md             # privacy and security policy
.gitignore              # ignored generated outputs and local caches
references/             # source-of-truth policy and contract docs
schemas/                # JSON Schemas for the five core artifacts,
                        # the per-slide render_model, and the template /
                        # theme / layout schemas
templates/              # internal layout / theme skeletons
  layouts/business_review/   # first template family; more may be added
    template.json
    theme.json
    layouts/<layout>.json
examples/               # synthetic-only example workspaces of varying lengths
scripts/                # local deterministic tooling
projects/               # generated workspaces (not committed; created by users)
```

`projects/<name>` is **not** hardcoded anywhere. Scripts accept arbitrary workspace paths.

## Verification today

Eighteen stdlib-only Python commands are wired up — four validators (`validate_artifacts.py`, `validate_scaffold.py`, `validate_workspace.py`, `validate_pptx_contract.py`), two deterministic generators (`generate_render_models.py`, `generate_svg_previews.py`), a deterministic PPTX exporter (`export_pptx.py`), a deterministic stage-1 (Intake) workspace initializer (`init_workspace.py`), a deterministic stage-2 (Brief) contract helper (`init_deck_brief.py`), a deterministic stage-3 (Plan) contract helper (`init_deck_plan.py`), a deterministic stage-4 (Design System) contract helper (`init_design_system.py`), a deterministic stage-5 (Per-slide Plan) contract helper (`init_slide_plans.py`), a deterministic stage-6 (Image Manifest) contract helper (`init_image_manifest.py`), a deterministic **D-One adapter contract stub** (`done_image_adapter.py`) that reads `image_manifest.json` plus a caller-supplied `--spec` of safe prompts (optionally carrying the seven clean-room taxonomy fields `rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain` per request when `--descriptor-vocabulary <path-to-d_one_descriptor_vocabulary.json>` is supplied), validates every requested id matches a manifest entry with `source == "d_one_local"`, scans every prompt against URIs / file paths / raw-source markers / 40-char source shingles / credential and PII shapes / full-slide-page-screenshot wording / public-distribution wording (`upload to public`, `public upload`, `share publicly`, `public hosting`, `publish to web`, `public url`, `public link`, `public cdn`, `host publicly`, ...), refuses any taxonomy value not in `image_taxonomy.<dim>.allowed_values` (and any taxonomy field present without `--descriptor-vocabulary`), writes a deterministic dry-run plan to `<workspace>/d_one_adapter_plan.json`, and re-validates the just-written plan against `schemas/d_one_adapter_plan.schema.json` (strict shape — `additionalProperties: false`, `mode` locked to `"dry_run"`, `schema_version` locked to `2`, `manifest_source` locked to `"d_one_local"`, plus a lowercase-identifier + forbidden-token pattern lock on every taxonomy value; a post-write schema-validation failure rolls the plan back) — plus a non-mutating `--validate-plan` mode that re-runs the schema, the workspace/manifest preflight, the cross-checks the schema cannot express (id resolution against the manifest, manifest_local_path agreement, no duplicate request ids, request_count equals len(requests)), the full prompt safety scan, AND (when `--descriptor-vocabulary` is supplied) the same vocabulary schema check + image_taxonomy allowed_values membership re-check against every taxonomy-bearing request — refusing taxonomy plans whose values drifted away from the supplied vocabulary, all against an existing `d_one_adapter_plan.json` (D-One adapter contract STUB only — does NOT call D-One / any image-generation model / any public network / any external service, does NOT generate any image bytes, does NOT mutate `image_manifest.json` or the supplied `--descriptor-vocabulary` file, does NOT change PPTX export behavior, and is NOT real D-One integration), a deterministic pre-D-One local image-asset materialization gate (`materialize_image_assets.py`) that copies caller-supplied local PNG / JPG / JPEG asset bytes from `--assets-dir` into the workspace paths declared by `image_manifest.json` and refuses unsafe paths / symlinks / missing files / unsupported extensions / duplicate ids / undeclared assets / writes outside the workspace (pre-D-One local asset gate only — does NOT call D-One / any image generation / any external service, does NOT open `input/source.md`, does NOT mutate `image_manifest.json`, does NOT generate full-slide screenshots), a deterministic Stage-1-to-6 local orchestration helper (`prepare_workspace.py`) that chains the six stage helpers above from explicit caller-supplied inputs (explicit-input orchestration only — does NOT plan a deck, extract source content, generate render_models / SVG / PPTX / reports, or call any external service), a deterministic local pipeline runner (`run_pipeline.py`) that chains validate → generate render_models → generate SVG previews → export PPTX → validate the produced PPTX for a prepared workspace, and a deterministic Stage-1-to-10 end-to-end runner (`run_explicit_pipeline.py`) that chains `prepare_workspace.py` (Stage 1-6, in-process) with `run_pipeline.py` (Stage 7-10, subprocess) into one command (explicit-input end-to-end orchestration only — does NOT plan a deck, extract source content, generate plan/spec/slide/image content, or call any external service). No third-party dependencies are required. `validate_pptx_contract.py` now gates the produced `.pptx` at the container level **and** with a minimal-evidence safety / editability layer (slide count, no external rels, no `file://` rels, relationship `Type` allow-list over the canonical OOXML rel URLs including the `image` URL, embedded-media inventory + targets-internal + embedded-only gates over `ppt/media/` parts and every `<a:blip>` in the package's content XML, no macros / OLE / ActiveX parts, at least one editable `<a:t>` run, no all-image slide, no blank slide, every slide carries at least one `<p:sp>` or `<p:cxnSp>`); full-inventory editability, theme palette mapping, determinism, and validator-side layout / primitive scope all remain TODO and are explicitly named that way in every run.

### Stage-1 (Intake): workspace initialization from a local source

`scripts/init_workspace.py` is the stdlib-only, deterministic seed step that creates a brand-new workspace from a single local Markdown or text file. It writes only the two artifacts every downstream stage needs **before** planning can start: `<workspace>/input/source.md` (a verbatim UTF-8 copy of the source) and `<workspace>/source_manifest.json` (schema-backed against `schemas/source_manifest.schema.json`, recording an opaque `source.id`, the workspace-relative `source.local_path` — **enum-locked to `input/source.md`** so unsafe shapes like `../../etc/passwd`, `/abs/path`, or `file:///etc/passwd` cannot slip past schema validation with a false-green; a future intake variant that wants a different local path must bump `schema_version` and extend the enum — the source `kind` (`markdown` / `text`), byte count, line count, and sha256). No timestamps, no absolute paths, and no random ids are written into the manifest — two runs from the same source produce byte-identical artifacts.

```
python3 scripts/init_workspace.py \
  --source path/to/local_source.md \
  --workspace path/to/new/workspace \
  [--source-id custom_id]            # defaults to --source filename stem
```

`init_workspace.py` does **NOT** plan a deck, does **NOT** call any public network / D-One / Qoder / image generation / external service, does **NOT** inspect any file outside `--workspace`, and does **NOT** log the source body text anywhere except the verbatim copy at `<workspace>/input/source.md`. After it succeeds, **stage 2** has narrow contract support via `scripts/init_deck_brief.py`, **stage 3** has narrow contract support via `scripts/init_deck_plan.py`, **stage 4** has narrow contract support via `scripts/init_design_system.py`, **stage 5** has narrow contract support via `scripts/init_slide_plans.py` (see the matching sections below); after those helpers succeed, **stage 6** has narrow contract support via `scripts/init_image_manifest.py` (see the matching section below). Once that succeeds, `scripts/run_pipeline.py` can take over for stages 7–10. The agent must declare the manifest's `source.id` in `deck_brief.source_refs`, which is the deck's required allow-list of source ids; every `slides[].source_refs` entry must be a non-empty subset of that allow-list.

Fail-closed gates (every gate aborts before any file is written under `--workspace`):

- `--source` must be a regular file (a symlink is refused — same anti-pattern `run_pipeline.py` rejects at `--output` / `--report-dir`), have a `.md` or `.txt` extension (case-insensitive), be valid UTF-8 (binary content is refused), and be non-empty;
- the string form of `--source` must not start with a URI-like scheme matching `^[A-Za-z][A-Za-z0-9+.\-]*:` — covers `http://`, `https://`, `file://`, `s3://`, `ftp://`, `data:`, `mailto:`, `javascript:`, and every other scheme of that shape regardless of whether `//` follows the `:`. The check fires before `Path.is_file()` / `Path.is_symlink()`, so a URI-shaped string never reaches the filesystem layer;
- `--workspace` must not be a symlink, must not already exist as a non-directory, and if it already exists as a directory it must be empty;
- the string form of `--workspace` is subject to the **same URI-scheme guard** as `--source` — without it, a URI-shaped argument (e.g. `data:foo`, `s3:bucket/key`, `https://attacker/ws`) would be silently resolved into the current working directory and `mkdir` would happily create a directory whose name carries the scheme prefix. The guard runs before `Path.resolve()`, so the unsafe string never reaches the filesystem layer;
- `--workspace` must resolve outside the repo root, outside the filesystem root `/`, outside the user's HOME directory itself (a workspace AT `$HOME` would shadow `~/Desktop` / `~/Documents` etc.), and outside a small list of system directories (`/etc`, `/bin`, `/sbin`, `/usr`, `/dev`, `/proc`, `/sys`, `/System`, `/Library/System` — `/tmp` and `/var/folders/...` are allowed because they're legitimate workspace locations);
- `--source-id` (defaulted from the `--source` filename stem when not supplied) must match the schema id pattern `^[A-Za-z0-9][A-Za-z0-9_.\-]*$` AND pass the repo's `local_path_is_safe` rule, so the id can never carry a URI scheme, leading slash, `..` segment, or empty string;
- the manifest is schema-validated **in memory** before the workspace is created, AND re-validated **on disk** via `scripts/validate_artifacts.py` after the write — so a serializer that silently coerced a field, or a schema/builder drift, fails closed.

**Rollback contract:** preflight failures (bad `--source`, bad `--workspace`, bad `--source-id`, in-memory schema mismatch, pre-write schema-walker exception) leave the filesystem untouched — no `mkdir` or `write_*` has run yet. A failure **after** the writes begin triggers a best-effort rollback that removes, in reverse order, the manifest, `input/source.md`, the `input/` subdirectory, and (only if this run created it) the workspace directory itself. A workspace directory that **pre-existed empty** is always preserved (the workspace-must-be-empty preflight gate guarantees it had no unrelated user content). The FAIL message includes a `rolled back: ...` line naming every path that was removed (or that could not be removed). Rollback fires on every post-preflight failure mode, including: any `Exception` or `SystemExit` raised during the writes (`OSError` mid-write, `TypeError` from `json.dumps`, etc.); the post-write `validate_artifact` re-validator returning a non-empty errors list; **and** the post-write re-validator RAISING. The catch clause is explicitly `(Exception, SystemExit)` — `validate_artifact` raises `SystemExit` on its own read-error branch, and `SystemExit` does **not** inherit from `Exception`, so a bare `except Exception:` would let it bypass rollback entirely. `KeyboardInterrupt` and `GeneratorExit` (other `BaseException` subclasses) are deliberately not caught so Ctrl-C remains responsive.

`scripts/init_workspace.py --self-test` exercises 53 in-script tempfixture scenarios — happy path on `.md` and `.txt` sources, byte-for-byte determinism between two independent runs, missing source, symlinked source, wrong extension, binary (non-UTF-8) content, empty source, URI-scheme `--source` covering `https://`, `file://`, `data:`, `mailto:`, `javascript:`, `s3:bucket/key` (both the `://` and the scheme-only forms), URI-scheme `--workspace` covering `https://`, `data:`, `s3:`, `javascript:` (the workspace gate fires at the string level before `Path.resolve()`), pre-existing non-empty workspace (prior contents preserved), symlink at `--workspace` (target untouched), regular file at `--workspace` (prior file preserved), workspace inside the repo root, workspace at `/`, workspace at `$HOME` root, workspace under `/etc`, `--source-id` with a URI scheme / `..` traversal / leading slash, valid custom `--source-id`, pre-existing **empty** workspace directory accepted, a round-trip validation of the on-disk manifest, three direct unit-tests of `_rollback_partial_writes`, four end-to-end rollback tests via `unittest.mock` (re-validation returning errors with fresh and pre-existing workspaces; re-validation raising `SystemExit` and `OSError`), **twelve** schema-level negative tests proving `source_manifest.schema.json` rejects every unsafe `local_path` shape (`../../etc/passwd`, `/etc/passwd`, `/abs/path/source.md`, `\\windows\\path`, `file:///etc/passwd`, `https://attacker.example/s.md`, `data:text/plain;base64,xx`, `input/../source.md`, `input/source.md/`, `INPUT/source.md`, `other/place.md`, empty string — the enum lock catches them where the previous `type: string, minLength: 1` schema gave a false-green), one positive test that the canonical `input/source.md` still validates, and one runtime-drift test proving `_build_manifest`'s defense-in-depth assertion fires (`ValueError` with `"internal contract violation"`) if `INPUT_FILENAME` is ever changed to a path-unsafe value. Exits non-zero if any scenario does not behave as expected.

```
python3 scripts/init_workspace.py --self-test
```

### Stage-2 (Brief): minimal deck_brief contract helper

`scripts/init_deck_brief.py` is the stdlib-only, deterministic Stage-2 helper that bridges an already-stage-1-initialized workspace (`source_manifest.json` + `input/source.md`, the artifacts `init_workspace.py` writes) into a minimal, schema-valid `<workspace>/deck_brief.json` whose `source_refs` declares the manifest's `source.id`. **This is Stage-2 contract support only — it is not a full prompt/report/Markdown-to-PPTX automation.** The helper writes only the four fields the deck_brief schema *requires* (`title`, `audience`, `objective`, `source_refs`) plus any optional metadata (`tone`, `language`, `approximate_slide_count`) the caller explicitly supplied. It does NOT read or parse `input/source.md` for headings, bullets, key messages, or constraints; it does NOT invent `key_messages` or `constraints` even when the source contains obvious candidates; and it does NOT plan a deck or produce `deck_plan` / `design_system` / `slide_plans` / `image_manifest`. Two runs from the same workspace + metadata produce a byte-identical brief.

```
python3 scripts/init_deck_brief.py \
  --workspace path/to/new/workspace \
  --title "Deck title" \
  --audience "Audience label" \
  --objective "What the deck is for" \
  [--tone neutral] \
  [--language en] \
  [--approximate-slide-count 8]
```

Fail-closed gates (every gate aborts before any file is written under `--workspace`):

- `--workspace` must be an existing directory whose string form does NOT start with a URI-like scheme matching `^[A-Za-z][A-Za-z0-9+.\-]*:` (same regex `init_workspace.py` applies to `--source` / `--workspace`);
- `--workspace` must NOT be a symlink and must NOT be a regular file;
- the workspace must already ship `source_manifest.json` as a **regular in-workspace file**: a symlink at that path (broken or resolvable) is refused outright by a symlink preflight that runs BEFORE the is_file check and BEFORE `check_source_manifest_bridge` / `read_text`, so neither a dangling target masquerading as the legacy "no manifest" no-op nor a resolvable link reading manifest bytes from outside the workspace can slip through (otherwise the agent has not yet completed Stage 1 and there's no `source.id` to reference);
- the manifest must be loadable and schema-valid against `schemas/source_manifest.schema.json`;
- `input/source.md` must exist as a regular UTF-8 file whose `byte_count` / `line_count` / `sha256` match the manifest (the same `check_source_manifest_bridge` gate the workspace validator runs — every counter mismatch / digest mismatch / missing source / malformed manifest / list-rooted manifest / manifest-symlink case is reported there and re-used here);
- `deck_brief.json` must NOT already exist in the workspace. A pre-existing **symlink** at that path (broken or resolvable) is refused outright, BEFORE the regular file-existence check, so `write_text()` cannot silently follow a dangling link and clobber its target — `Path.exists()` returns False for a broken symlink, so a bare existence gate would otherwise let that happen (same anti-pattern `run_pipeline.py` forbids at `--output` / `--report-dir`). A pre-existing regular file at the same path is also refused (rename or remove it first);
- `--title` / `--audience` / `--objective` are required, must each be non-empty after `.strip()`, and must each be single-line — a value containing a newline or carriage return is refused so the helper cannot be used as a content-extraction tool;
- `--tone` / `--language` are optional and subject to the same single-line constraint when supplied;
- `--approximate-slide-count` is optional and must be a positive integer;
- the brief is schema-validated **in memory** against `schemas/deck_brief.schema.json` before any write, and **re-validated on disk** via `scripts/validate_artifacts.py` after the write.

**Rollback contract:** preflight failures leave the workspace untouched. A post-write re-validation failure (the validator returns errors OR raises) removes the just-written `deck_brief.json` so the workspace returns to its pre-call state. The catch clause is explicitly `(Exception, SystemExit)` — `validate_artifact` raises `SystemExit` on its own read-error branch, and `SystemExit` does not inherit from `Exception`, so a bare `except Exception:` would let the rollback be bypassed. `KeyboardInterrupt` is deliberately not caught.

`scripts/init_deck_brief.py --self-test` exercises 29 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` — happy path (minimum-required metadata emits no invented optional fields), optional fields included only when supplied, byte-for-byte determinism between two independent runs, missing `source_manifest.json` (no silent fallback to an invented id), **broken symlink at `source_manifest.json` refused at the preflight (dangling target never created, no `deck_brief.json` written)**, **resolvable symlink at `source_manifest.json` refused at the same preflight (outside target bytes preserved byte-identical, no `deck_brief.json` written)**, missing `input/source.md`, malformed `source_manifest.json` (non-JSON), schema-invalid `source_manifest.json` (missing required key), sha256 mismatch between manifest and on-disk source, pre-existing regular-file `deck_brief.json` refused with prior bytes preserved, **broken symlink at `deck_brief.json` refused before any write — the dangling target is never created (proves `Path.exists()`'s false-False for a broken link does not bypass the gate)**, **resolvable symlink at `deck_brief.json` refused with the link target's bytes preserved byte-identical**, missing / regular-file / URI-shaped `--workspace`, empty / multi-line `--title` / `--audience` / `--objective`, empty `--tone`, non-positive `--approximate-slide-count`, on-disk re-validation via `validate_artifact`, mocked post-write rollback (return errors), mocked post-write rollback (raise `SystemExit`), source-body marker phrase NEVER copied into the brief (proves no content extraction), `source_refs` is exactly `[source.id]` (the (3) contract gate), and a mutated brief that drops `source.id` from `source_refs` is caught fail-closed by `check_source_manifest_bridge` (the (4) contract gate).

```
python3 scripts/init_deck_brief.py --self-test
```

After Stage 2 succeeds, **stage 3** has narrow contract support via `scripts/init_deck_plan.py` (see next section); after that helper succeeds, **stage 4** has narrow contract support via `scripts/init_design_system.py` (see the section after); after that helper succeeds, **stage 5** has narrow contract support via `scripts/init_slide_plans.py` (see the section two below); after that helper succeeds, **stage 6** has narrow contract support via `scripts/init_image_manifest.py` (see the matching section below). Once that succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### Stage-3 (Plan): minimal deck_plan contract helper

`scripts/init_deck_plan.py` is the stdlib-only, deterministic Stage-3 helper that bridges an already-stage-2-initialized workspace (`source_manifest.json` + `input/source.md` + `deck_brief.json`) plus a caller-supplied `--plan-spec` JSON file into a minimal, schema-valid `<workspace>/deck_plan.json`. **This is Stage-3 contract support only — it is not a full prompt/report/Markdown-to-PPTX automation.** The helper validates the caller-supplied plan-spec against `schemas/deck_plan.schema.json` and the planner-semantics cross-checks (planned-count equality, slide indices unique + contiguous 1..N, section coverage 1:1 with deck_plan slides, every `slide.section_id` resolves to a declared section, every `slide.source_refs` value is declared in `deck_brief.source_refs`), and writes the result deterministically. It does NOT read or parse `input/source.md` for any business content; it does NOT invent `template` / `rationale` / `slides` / `sections` when the caller does not supply them (the caller passes the plan content via `--plan-spec`); and it does NOT produce `design_system.json` / `slide_plans/*.json` / `image_manifest.json` / `render_models/*` / `svg_previews/*` / any `.pptx`. Two runs from the same workspace + spec produce a byte-identical plan.

The plan-spec contract is a JSON object whose shape is exactly the deck_plan candidate to be written:

```
{
  "template": "<template name under templates/layouts/>",
  "planning": {
    "planned_slide_count": <integer; helper enforces == len(slides)>,
    "rationale": "<short reason for length / structure>"
  },
  "sections": [
    {"id": "<stable id>", "title": "...", "summary": "...",
     "slide_indices": [<1-based indices the section covers>]}
  ],
  "slides": [
    {"index": <1-based, unique, contiguous from 1>, "layout": "...",
     "title": "...", "section_id": "<must resolve to sections[].id>",
     "summary": "...", "density": "low|medium|high",
     "source_refs": [<each value must be in deck_brief.source_refs>]}
  ]
}
```

```
python3 scripts/init_deck_plan.py \
  --workspace path/to/stage2/workspace \
  --plan-spec path/to/plan_spec.json
```

Fail-closed gates (every gate aborts before any file is written under `--workspace`):

- `--workspace` must be an existing directory whose string form does NOT start with a URI-like scheme (same regex `init_workspace.py` / `init_deck_brief.py` apply);
- `--workspace` must NOT be a symlink and must NOT be a regular file;
- the workspace must already ship `source_manifest.json` AND `deck_brief.json` as **regular in-workspace files**: symlinks at either path (broken or resolvable) are refused outright by symlink preflights that run BEFORE the is_file / read_text / bridge gates;
- the existing `check_source_manifest_bridge` must pass (manifest schema-valid, `input/source.md` matches counters / digest, `deck_brief.source_refs` declares `source_manifest.source.id`);
- `--plan-spec` must be an existing regular file (symlink refused at the preflight, broken or resolvable), must parse as JSON, must decode to an object, and must validate against `schemas/deck_plan.schema.json`;
- the spec must satisfy the planner-semantics cross-checks: `planning.planned_slide_count == len(slides)`; `slides[].index` is unique AND contiguous 1..N (the schema only requires `index >= 1`, so contiguity is enforced at the helper layer to match what stages 7–10 assume); `sections[].slide_indices` cover every `slides[].index` exactly once (no duplicates, no missing, no orphans); every `slides[].section_id` resolves to a declared section AND that section's `slide_indices` lists the slide's index; every `slides[].source_refs` value is declared in `deck_brief.source_refs`;
- `deck_plan.json` must NOT already exist in the workspace. A pre-existing symlink at that path (broken or resolvable) is refused outright, BEFORE the regular file-existence check, so `write_text()` cannot silently follow a dangling link and clobber its target. A pre-existing regular file at the same path is also refused (rename or remove it first);
- the plan is schema-validated **in memory** against `schemas/deck_plan.schema.json` before any write, and **re-validated on disk** via `scripts/validate_artifacts.py` after the write.

**Rollback contract:** preflight failures leave the workspace untouched. A post-write re-validation failure (the validator returns errors OR raises) removes the just-written `deck_plan.json` so the workspace returns to its pre-call state. The catch clause is explicitly `(Exception, SystemExit)` — `validate_artifact` raises `SystemExit` on its own read-error branch, and `SystemExit` does not inherit from `Exception`, so a bare `except Exception:` would let the rollback be bypassed. `KeyboardInterrupt` is deliberately not caught.

`scripts/init_deck_plan.py --self-test` exercises 29 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` — happy path (1-slide / 3-slide), byte-for-byte determinism between two independent runs, missing `deck_brief.json` (no silent fallback), malformed deck_brief, deck_brief whose `source_refs` omits `source_manifest.source.id` (refused at the bridge), `planning.planned_slide_count != len(slides)`, duplicate slide indices, **non-contiguous slide indices (e.g. `[1, 2, 4]`)**, section coverage that omits a slide, section coverage that lists an orphan slide_index, unknown `slide.section_id`, `slide.source_refs` value not declared in `deck_brief.source_refs`, no-source_manifest legacy workspace refused (the helper requires Stage-1; the existing prepared examples that pre-date `init_workspace.py` simply do not run this helper — their workspace-validator runs remain valid because the bridge is a no-op when the manifest is absent), pre-existing regular-file `deck_plan.json` refused with prior bytes preserved, **broken symlink at `deck_plan.json` refused before any write — the dangling target is never created**, **resolvable symlink at `deck_plan.json` refused with the link target's bytes preserved**, broken symlink at `source_manifest.json` refused (no deck_plan written, dangling target never created), `--plan-spec` symlink (broken or resolvable) / malformed JSON / list-rooted / schema-invalid, missing / regular-file / URI-shaped `--workspace`, source-body marker phrase NEVER copied into the plan (proves no content extraction), mocked post-write rollback (return errors), mocked post-write rollback (raise `SystemExit`), and on-disk re-validation via `validate_artifact`.

```
python3 scripts/init_deck_plan.py --self-test
```

After Stage 3 succeeds, **stage 4** has narrow contract support via `scripts/init_design_system.py` (see next section); after that helper succeeds, **stage 5** has narrow contract support via `scripts/init_slide_plans.py` (see the section after); after that helper succeeds, **stage 6** has narrow contract support via `scripts/init_image_manifest.py` (see the matching section below). Once that succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### Stage-4 (Design System): minimal design_system contract helper

`scripts/init_design_system.py` is the stdlib-only, deterministic Stage-4 helper that bridges an already-stage-3-initialized workspace (`source_manifest.json` + `input/source.md` + `deck_brief.json` + `deck_plan.json`) plus EXACTLY ONE explicit caller input — either `--spec <path>` (a JSON file whose shape is exactly the design_system candidate to be written) OR `--theme-from-template` + `--template-root <dir>` (project palette / typography / grid from the theme of the template named in `deck_plan.template`, resolved against the supplied template root) — into a minimal, schema-valid `<workspace>/design_system.json`. **This is Stage-4 contract support only — it is not a full prompt/report/Markdown-to-PPTX automation.** The helper validates the candidate design_system against `schemas/design_system.schema.json` (hex-coded palette colors, CSS-style font fallback chains, positive numeric font sizes, positive integer grid dimensions, non-negative integer margin). The helper itself never opens `input/source.md` — it invokes the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`), which reads the source bytes ONLY for byte-level integrity checks (UTF-8 decode validity, byte_count / line_count / sha256 match against the manifest), discards the decoded string inside the bridge, and never inspects bytes for headings / bullets / paragraphs / token candidates. The helper does NOT infer palette / typography / grid from raw source text (tokens come exclusively from `--spec` or the explicitly named template theme); and it does NOT produce `slide_plans/*.json` / `image_manifest.json` / `render_models/*` / `svg_previews/*` / any `.pptx`. Template resolution is anchored on `deck_plan.template` only — there is no product-level template default; the name `business_review` is just the template the current repo ships, not a fallback. Two runs from the same workspace + (spec or template-root) produce a byte-identical design_system.

```
# Mode 1: caller supplies a complete design_system JSON.
python3 scripts/init_design_system.py \
  --workspace path/to/workspace \
  --spec path/to/design_system.json

# Mode 2: derive palette/typography/grid from a template's theme.
python3 scripts/init_design_system.py \
  --workspace path/to/workspace \
  --theme-from-template \
  --template-root templates/layouts
```

Fail-closed gates (every gate aborts the run and writes nothing):

- `--workspace` must be an existing directory whose string form does NOT start with a URI-like scheme (same regex `init_workspace.py` applies) and must not itself be a symlink;
- the workspace must already ship `source_manifest.json`, `deck_brief.json`, AND `deck_plan.json` as **regular in-workspace files** (symlinks at any of those paths, broken or resolvable, are refused outright);
- the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`) must pass, `deck_brief.json` must validate against `schemas/deck_brief.schema.json`, `deck_plan.json` must validate against `schemas/deck_plan.schema.json`, AND `deck_plan.json` must pass EXACTLY the planner-semantics cross-checks `init_deck_plan.py` applies before writing a Stage-3 artifact — re-using `init_deck_plan._cross_check_planner_semantics` (planned_slide_count == len(slides); slide indices unique AND contiguous 1..N; section coverage 1:1 with no duplicates / missing / orphans; slide.section_id resolves AND the section's slide_indices lists the slide's index; slide.source_refs subset of deck_brief.source_refs) so Stage-4 cannot advance from a deck_plan `init_deck_plan.py` itself would have refused;
- exactly one of `--spec` / `--theme-from-template` must be supplied (no silent inference fallback);
- in `--theme-from-template` mode `--template-root` is required and must be a real directory (no symlink, no URI scheme); `deck_plan.template` must be a non-empty string that passes `local_path_is_safe` and resolves inside `--template-root` (a `..` / leading-slash / URI-scheme template name is refused at the path-safety gate before any filesystem read); the resolved `template.json` and the referenced theme file must each be regular files (symlinks refused) that validate against `schemas/template.schema.json` / `schemas/theme.schema.json`;
- in `--spec` mode the spec must be an existing regular file (symlink refused), parse as JSON, decode to an object, and validate against `schemas/design_system.schema.json`;
- `design_system.json` must NOT already exist as a symlink (broken or resolvable) AND must NOT already exist as a regular file (no overwrite; rename or remove the prior design_system first).

The candidate is schema-validated **in memory** before any write, and **re-validated on disk** via `scripts/validate_artifacts.py` after the write. A post-write failure rolls back the just-written design_system so the workspace returns to its pre-call state.

`scripts/init_design_system.py --self-test` exercises in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path (`--spec`), happy path (`--theme-from-template`), determinism in both modes, missing / malformed / schema-invalid `deck_plan.json`, schema-valid-but-semantically-invalid `deck_plan.json` (planned_slide_count mismatch, broken section coverage, undeclared slide.source_refs, duplicate slide indices, non-contiguous slide indices, unknown slide.section_id) refused at the planner-semantics cross-check (the same gate `init_deck_plan.py` applies before writing a Stage-3 artifact), missing `deck_brief.json`, unknown template (no business_review fallback), unsafe template path (`..` and URI scheme), missing theme file (`template.theme_ref` dangling), missing / malformed / list-rooted / schema-invalid `--spec` (bad hex color, empty font_family, non-positive grid width, negative margin), pre-existing `design_system.json` refused with prior bytes preserved, broken / resolvable symlink at `design_system.json` refused with target preserved, symlink at `--spec` / theme file / `--workspace` refused, URI-scheme `--workspace` refused, mutually exclusive `--spec` + `--theme-from-template`, neither mode supplied (no silent inference), `--theme-from-template` without `--template-root`, `--template-root` supplied without `--theme-from-template` (no silent mode switch), `--template-root` not a directory, source-body marker phrase NEVER copied into the design_system (proves no content extraction), mocked post-write rollback (return errors and raise `SystemExit`), on-disk re-validation via `validate_artifact`, and a custom (non-business_review) template name resolved correctly via `--template-root` (proves no hardcoded template default).

```
python3 scripts/init_design_system.py --self-test
```

After Stage 4 succeeds, **stage 5** has narrow contract support via `scripts/init_slide_plans.py` (see next section); after that helper succeeds, **stage 6** has narrow contract support via `scripts/init_image_manifest.py` (see the matching section below). Once that succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### Local Stage 5 (Per-slide Plan) — narrow contract support

`scripts/init_slide_plans.py` is the stdlib-only, deterministic Stage-5 helper that bridges an already-stage-4-initialized workspace (`source_manifest.json` + `input/source.md` + `deck_brief.json` + `deck_plan.json` + `design_system.json`) plus a required `--template-root <dir>` (against which `deck_plan.template` must resolve — no product-level default; `business_review` is just the template the current repo ships) and a required `--specs-dir <dir>` of caller-supplied `*.json` slide-plan candidates into a complete, schema-valid `<workspace>/slide_plans/<idx:02d>_<layout>.json` set. **This is Stage-5 contract support only — it is not a full prompt/report/Markdown-to-PPTX automation.** The helper validates every candidate against `schemas/slide_plan.schema.json` and cross-checks coverage against `deck_plan.slides[]`: every spec's `index` must match exactly one deck_plan slide (no duplicates across spec files, no missing slides, no orphan specs), `spec.layout` / `spec.title` must agree with the deck_plan slide's `layout` / `title`, every layout referenced by the deck_plan must be declared by `template.layouts`, and every required slot of the per-layout file under `<template-root>/<deck_plan.template>/layouts/<layout>.json` must be covered by a matching slide_plan block (id + kind). The helper itself never opens `input/source.md` — it invokes the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`), which reads the source bytes ONLY for byte-level integrity checks (UTF-8 decode validity, byte_count / line_count / sha256 match against the manifest) and discards the decoded string. The helper does NOT infer slide content from raw source text (every body comes verbatim from `--specs-dir`); it does NOT produce `image_manifest.json` / `render_models/*` / `svg_previews/*` / any `.pptx`. Two runs from the same workspace + template-root + specs-dir produce byte-identical slide_plan files.

```
python3 scripts/init_slide_plans.py \
  --workspace path/to/stage4/workspace \
  --template-root templates/layouts \
  --specs-dir path/to/slide_plan_specs
```

Fail-closed gates (every gate aborts the run and writes nothing):

- `--workspace` / `--template-root` / `--specs-dir` must each be an existing directory whose string form does NOT start with a URI-like scheme (same regex `init_workspace.py` / `init_deck_brief.py` / `init_deck_plan.py` / `init_design_system.py` apply) and must not itself be a symlink;
- the workspace must already ship `source_manifest.json` + `deck_brief.json` + `deck_plan.json` + `design_system.json` as regular in-workspace files (symlinks at any of those paths, broken or resolvable, are refused outright by symlink preflights);
- the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`) must pass; `deck_brief.json` / `deck_plan.json` / `design_system.json` each validate against their schema; `deck_plan.json` must additionally pass EXACTLY the planner-semantics cross-checks `init_deck_plan.py` applies before writing a Stage-3 artifact (planned-count equality, slide indices unique AND contiguous 1..N, section coverage 1:1, slide.section_id resolution, slide.source_refs subset of deck_brief.source_refs);
- `deck_plan.template` is a non-empty string that passes `local_path_is_safe` and resolves inside `--template-root`; `<template-root>/<deck_plan.template>/template.json` is a regular file (symlink refused), parses as JSON, and validates against `schemas/template.schema.json`; the `<template-root>/<deck_plan.template>/layouts/` subdirectory must NOT be a symlink (broken or resolvable; closes the "redirect layouts/ to an outside directory" attack); every layout referenced by `deck_plan.slides[].layout` appears in `template.layouts`, passes `local_path_is_safe`, AND resolves inside the template directory (belt-and-braces — `template.layouts[]` is pattern-locked at the schema layer); each used `<template-root>/<deck_plan.template>/layouts/<layout>.json` is a regular file (symlink refused) and validates against `schemas/layout.schema.json`;
- per spec: `--specs-dir/*.json` files are symlink-refused, parse as JSON, decode to an object, validate against `schemas/slide_plan.schema.json`, and cross-check 1:1 against deck_plan.slides[] by index — duplicates across spec files, missing slides, orphan specs, layout/title mismatches, undeclared template layouts, and missing required layout slots are all refused before any write;
- `<workspace>/slide_plans/` must NOT be a symlink (broken or resolvable) AND must NOT pre-exist with any files in it; an empty pre-existing directory is accepted and reused.

`scripts/init_slide_plans.py --self-test` exercises 26 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path, determinism between two independent runs (byte-identical slide_plan files), missing `deck_plan.json`, malformed `deck_plan.json` (non-JSON), missing `design_system.json`, schema-invalid `design_system.json` (bad hex color), unknown template (no `business_review` fallback), spec missing a required layout slot, orphan spec (index not in deck_plan), missing spec (deck declares more slides than the specs-dir supplies), duplicate slide index across two spec files, symlink at `--specs-dir`, symlink at an individual `--specs-dir/*.json` spec file, pre-existing non-empty `slide_plans/` (refused with prior bytes preserved), source-body marker phrase NEVER copied into any slide_plan (proves no content extraction), URI-shaped `--workspace`, spec/deck_plan layout/title mismatch, schema-invalid spec (missing required `blocks`), non-contiguous deck_plan indices `[1, 2, 4]` (refused at the planner-semantics cross-check), undeclared template layout (deck_plan references a layout `template.layouts` does not declare), empty pre-existing `slide_plans/` (accepted and written into), broken symlink at `slide_plans/` (refused, dangling target never created), **symlink at `<template>/layouts/` refused before any layout file is opened** (closes the symlink-to-outside-template attack on the layouts subdirectory), **mocked post-write re-validation failure rolls back every written slide_plan AND the slide_plans/ directory this run created** (workspace returns to pre-call state), **mocked write_text failure mid-loop rolls back every file already on disk PLUS the partial file at the failing path AND the slide_plans/ directory** (proves out_path is added to the rollback set BEFORE the write call, so a half-written file is still cleaned up), and **post-write rollback preserves an empty pre-existing slide_plans/ directory** (proves rollback only removes what THIS run created).

```
python3 scripts/init_slide_plans.py --self-test
```

After Stage 5 succeeds, **stage 6** has narrow contract support via `scripts/init_image_manifest.py` (see the next section). Once that succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### Local Stage 6 (Image Manifest) — narrow contract support

`scripts/init_image_manifest.py` is the stdlib-only, deterministic Stage-6 helper that bridges an already-stage-5-initialized workspace (`source_manifest.json` + `input/source.md` + `deck_brief.json` + `deck_plan.json` + `design_system.json` + a non-empty `slide_plans/` directory whose every `*.json` file is a regular file and validates against `schemas/slide_plan.schema.json`) plus a caller-supplied `--spec` JSON file whose shape is exactly the image_manifest candidate the caller wants written into a schema-valid `<workspace>/image_manifest.json`. **This is Stage-6 contract support only — it is not a full prompt/report/Markdown-to-PPTX automation.** The helper validates the `--spec` against `schemas/image_manifest.schema.json`, refuses duplicate `images[].id`, refuses unsafe `images[].local_path` (URI scheme / POSIX-absolute / `..` traversal / protocol-relative), refuses asset symlinks (the leaf-symlink check runs BEFORE the within-workspace check so an asset symlink pointing outside the workspace surfaces with the clear "symlink" diagnostic), and requires every declared `local_path` to resolve inside the workspace to an existing regular file. The helper re-runs the Stage-5 slide-plan coverage gate `init_slide_plans.py` applies — the union of `slide_plan.index` values must exactly cover `deck_plan.slides[].index` (no duplicate indices across slide_plan files, no missing slides, no orphan slide_plans) and each pair's `layout` / `title` must agree with the deck_plan slide — so a hand-edited `slide_plans/` that drifts from `deck_plan` is refused before Stage-6 advances. Slot coverage against the per-layout file is NOT re-run here (it needs the template root, and Stage-5 already enforced it on the producer side; Stage-6 only catches caller-side drift in the deck_plan ↔ slide_plan pairing). The helper cross-checks every `slide_plan.image_refs[*]` value across the workspace against `images[].id`; an empty `images[]` list is accepted ONLY when no slide_plan references any image. The helper itself never opens `input/source.md` — it invokes the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`), which reads the source bytes ONLY for byte-level integrity checks (UTF-8 decode validity, byte_count / line_count / sha256 match against the manifest) and discards the decoded string. The helper does NOT generate any image asset, does NOT call D-One / Qoder / any public network / image generation / external service, does NOT extract image refs from raw source text, and does NOT produce `render_models/*` / `svg_previews/*` / any `.pptx`. Two runs from the same workspace + spec produce a byte-identical manifest.

```
python3 scripts/init_image_manifest.py \
  --workspace path/to/stage5/workspace \
  --spec path/to/image_manifest_spec.json
```

Fail-closed gates (every gate aborts the run and writes nothing):

- `--workspace` must be an existing directory whose string form does NOT start with a URI-like scheme (same regex as the other `init_*` helpers) and must not itself be a symlink;
- the workspace must already ship `source_manifest.json` + `deck_brief.json` + `deck_plan.json` + `design_system.json` as regular in-workspace files (symlinks refused), and must ship a non-empty `slide_plans/` directory whose every `*.json` file is a regular file (symlinks refused) and validates against `schemas/slide_plan.schema.json`;
- the Stage-1/Stage-2 bridge (`check_source_manifest_bridge`) must pass; `deck_brief.json` / `deck_plan.json` / `design_system.json` each validate against their schema; `deck_plan.json` must additionally pass EXACTLY the planner-semantics cross-checks `init_deck_plan.py` applies before writing a Stage-3 artifact;
- Stage-5 slide-plan coverage must hold: the union of `slide_plan.index` values exactly covers `deck_plan.slides[].index` (no duplicate indices across slide_plan files, no missing slides, no orphan slide_plans), and each pair's `layout` / `title` agree with the deck_plan slide. Slot coverage against the per-layout file is NOT re-run here (Stage-5 already enforced it on the producer side);
- `--spec` must be an existing regular file (symlinks, URI-shaped values, malformed JSON, list-rooted JSON all refused at the preflight) and must validate against `schemas/image_manifest.schema.json`;
- no two `images[].id` values may be equal; every `images[].local_path` passes `validate_scaffold.local_path_is_safe`, the leaf is not a symlink, the path resolves inside `--workspace`, and the resolved target is an existing regular file;
- every `slide_plan.image_refs[*]` value is declared in `images[].id`; an empty `images[]` list is accepted ONLY when no slide_plan references any image;
- `image_manifest.json` must NOT already exist as a symlink (broken or resolvable) AND must NOT already exist as a regular file (no overwrite; rename or remove the prior manifest first).

The candidate manifest is schema-validated in memory before any write and re-validated on disk afterwards via `scripts/validate_artifacts.py`; a post-write re-validation failure (return errors OR raise) rolls back the just-written manifest so the workspace returns to its pre-call state. The catch clause is `(Exception, SystemExit)` so `validate_artifact`'s `SystemExit` branch on read-errors still routes through rollback.

`scripts/init_image_manifest.py --self-test` exercises 28 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path with one local asset, happy path with empty `images[]` + no `image_refs`, determinism between two independent runs, missing `--spec`, symlink `--spec`, malformed `--spec` (non-JSON), schema-invalid `--spec` (missing required `images`), list-rooted `--spec`, duplicate `images[].id`, three unsafe `local_path` variants (URI / POSIX-absolute / `..` traversal), missing image asset, symlinked image asset, undeclared `slide_plan.image_refs` id, pre-existing `image_manifest.json` refused with prior bytes preserved, broken symlink at `image_manifest.json` (dangling target never created), resolvable symlink at `image_manifest.json` (outside target bytes preserved), empty `slide_plans/` rejected as a Stage-5 prerequisite failure, URI-shaped `--workspace`, source-body marker phrase NEVER copied into the manifest (proves no content extraction), mocked post-write rollback (return errors / raise `SystemExit`), the "empty images + non-empty refs" cross-check diagnostic, and the Stage-5 coverage gates (missing slide_plan whose index `deck_plan` declares, orphan slide_plan whose index `deck_plan` does not declare, duplicate slide_plan indices across two files, slide_plan/deck_plan title mismatch).

```
python3 scripts/init_image_manifest.py --self-test
```

After Stage 6 succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### D-One adapter contract stub (NOT D-One integration)

`scripts/done_image_adapter.py` is the stdlib-only, deterministic **D-One adapter contract STUB**. It sits between `init_image_manifest.py` (which writes the manifest) and the eventual real D-One generator — and intentionally does not implement real generation today. Given a workspace that already ships a schema-valid `image_manifest.json` plus a caller-supplied `--spec <prompt-spec.json>` whose top-level `requests[]` lists one entry per asset the caller wants D-One to generate (each entry: required `id` matching an `images[].id` whose `source == "d_one_local"` and non-empty `prompt`; optional `intended_use` / `width_px` / `height_px`; AND optionally any subset of the seven clean-room taxonomy fields `rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain`, gated by the optional `--descriptor-vocabulary` below), the helper validates the manifest, validates the spec, scans every prompt against a defense-in-depth deny list, optionally validates the descriptor vocabulary and re-checks every taxonomy value against `image_taxonomy.<dim>.allowed_values`, and writes a deterministic dry-run plan file. **This is the D-One adapter contract STUB, not D-One integration.** The helper does **not** call D-One / Qoder / any image-generation model / any public network / any external service; does **not** generate any image bytes (PNG / JPG / JPEG / SVG / GIF / WebP); does **not** mutate `image_manifest.json` or the supplied `--descriptor-vocabulary` file (both byte-identical before and after a successful run); does **not** open `input/source.md` for content extraction (only its raw bytes, only for the 40-character shingle check); does **not** generate full-slide screenshots; does **not** produce `render_models/*`, `svg_previews/*`, or any `.pptx`; and does **not** change PPTX export behavior. Two runs from byte-identical inputs produce byte-identical plan-file bytes (requests are emitted sorted by `id`; taxonomy fields, when present, are projected in the canonical `rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain` order).

```
python3 scripts/done_image_adapter.py \
  --workspace path/to/stage6/workspace \
  --spec path/to/prompt_spec.json \
  [--plan-out path/to/plan.json] \
  [--descriptor-vocabulary path/to/d_one_descriptor_vocabulary.json]
```

Fail-closed gates (every gate aborts the run; a mid-write failure rolls the plan file back):

- `--workspace`, `--spec`, AND `--descriptor-vocabulary` URI-shape are refused at the string layer before any filesystem call;
- `--workspace` must not be a symlink, must be an existing directory; `--spec` must not be a symlink, must be an existing regular file that parses as JSON and decodes to an object whose only top-level key is `requests`; `--descriptor-vocabulary` (when supplied) must not be a symlink, must be an existing regular file that parses as JSON, decodes to an object, and validates against `schemas/d_one_descriptor_vocabulary.schema.json` (every `image_taxonomy.<dim>.allowed_values` is projected into a per-dimension set for the runtime membership check);
- `requests[]` is a non-empty list of objects with required keys `id` (non-empty string) and `prompt` (non-empty string), optional keys `intended_use` (non-empty string) / `width_px` (positive int) / `height_px` (positive int) / and any subset of the seven taxonomy fields (`rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain`, each a non-empty string), no extra keys, no duplicate `id`s;
- a request carries one or more taxonomy fields **only when** `--descriptor-vocabulary` is supplied (any taxonomy field in a spec without the flag is refused); every present taxonomy value must be a member of the matching `image_taxonomy.<dim>.allowed_values` set (the schema additionally pattern-locks the value at the lowercase-identifier + forbidden-token deny clause as belt-and-braces);
- every `requests[].id` matches an `images[].id` in the manifest whose `source == "d_one_local"` (`local_asset` / `synthetic` entries are refused outright so a caller cannot smuggle a non-D-One request into the channel);
- every prompt passes the safety scan: URI schemes (`http://`, `file://`, `data:`, `mailto:`, ...); absolute / traversal / Windows / UNC / home-shortcut file-path shapes; raw-source marker literals (`<SOURCE>`, `BEGIN SOURCE`, ...); any 40-character substring of `input/source.md` if the source body is present (the shingle gate catches a caller who copy-pasted source body into a prompt; the scan walks every 40-char alignment of the PROMPT and checks each window for membership in the source, so every alignment is visited without skipping — the prompt-side walk is cheaper than a source-side walk when prompts are short, which is typical); credential shapes (JWT, AWS access key, PEM marker, long-hex blob, bearer token, and credential literals `password:` / `secret:` / `api_key:` / `token:` / ...); customer / account / contact-id shapes (emails, phone numbers, SSN-like strings, UUIDs, `customer_id` / `account_id` literals); full-slide / page-generation / screenshot wording (`full slide`, `whole slide`, `screenshot`, `render the slide`, ...); public-distribution wording (`upload to public`, `public upload`, `share publicly`, `public hosting`, `publish to web`, `public url`, `public link`, `public cdn`, `host publicly`, ...); editable-text wording — universal, applies regardless of `text_policy` — asking the generated image to carry copy that must remain exact / editable / searchable / rewordable / data-faithful (`slide title`, `page title`, `body copy`, `native text`, `text overlay`, `exact text`, `editable copy`, `searchable text`, `rewordable copy`, `data value`, `axis label`, `kpi number`, `legend text`, `headline text`, `subtitle text`, `callout text`, `annotation text`, `caption text`, ...; such copy belongs in the native SVG / PPT text layer, not the generated raster); and when `text_policy == 'no_text'`, in-image-text wording (`include text`, `with text`, `visible text`, `add letters`, `show numerals`, `include a caption`, `with a label`, `lettering`, `typography`, `wordmark`, `calligraphy`, `monogram`, `text in the image`, ...) — self-contradictory under a policy that already forbids any visible in-image text; the rule is editability-based, NOT script-based, so decorative artwork wording under `text_policy=decorative_glyphs` / `caption_safe` (including non-Latin / CJK / Cyrillic / Arabic glyphs) is unaffected;
- every `requests[].intended_use` (if present) re-runs the full-slide-wording AND universal editable-text subsets (so a manifest description leaking `"full-slide background"` or `"slide title"` into the spec is refused);
- `input/source.md` is read only when it exists as a regular non-symlink file (a symlinked source is refused outright; a missing source body is acceptable and skips the shingle check only);
- `--plan-out` (defaults to `<workspace>/d_one_adapter_plan.json`) is symlink-refused (leaf-symlink check BEFORE `_resolves_within` so a symlink at the target that points outside the workspace surfaces with the clear "symlink" diagnostic), `local_path_is_safe` against the workspace-relative form, `_resolves_within` against `--workspace`, parent-path segments not symlinks, and not pre-existing as a regular file (caller must remove a prior plan to re-run — no overwrite, matching `init_image_manifest`'s contract);
- a mid-write `OSError` on `--plan-out` unlinks the partial file and exits non-zero; post-conditions re-read the plan to confirm it re-parses to the in-memory candidate, **validate the just-written plan against `schemas/d_one_adapter_plan.schema.json`** (strict shape — `additionalProperties: false` at the root and per-request; `schema_version` locked to integer `2` (bumped from `1` alongside the optional per-request taxonomy fields — an older reader with `schema_version=1` would reject the new properties under per-request `additionalProperties:false`, so the shape change required a paired version bump); `mode` locked to `"dry_run"`; `note` must contain the literal `"D-One adapter contract stub"`; `manifest_source` locked to `"d_one_local"`; positive integer `request_count` / `width_px` / `height_px`), and re-read `image_manifest.json` to confirm bytes are byte-identical pre/post; a post-condition failure (including a schema-validation failure) unlinks the plan and rolls the workspace back to its pre-call state.

The same plan schema can be checked independently in two ways. For a structural check that does not require the workspace, use the generic scaffold validator:

```
python3 scripts/validate_artifacts.py \
  --schema schemas/d_one_adapter_plan.schema.json \
  path/to/d_one_adapter_plan.json
```

For the full re-validation (schema + workspace + cross-checks + prompt safety scan + optional descriptor-vocabulary check), use `done_image_adapter.py --validate-plan` against an existing plan file. The validator is **non-mutating** — the plan file, the manifest, and the supplied vocabulary (when present) are byte-identical pre/post, and no other workspace file is touched:

```
python3 scripts/done_image_adapter.py --validate-plan \
  --workspace path/to/stage6/workspace \
  --plan path/to/stage6/workspace/d_one_adapter_plan.json \
  [--descriptor-vocabulary path/to/d_one_descriptor_vocabulary.json]
```

`--validate-plan` runs four layers of gates: (1) string- and filesystem-layer gates on `--workspace` / `--plan` / (optional) `--descriptor-vocabulary` (URI refused, symlink refused, exists, regular file); (2) the same `image_manifest.json` + `input/source.md` preflight the write path uses (manifest schema-valid, no duplicate ids, every `local_path` safe), and — when `--descriptor-vocabulary` is supplied — the same schema + `image_taxonomy.<dim>.allowed_values` projection the write path uses; (3) `schemas/d_one_adapter_plan.schema.json` (catches malformed plans, list-rooted plans, unknown top-level / per-request fields, missing required fields, `mode != "dry_run"`, `schema_version` drift, `note` missing the dry-run sentinel, `manifest_source != "d_one_local"`, non-positive dimensions, AND the lowercase-identifier + forbidden-token pattern lock on every taxonomy value); (4) cross-checks the schema cannot express (`request_count == len(requests)`; no duplicate request ids; every id resolves in the manifest with `source == "d_one_local"`; `manifest_local_path` / `manifest_source` agree with the matching manifest entry byte-for-byte; `manifest_local_path` re-passes `local_path_is_safe` + `_resolves_within`; every `prompt` re-passes the full URI / file-path / raw-source marker / 40-char source shingle / credential / PII / full-slide-page-screenshot wording / public-distribution wording / universal editable-text wording / `text_policy=no_text`-aware in-image-text wording deny list, and every `intended_use` re-passes the full-slide-wording AND universal editable-text subsets of that list — the same per-field scope the write path applies, since the manifest schema's own description already says `intended_use` should never be `"full-slide background"`; AND when any plan request carries one or more taxonomy fields, `--descriptor-vocabulary` MUST have been supplied and every present taxonomy value MUST be a member of the matching `image_taxonomy.<dim>.allowed_values` set — drift detection: a plan whose taxonomy value is regex-shape valid but no longer in the vocabulary's allowed_values is refused here).

`scripts/done_image_adapter.py --self-test` exercises 248 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: the happy path; determinism between two independent runs (byte-identical plan bytes); unknown manifest id refused; manifest entry whose `source != "d_one_local"` refused; raw-source marker `<SOURCE>` refused; 40-character shingle of `input/source.md` in the prompt refused (raw-source paste suspected); a 40-character source substring at a non-stride-aligned source offset refused (regression for an earlier stride-8 source-side scan that missed offsets that were not multiples of 8); **ten URL variants refused** (`https://`, `http://`, `file://`, `data:text/plain;base64,...`, AND the regressions for the earlier scan that required `//` or `[A-Za-z0-9]` after the colon — `tel:+1-555-...`, `sms:+1-555-...`, bare `data:,malicious-payload`, `magnet:?xt=urn:btih:...` — AND the 1-character-scheme regressions `a:malicious-content` / `x:hidden-payload`); **seven file-path shapes refused** (Unix absolute, `/var/log/...`, `~/`, `../`, Windows `C:\\`, Windows forward-slash `C:/Windows/System32/cmd.exe`, UNC `\\\\server\\share`); seven credential shapes refused (AWS, JWT, PEM, long hex, bearer, `password:`, `api_key:`); five personal-id shapes refused (email, SSN, UUID, `customer_id`, `account_id`); eight full-slide / page-generation / screenshot wording variants refused; **ten public-distribution wording variants refused** (`upload to public`, `public upload`, `share publicly`, `public hosting`, `publish to web`, `public url`, `public link`, `public cdn`, `host on a public`, `host publicly`); `intended_use` containing `"full-slide background"` refused; `--plan-out` outside `--workspace` refused; symlink at `--workspace` refused; symlink at `--spec` refused; symlink at `--plan-out` (target points outside the workspace) refused with the dangling target never created; symlink at `input/source.md` refused; URI-shaped `--workspace` refused at the string layer; URI-shaped `--spec` refused at the string layer (even when `--workspace` would have failed an earlier filesystem gate); pre-existing `--plan-out` regular file refused with prior bytes preserved; malformed `image_manifest.json` refused; schema-invalid `image_manifest.json` refused; spec missing `requests` refused; spec with extra top-level key refused; duplicate request id refused; empty / whitespace-only prompt refused; whole-workspace before/after diff proving the successful run produces ONLY the plan file (no image bytes generated; no `media/*` asset created); mocked `Path.write_text` failure leaves no plan file on disk; no-content-leak proof (a distinctive marker in `input/source.md` never appears in the plan file, the manifest, or the helper's stdout/stderr); plan-file determinism (requests sorted by id regardless of spec order); **a write-path regression that monkey-patches `_build_plan` to drop the `mode` field and verifies the post-write schema-validation step rolls the plan file back**; the standalone `--validate-plan` matrix — happy-path round-trip after the write path; non-mutating proof (manifest bytes, plan bytes, workspace file set all byte-identical pre/post); list-rooted plan refused by schema (`type: object`); unknown top-level field refused (`additionalProperties: false`); missing required top-level field refused; `mode != "dry_run"` refused (enum); `schema_version != 2` refused (enum); `note` missing the dry-run sentinel refused (pattern); per-request `manifest_source != "d_one_local"` refused (enum); unknown per-request field refused; missing required per-request field refused; duplicate request id refused (cross-check); `request_count != len(requests)` refused (cross-check); request id missing from the manifest refused (cross-check); `manifest_local_path` disagreement with the manifest entry refused (cross-check); URL / 40-char source shingle / credential / full-slide wording / public-distribution wording / universal editable-text wording / `text_policy=no_text`-aware in-image-text wording in a plan prompt refused (cross-check safety scan); `intended_use` containing full-slide wording AND `intended_use` containing universal editable-text wording refused; malformed plan JSON refused; missing `--plan` refused; symlink at `--plan` refused; URI-shaped `--plan` refused at the string layer; **AND the full `--descriptor-vocabulary` taxonomy matrix** — valid taxonomy (all five original fields with canonical values) round-trips through write + validate-plan with the plan carrying every supplied taxonomy field; omitted taxonomy accepted with OR without `--descriptor-vocabulary` (plan stays taxonomy-free); unknown taxonomy value (regex-shape-valid `blueprint_style` not in `image_taxonomy.rendering_style.allowed_values`) refused with an `allowed_values` diagnostic and no plan written; **fourteen unsafe-token taxonomy values refused** across the original five dimensions and the full 9-bounded-token + 5-compound deny list (`public` / `upload` / `raw` / `customer` / `confidential` / `screenshot` / `credential` / `password` / `secret` as bounded tokens, AND `full_slide` / `image_search` / `web_generation` / `page_generation` / `slide_generation` as compounds); missing `--descriptor-vocabulary` when the spec carries any taxonomy field refused (write path); plan drift on `--validate-plan` (a plan whose taxonomy value was rewritten to `blueprint_style` after the write is refused by the cross-check); missing `--descriptor-vocabulary` when the plan carries taxonomy fields refused (validate-plan path); a taxonomy-free plan accepts an optional `--descriptor-vocabulary` without complaint; schema-invalid `--descriptor-vocabulary` refused with no plan written; symlinked `--descriptor-vocabulary` refused; URI-shaped `--descriptor-vocabulary` refused at the string layer; missing `--descriptor-vocabulary` file refused; taxonomy-write determinism (two independent runs from identical taxonomy-bearing inputs produce byte-identical plan-file bytes); the supplied `--descriptor-vocabulary` file is byte-identical pre/post a successful write; **a write-path regression that monkey-patches `_load_descriptor_vocabulary` to return a stale "before" snapshot, proving the post-condition vocab byte-identical gate fires and rolls the just-written plan back when the bytes disagree at re-read**; **AND the prompt-intent contract matrix** for the two extension dimensions `text_policy` + `subject_domain` (aligned idea-level with the latest upstream ai-image prompt-intent work but with no upstream code / text / assets copied) — valid text_policy / subject_domain values round-trip through write + validate-plan; missing `--descriptor-vocabulary` refuses a spec carrying either field; unknown values refused with `image_taxonomy.<dim>.allowed_values` diagnostics; **twenty-eight unsafe-token values refused** across both new dimensions and the full 9-bounded-token + 5-compound deny list (`public_*` / `upload_*` / `raw_*` / `customer_*` / `confidential_*` / `screenshot_*` / `credential_*` / `password_*` / `secret_*` AND `full_slide` / `image_search` / `web_generation` / `page_generation` / `slide_generation` — every bounded token AND every compound is exercised against both new dimensions, with no overclaim); and validate-plan drift (a plan whose text_policy / subject_domain value is no longer in `allowed_values` is refused); **AND the editability text-layer policy matrix**: the universal editable-text wording deny list refused in the spec / plan prompt (regardless of `text_policy`) AND in `intended_use` (the manifest field re-runs the universal subset); when `text_policy == 'no_text'` the in-image-text wording deny list refuses prompts that ask for visible letters / numerals / captions / labels / titles / quotes / slogans / lettering / typography / wordmark / calligraphy / monogram / text-in-the-image; when `text_policy == 'decorative_glyphs'` decorative artwork wording is ACCEPTED, including non-Latin / CJK / Cyrillic / Arabic decorative wording — proving the gate is editability-based, NOT a script-based ban; the universal editable-text rule still fires under `text_policy == 'decorative_glyphs'` so a slide-title chrome request remains refused even when stylised letterforms are permitted; AND a strict-adjacency negation exemption (BEFORE-side `no` / `not` / `never` / `without` immediately preceding the literal, OR AFTER-side optional copula `is` / `are` / `was` / `were` + refusal word `forbidden` / `prohibited` / `denied` / `disabled` / `blocked` / `refused` / `banned` or refusal bigram `not used` / `not allowed` / `not permitted` / `not enabled`) so policy-reinforcement wording like `no visible text` / `do not include text` / `without lettering` / `lettering is forbidden` / `caption text is forbidden` / `body copy is not used` is ACCEPTED rather than refused — punctuation between the negation word / copula and the literal (`,` / `.` / `;` / `...`) is treated as a clause boundary that BREAKS adjacency, so `no, include text` / `not. include text` / `without... lettering` / `is, forbidden` all REFUSE rather than exempt (closes a comma-separated false-green); trailing sentence-end punctuation `.` / `!` / `?` on the final refusal word IS tolerated so `lettering is forbidden.` still exempts; the exemption is per-occurrence, so a mixed prompt where one phrase is negated and another phrase is an unnegated request (`do not include text AND add a caption`) STILL fires on the unnegated phrase; AND the deny-list literals consulted via the policy text-layer scan are matched boundary-aware (a word boundary on each side of the literal), so the scan only fires on whole-word / whole-phrase matches and never on a prefix / suffix substring of a longer word — closes a Codex-flagged false positive where the literal `image text` matched the prefix of `image texture`, and similarly lets normal image-generation wording like `with textured paper` / `context lighting` / `texture pattern` pass under `text_policy='no_text'` without weakening the gate for real in-image-text requests (`include text`, `with text`, `add a caption`, `lettering`, `typography`, ..., covered by the existing TPI7 / TPI11-TPI15 matrix above).

```
python3 scripts/done_image_adapter.py --self-test
```

When real D-One integration ships, the dry-run plan this stub produces becomes the input to the generator step (D-One reads the validated requests + prompts from the plan, generates local PNG / JPG / JPEG bytes under a controlled local directory). The generator's output bytes then flow through `materialize_image_assets.py` below for the same magic-byte and path-safety gates that gate any other local asset today. Real MCP / D-One generation is intentionally **not** wired today.

### Mockable local D-One generation runner (NOT real D-One integration)

`scripts/run_d_one_generation.py` is the stdlib-only, deterministic **mockable local D-One generation runner boundary** — the place where a future real D-One generator will be wired in. Today it only operates in mock mode. Given a workspace that already ships a schema-valid `image_manifest.json` plus a `d_one_adapter_plan.json` (the dry-run plan that `done_image_adapter.py` wrote — default `<workspace>/d_one_adapter_plan.json`, overridable via `--plan`), and optionally a `--descriptor-vocabulary <path-to-d_one_descriptor_vocabulary.json>` (required iff the plan carries one or more of the seven taxonomy fields `rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain`; forwarded verbatim to `done_image_adapter.validate_plan_file` so the runner re-runs the same `image_taxonomy.<dim>.allowed_values` check the writer used), the runner emits local PNG / JPG / JPEG bytes named `<id>.<ext>` into a caller-supplied `--assets-dir` whose flat layout exactly matches what `scripts/materialize_image_assets.py` consumes (`<id>.<ext>` files at the assets-dir root, keyed by `images[].id` plus the lower-cased extension of `images[].local_path`).

Two mock providers (mutually exclusive; exactly one is required — there is no real-D-One flag today):

- `--fixtures-dir <dir>`: every plan request id must have a matching `<id>.<ext>` file inside this directory. The runner copies the fixture bytes verbatim into `<assets-dir>/<id>.<ext>`.
- `--allow-synthetic-bytes`: the runner emits a fixed minimal magic-byte-valid PNG or JPEG payload per declared extension. Intended for self-tests and pipeline smoke runs where the bytes only need to satisfy the magic-byte gate downstream.

**This is a mockable local runner boundary, NOT real D-One integration.** Real D-One / MCP / model-API integration is intentionally **TODO** (see `references/d-one-image-policy.md`). The runner does **not** call D-One / Qoder / any image-generation model / any public network / any external service / image search / telemetry endpoint; does **not** copy any business content from `input/source.md` into any output (the validator pass it invokes — `done_image_adapter.validate_plan_file` — reads `input/source.md` only for the 40-character raw-byte shingle scan against each plan prompt and discards the decoded string; no source-derived data ever reaches a runner output, which is always either a verbatim fixture-byte copy or a fixed synthetic payload); does **not** mutate `<workspace>/image_manifest.json`, `d_one_adapter_plan.json`, or the supplied `--descriptor-vocabulary` file (all three byte-identical pre/post a successful run); does **not** generate `render_models/*`, `svg_previews/*`, or any `.pptx`; does **not** change PPTX export behavior; does **not** emit SVG / GIF / WebP / TIFF bytes (the embed surface `scripts/export_pptx.py` supports today is PNG / JPG / JPEG, so those are the only extensions the runner will materialize); and does **not** generate full-slide screenshots.

```
python3 scripts/run_d_one_generation.py \
  --workspace path/to/stage6/workspace \
  --assets-dir path/to/output/assets \
  --fixtures-dir path/to/local/fixtures \
  [--descriptor-vocabulary path/to/d_one_descriptor_vocabulary.json]
```

or for the self-test / smoke path:

```
python3 scripts/run_d_one_generation.py \
  --workspace path/to/stage6/workspace \
  --assets-dir path/to/output/assets \
  --allow-synthetic-bytes \
  [--descriptor-vocabulary path/to/d_one_descriptor_vocabulary.json]
```

Fail-closed gates: `--workspace` / `--assets-dir` / `--plan` / `--fixtures-dir` / `--descriptor-vocabulary` URI-shape refused at the string layer before any filesystem call; each path not a symlink (broken or resolvable); workspace exists as a directory; assets-dir / fixtures-dir exist as directories; plan exists as a regular file and passes `done_image_adapter --validate-plan` (the full plan-schema + manifest cross-checks + per-request prompt safety re-scan — URIs, file paths, raw-source markers, 40-char `input/source.md` shingles, credentials, PII, full-slide wording; AND, when `--descriptor-vocabulary` is supplied, the same `image_taxonomy.<dim>.allowed_values` membership re-check the writer used; AND, refusal of any taxonomy-bearing plan when `--descriptor-vocabulary` is NOT supplied — closing the dead-end where a taxonomy plan would otherwise have no downstream consumer); every plan request's `manifest_source == "d_one_local"` and `manifest_local_path` re-passes `local_path_is_safe` + `_resolves_within`; every lower-cased extension is one of `.png` / `.jpg` / `.jpeg` (SVG / GIF / WebP / TIFF refused); the output filename `<id>.<ext>` passes `local_path_is_safe`, resolves inside `--assets-dir`, AND contains no `/` or `\\` (the flat-layout gate — `local_path_is_safe` rejects `..` segments and URI schemes but allows internal `/`, so an id like `'a/b'` would otherwise slip through, create a subdirectory under `--assets-dir`, and break the handoff to `materialize_image_assets.py`'s flat-layout gate; the runner fails fast here with a clear diagnostic instead); no target pre-exists (no overwrite); no target is a symlink. In fixture mode: every `<fixtures-dir>/<id>.<ext>` exists as a regular non-symlink file whose first bytes match the declared-extension magic signature (missing fixtures, wrong magic bytes, symlinked fixtures all refused). The mutually-exclusive provider-mode invariant is enforced (`--fixtures-dir` + `--allow-synthetic-bytes` together → refused; neither → refused). Apply / rollback contract: each scheduled output is added to the rollback set BEFORE its `open('wb')` call so a half-written file at the failing path is still cleaned up on a mid-loop raise (the catch clause is `(Exception, SystemExit)`); post-condition re-reads every output and asserts the bytes match the magic signature; `image_manifest.json`, the plan file, AND the supplied `--descriptor-vocabulary` file are re-read and asserted byte-identical pre/post; a post-condition failure rolls back every output the runner wrote in this run.

`scripts/run_d_one_generation.py --self-test` exercises 34 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: synthetic happy path (PNG + JPG outputs land with magic-byte-valid bytes); end-to-end with `materialize_image_assets.py` (synthetic outputs flow into the workspace's declared `local_path` targets); fixture happy path (caller-supplied bytes copied verbatim); determinism (byte-identical outputs across two independent runs); provider-mode invariants (both flags refused as mutually exclusive; neither flag refused); fixture missing / wrong-magic / symlinked; pre-existing output target refused (no overwrite, prior bytes preserved); symlink at `--assets-dir` / `--fixtures-dir` / `--workspace` / `--descriptor-vocabulary` refused; URI-shaped `--workspace` / `--assets-dir` / `--plan` / `--fixtures-dir` / `--descriptor-vocabulary` refused at the string layer; plan request with unsupported extension (e.g. `.svg`) refused; **path-shaped id (e.g. `'a/b'`) refused at the flat-layout gate before any write, with no subdirectory created under `--assets-dir`** (regression for an id that would otherwise slip past `local_path_is_safe` and break the `materialize_image_assets.py` handoff); plan-validator gate fires before any output is written (tampered prompt with URL refused); manifest + plan byte-identical pre/post a successful run; mid-write rollback (monkey-patched `Path.write_bytes` raises mid-loop, every scheduled output unlinked, assets-dir returns to pre-call state); workspace missing `image_manifest.json` refused; plan file missing refused; end-to-end fixture mode with `materialize_image_assets.py` (`gamma.jpeg` flows through and lands at the manifest's `local_path` with byte-identical fixture bytes); no-extraneous-files guarantee (a successful synthetic run produces ONLY the `<id>.<ext>` outputs); **the `--descriptor-vocabulary` taxonomy round-trip** — a taxonomy-bearing plan + matching `--descriptor-vocabulary` round-trips through the synthetic runner with vocab bytes byte-identical post-run; a taxonomy-bearing plan WITHOUT `--descriptor-vocabulary` is refused (assets-dir untouched); plan drift refused at the runner boundary (a plan whose taxonomy value is no longer in `image_taxonomy.<dim>.allowed_values` is refused before any write); a taxonomy-free plan accepts an optional `--descriptor-vocabulary` without complaint; **AND the prompt-intent contract forwarding scenarios** — a plan that ONLY carries `text_policy` + `subject_domain` (no other taxonomy fields) round-trips through the runner with vocab byte-identical post-run, and the runner refuses the same plan when `--descriptor-vocabulary` is omitted.

```
python3 scripts/run_d_one_generation.py --self-test
```

When real D-One integration ships, it will replace the mock providers in this runner — the same `d_one_adapter_plan.json` input, the same `<id>.<ext>` output naming convention, the same `--assets-dir` flat-layout contract, and the same magic-byte gate downstream remain — but real D-One / MCP / model-API calls are intentionally **TODO** today and the runner refuses to operate without an explicit mock-mode flag.

### Pre-D-One local image-asset materialization gate

`scripts/materialize_image_assets.py` is the stdlib-only, deterministic **pre-D-One local asset gate**. Given a workspace that already ships a schema-valid `image_manifest.json` plus a `--assets-dir <dir>` of local PNG / JPG / JPEG asset files keyed by `<id>.<ext>` (where `id` matches an `images[].id` in the manifest and `<ext>` is the lower-cased extension of the declared `images[].local_path`), the helper either **copies** the asset bytes into the workspace paths declared by each `images[].local_path` when the target does not yet exist, or **verifies** the pre-existing target in place when it does — in both cases the post-condition is that every declared target is a regular non-symlink PNG / JPG / JPEG file whose first bytes match the magic-byte signature for the declared extension. The verify-or-copy semantics support two distinct workflows; `scripts/init_image_manifest.py` itself refuses to overwrite a pre-existing `image_manifest.json`, so the two workflows do NOT chain through it (each stands on its own, and the manifest must already be in place before materialize runs):
- **Direct-author workflow (no init helper):** the agent writes `<workspace>/image_manifest.json` directly (e.g. by copying an authored spec JSON verbatim), places PNG / JPG / JPEG asset bytes under `--assets-dir` keyed by `<id>.<ext>`, then runs materialize. Every declared target hits the **copy** branch and the bytes land at `<workspace>/<local_path>`. `init_image_manifest.py` is NOT invoked in this path — it would refuse to overwrite the already-written manifest;
- **init_image_manifest workflow (assets pre-placed):** the agent places PNG / JPG / JPEG asset bytes directly at `<workspace>/<local_path>` for every declared image, then runs `scripts/init_image_manifest.py --workspace ws --spec spec.json` (which requires every declared `local_path` to already resolve to an existing regular non-symlink file before it writes the manifest). materialize is then an OPTIONAL follow-up that takes the **verify-only** branch for every target — defense in depth, since `init_image_manifest` only checks file existence at each declared `local_path` and does NOT check PNG / JPEG magic bytes. `--assets-dir` is typically empty in this workflow (or carries the same bytes the agent originally placed; either is fine).

Re-running the helper on a fully-materialized workspace (whichever workflow produced it) with the same `--assets-dir` is idempotent: every target hits the verify branch, nothing is copied, no manifest field is mutated. **This is the pre-D-One local asset gate, not D-One itself.** The helper does **not** call D-One / Qoder / any public network / image generation / image search / model API / external service, does **not** open `input/source.md` for business content (so the source body never reaches a prompt or image field), does **not** invent prompts, does **not** mutate `image_manifest.json` (manifest bytes must be byte-identical before and after a successful run), does **not** generate full-slide screenshots, and does **not** produce `render_models/*`, `svg_previews/*`, or any `.pptx`. Two runs from a clean state into byte-identical inputs produce byte-identical bytes at each declared `local_path`.

```
python3 scripts/materialize_image_assets.py \
  --workspace path/to/stage6/workspace \
  --assets-dir path/to/local/assets/
```

Fail-closed gates (every gate aborts the run and rolls back any target this run wrote):

- `--workspace` must be an existing directory; URI-shaped values refused; the workspace must not itself be a symlink;
- `--assets-dir` must be an existing directory; URI-shaped values refused; the directory must not itself be a symlink, must not contain any subdirectory at its root (the contract is flat keyed by id+extension), and must not contain any symlink entry at its root (refused with the clear "symlink" diagnostic);
- `<workspace>/image_manifest.json` must be a regular non-symlink file that parses as JSON, decodes to an object, and validates against `schemas/image_manifest.schema.json`;
- no two `images[].id` values may be equal (defense-in-depth; `init_image_manifest` already refuses this on the producer side);
- every `images[].local_path` passes `validate_scaffold.local_path_is_safe`, every declared extension (lower-cased) is one of `.png` / `.jpg` / `.jpeg` (the embed surface `scripts/export_pptx.py` supports — SVG / GIF / WebP / TIFF / BMP / ICO and anything else fall back to the placeholder shape in the exporter and are refused here), every target `<workspace>/<local_path>` resolves inside the workspace, the leaf is not a symlink, and no parent directory along the path is a symlink;
- **per-image branch (verify vs. copy):** if `<workspace>/<local_path>` already exists, the leaf must be a regular file (not a symlink) and its first bytes must match the PNG / JPEG magic signature for the declared extension — this is the verify-only branch (no copy is scheduled and the corresponding `--assets-dir` file, if any, is left unused); if `<workspace>/<local_path>` does not yet exist, the source file `<assets-dir>/<id>.<ext>` must exist as a regular non-symlink file (the source name passes the same path-safety rule, so a path-shaped id like `../escape` is refused at the computed source-name gate), the source path resolves inside `--assets-dir`, and the file's first bytes match the PNG / JPEG magic-byte signature for the declared extension (so a `.png`-named file with JPEG bytes — or vice versa — is refused);
- every file at the `--assets-dir` root must be matched by some declared `<id>.<ext>` pair (undeclared files are refused outright; this check is independent of whether the matching target is pre-existing — an `--assets-dir` file that matches a declared id is never undeclared even when it ends up unused);
- the apply loop iterates only over scheduled copy entries (verify-only entries skip the loop entirely — nothing is written for them, and they are not added to the rollback set), reads source bytes, re-checks magic bytes against the full payload (defense-in-depth against a TOCTOU swap between the preflight head read and the apply), and copies via `Path.write_bytes`; each copied target is added to the rollback set BEFORE its `open('wb')` call so a half-written file at the failing path is still cleaned up on a mid-loop raise. The catch clause is `(Exception, SystemExit)`;
- the post-condition re-reads `image_manifest.json` and asserts the bytes are byte-identical to the pre-call state (the helper never opens the manifest for writing); every declared target must exist as a regular non-symlink file whose first bytes match the magic signature for the declared extension; a post-condition failure rolls back every target the helper wrote in this run (verify-only entries are not rolled back because the helper did not create them).

`scripts/materialize_image_assets.py --self-test` exercises in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path PNG, happy path JPG, happy path JPEG with mixed-case `.JPEG` extension, determinism between two independent runs (byte-identical target bytes), missing source asset, symlink source asset (refused with the clear "symlink" diagnostic, not the generic "escapes" one), three unsafe manifest `local_path` variants (URI / POSIX-absolute / `..` traversal), six unsupported-extension variants (SVG / GIF / WebP / TIFF / BMP / ICO), undeclared file in `--assets-dir`, duplicate `images[].id`, **pre-existing target with valid PNG bytes accepted as a verify-only entry** (post-init_image_manifest workflow — target bytes byte-identical pre/post), **pre-existing target with invalid bytes (wrong magic) refused at the verify-in-place magic gate**, **pre-existing valid target alongside a matching `--assets-dir` entry succeeds as verify-only** (the `--assets-dir` file is not flagged as undeclared and the target is not overwritten), **idempotency: first run copies, second run verifies in place; target bytes byte-identical across runs**, magic-byte mismatch (PNG-named file with JPEG bytes), URI-shaped `--workspace`, symlink `--workspace`, symlink `--assets-dir`, missing / malformed / symlink / schema-invalid `image_manifest.json`, empty manifest + empty assets-dir as an OK no-op, empty manifest + rogue assets-dir entry refused at the undeclared gate, assets-dir subdirectory refused (the contract is flat), symlink sibling in assets-dir refused at the dir-enumeration gate, manifest bytes byte-identical pre/post a successful run (proves the helper does not invent prompts or mutate fields), source-body marker phrase NEVER copied into any written field or target bytes or stdout/stderr (helper does not read `input/source.md` for content), mocked mid-loop write failure rolling back every target including the partial one at the failing path, id containing path-shaped characters refused at the computed-source-name safety gate, symlink at the declared target path refused with the dangling target never created, and symlink at a parent directory of the target refused with the outside directory never written to.

```
python3 scripts/materialize_image_assets.py --self-test
```

When real D-One integration ships, it will plug in upstream of this gate: D-One produces local PNG / JPG / JPEG bytes under a controlled local directory, the agent places them under `--assets-dir`, and `materialize_image_assets.py` then enforces every safety rule above before any byte lands in the workspace. See `references/d-one-image-policy.md` for the policy that any such integration must satisfy before it ships, `references/d-one-live-integration-design.md` for the narrow design (allowed / forbidden inputs, prompt redaction, output rules, fail-closed conditions, audit evidence, manual approval boundaries) the future real-D-One wiring must follow, and `references/d-one-live-run-readiness.md` for the enumerable PASS-gate checklist (endpoint allow-list, descriptor vocabulary, audit evidence fields, manual approval boundaries, failure / stop conditions, exact evidence required for PASS) plus the not-yet-wired contract sketch in `schemas/d_one_live_run_evidence.schema.json`. The R1 endpoint allow-list, V1 descriptor vocabulary, and pre-call audit-out path-placement preflight contracts are sketched (NOT wired) at `schemas/d_one_endpoint_allowlist.schema.json` / `schemas/d_one_descriptor_vocabulary.schema.json` / `schemas/d_one_preflight_audit.schema.json` with synthetic placeholders under `examples/d_one_endpoint_allowlist_template.json` / `examples/d_one_descriptor_vocabulary_template.json` / `examples/d_one_preflight_audit_template.json` and a consolidating reference at `references/d-one-live-prerequisites.md`. Mock / local path is verified today; real D-One is not.

The complementary **source-attached image asset propagation contract** is sketched at `schemas/source_image_asset.schema.json` with a synthetic placeholder under `examples/source_image_asset_template.json` and the propagation rules under `references/source-image-asset-policy.md`. It describes how local PNG / JPG / JPEG assets that arrived attached to a source declare a stable `id`, a `source_ref` linking back to `source_manifest.source.id`, an `input/assets/...` `local_path`, an `assets/...` `destination_path` (the value `image_manifest.images[].local_path` is expected to carry), a `media_type` enum-locked to `image/png` / `image/jpeg`, a positive `byte_count`, and a lowercase-hex `sha256` — with the schema's path patterns and media_type enum refusing URL / `file://` / `data:` schemes, absolute paths, `..` traversal, mixed-case extensions, and unsupported formats at the schema level. A narrow stdlib-only read-only **registry validator** ships at `scripts/validate_source_image_assets.py`: it schema-validates `<workspace>/source_image_assets.json` and enforces unique ids, `source_ref == source_manifest.source.id` (when a sibling `source_manifest.json` is present), `local_path` / `destination_path` safety + within-workspace + no-symlink (source leaf and the parent chain of both paths), source asset is a regular file whose on-disk `byte_count` + `sha256` match the registry, `media_type` agrees with the extension AND the on-disk PNG / JPEG magic-byte signature, (when a sibling `image_manifest.json` is present) for every registry `assets[].id` that ALSO appears as `image_manifest.images[].id`, the matching entry has `source == "local_asset"` with `local_path == registry destination_path` (G10 — alignment, with the original "silently tolerate registry-not-in-manifest" contract preserved unchanged), AND every registry `assets[].id` is declared in `image_manifest.images[].id` — a registry id the manifest does not declare fails closed under the separate G13 completeness gate, so a future relaxation of one direction can flip exactly one gate without disturbing the other — and a deny scan on every `id` / `source_ref` / `local_path` / `destination_path` value that — after the field is lower-cased and stripped of every non-alphanumeric character — refuses the COMBINATION of the marker `public` with any of the propagation-verb / surface markers `upload`, `share`, `sharing`, `url`, `link`, `post`, `publish`, `distribut`. Refusing the combination (rather than a fixed list of pre-baked compound tokens) closes every schema-permitted bypass — separator permutations like `public-upload` / `Public_Upload`, word-order permutations like `share_public` / `share_to_public`, morphology permutations like `publicly_shared` / `public_sharing` / `public_distribution`, and intervening-token permutations like `public_share_url` all collapse to `public` + verb and trip the deny under one rule. `public` alone (`public_chart` / `publication_meta`) and verbs alone (`share_image` / `upload_chart` / `permalink_marker`) both pass — only the combination is policy-violating. Safe synthetic ids like `source_hero` / `product_marker` / `local_chart_asset` contain neither marker and pass. The validator does NOT call D-One / any network / any model API, does NOT mutate any file, and does NOT perform pipeline propagation — `scripts/materialize_image_assets.py` is still the only helper that copies bytes, and the D-One descriptor taxonomy / real-D-One UNVERIFIED status are unchanged. Run `python3 scripts/validate_source_image_assets.py --self-test` for the in-script tempfixture scenarios covering every gate.

### D-One live-run evidence validator (does NOT run D-One)

`scripts/validate_d_one_live_run_evidence.py <path/to/record.json>` is a stdlib-only, single-file, **non-mutating** local validator for a future D-One live-run evidence record. **This validator does NOT call D-One / Qoder / MCP / any image-generation model / any public network / any external service; it does not run a live call; it does not mutate any file.** A schema-PASS result here is a **necessary, not sufficient** precondition for flipping `references/d-one-live-run-readiness.md`'s status from `UNVERIFIED` to `VERIFIED` — A19 (re-binding the pinned `plan_sha256` to the actual plan on disk via `scripts/done_image_adapter.py --validate-plan`) and the `references/d-one-live-integration-design.md` §11 live verification matrix still have to be exercised separately.

```
python3 scripts/validate_d_one_live_run_evidence.py path/to/evidence.json
```

Gates layered on the supplied file: G1 readable JSON object (existing regular non-symlink file, parses, object root — a list / string / number / bool / null root is refused); G2 schema (A10 — against `schemas/d_one_live_run_evidence.schema.json` under the same draft-07 subset `scripts/validate_artifacts.py` implements; missing required fields, unknown enums, additional properties, URI-shaped free-form fields all refused at this layer); G3 per-request outcome consistency (A12); G4 run_outcome consistency + stop-condition recording (A13 — `run_outcome != "ok"` requires `gate_fired` from F1-F17); G5 cleanup_ref consistency (A14); G6 plan_request_count == len(requests) (A15 local half — the plan-binding half is A19 territory, covered by `done_image_adapter.py --validate-plan` separately); G7 strict per-image-review coverage (A16 — no missing, orphan, or duplicate review row; for fully-failed runs where no request reached `outcome == "ok"`, both sides of the set-equality are empty and the record MUST carry `approvals.per_image_review_refs: []`; the schema's `minItems` on that array is `0` for exactly this case); G8 materialized_sha256 == output_sha256 when both present (A17); G9 requests sorted by id (A18 local half); G10 PASS overclaim guard (readiness §8 — `run_outcome == "ok"` requires every request `outcome == "ok"` AND every review `decision == "pass"`); G11 free-form field scan (A11 + readiness §6 + privacy rules — credentials, PII, employee-id literals, private-IPv4 / internal-TLD shapes, raw-source markers, public-upload / public-sharing / public-hosting phrases — `upload to public ...` / `public upload` / `share publicly` / `public hosting` / `publish to web` / `public URL` — with safe negated boundary wording such as `no public upload`, `public upload is forbidden`, `public hosting is not used` exempted). Refusal scope explicitly includes: malformed JSON; list-rooted JSON; missing required fields; unknown PASS claims without required evidence; credentials / tokens; internal endpoints; employee IDs; real-business-data markers; public-upload / public-sharing / public-hosting instructions.

```
python3 scripts/validate_d_one_live_run_evidence.py --self-test
```

Exits non-zero on any gate failure; PASS prints a note that the validator does NOT run D-One and that PASS here is necessary, not sufficient.

For the first company-machine live trial, see `references/d-one-live-trial-packet.md` — a single concrete checklist (preconditions, run order, stop conditions, evidence to collect, when to stop adopting the packet) layered on top of `d-one-live-run-readiness.md`. A synthetic schema-valid AND cross-check-valid baseline ships at `examples/d_one_live_run_evidence_template.json`; the operator copies it, replaces every placeholder value with what the trial actually produced, and re-runs `scripts/validate_d_one_live_run_evidence.py` against the result. Adopting the packet does NOT change the readiness file's `Real D-One path: UNVERIFIED` wording — only an actual trial whose audit record passes the validator AND every §8 PASS gate does.

### Stage-1-to-6 local orchestration helper

`scripts/prepare_workspace.py` is the stdlib-only, deterministic local orchestrator that chains the six existing per-stage contract helpers — `init_workspace.py` → `init_deck_brief.py` → `init_deck_plan.py` → `init_design_system.py` → `init_slide_plans.py` → `init_image_manifest.py` — in canonical order from explicit caller-supplied inputs into a workspace that `scripts/run_pipeline.py` can later consume. **This is explicit-input orchestration only — it is NOT a full prompt/report/Markdown-to-PPTX automation.** Every stage's content comes from the caller (CLI flags or JSON spec files); the orchestrator never parses `input/source.md` for business content, never invents `deck_brief` / `deck_plan` / `design_system` / `slide_plans` / `image_manifest` content, never generates any image asset / `render_models/*` / `svg_previews/*` / `.pptx` / pipeline report, never calls D-One / Qoder / any public network / image generation / telemetry / external service, and never modifies files outside `--workspace`.

```
python3 scripts/prepare_workspace.py \
  --workspace /path/to/new/workspace \
  --source /path/to/source.md \
  --title "Deck title" \
  --audience "Internal review" \
  --objective "Show X and Y" \
  --plan-spec /path/to/plan_spec.json \
  --theme-from-template \
  --template-root templates/layouts \
  --slide-specs-dir /path/to/slide_specs \
  --image-manifest-spec /path/to/image_manifest_spec.json
```

`--design-system-spec <path>` is accepted in place of `--theme-from-template` (mutually exclusive; exactly one must be supplied). Optional flags `--source-id`, `--tone`, `--language`, and `--approximate-slide-count` are forwarded to the matching stage helpers. Fail-closed semantics: the first stage that returns a non-zero exit code halts the run; every downstream stage is marked SKIPPED and is NOT invoked, so no half-baked workspace advances into a later stage. Each stage helper owns its own rollback contract — the orchestrator does NOT roll back earlier-stage artifacts when a later stage fails (the same as running the helpers manually one at a time). A caller who needs a clean retry should delete the workspace directory and rerun with corrected inputs.

`scripts/prepare_workspace.py --self-test` exercises tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path through Stage 6 with a two-slide `cover` + `key_message` fixture against the `business_review` template (every Stage-1-through-6 artifact lands on disk; canonical stage order is observed), per-stage failure short-circuit (each of the six stages, in turn, is forced to fail with a minimal targeted-bad input — empty source, empty title, malformed plan_spec, mutually-exclusive design-system arguments, missing specs-dir, malformed image-manifest spec — and every downstream stage is verified SKIPPED), pre-existing unsafe workspace refused at Stage 1 (symlink at `--workspace`; the symlink target directory is verified untouched), pre-existing non-empty workspace refused at Stage 1 with prior contents preserved byte-identical, the source-body marker phrase appears in `input/source.md` only and never in any generated artifact, determinism between two independent runs from byte-identical inputs into two different empty workspaces (every Stage-1-through-6 artifact is byte-identical across the two runs), and an end-to-end check that the prepared workspace passes `scripts/validate_workspace.py` against the `business_review` template (the same gate `scripts/run_pipeline.py` applies up front).

```
python3 scripts/prepare_workspace.py --self-test
```

After `prepare_workspace.py` succeeds, `scripts/run_pipeline.py` can take over for stages 7–10.

### Single-artifact structural validation

```
python3 scripts/validate_artifacts.py \
  --schema schemas/deck_brief.schema.json \
  examples/synthetic_20_page_business_review/deck_brief.json
```

(Any example workspace can be substituted; the upper-range 20-page fixture is shown here, but any deck shape under `examples/` works the same way.)

The validator implements a **subset** of JSON Schema (`type`, `required`, `enum`, `pattern`, `minLength`/`maxLength`, `minimum`/`maximum`/`exclusiveMinimum`/`exclusiveMaximum`, `minItems`/`maxItems`, nested `properties`, `additionalProperties` boolean or schema, and `items` as a single subschema). Full JSON-Schema validation is TODO and the script labels its output `(subset)`.

### Scaffold validation runner

`scripts/validate_scaffold.py` exercises every check the scaffold currently supports in one pass:

```
python3 scripts/validate_scaffold.py
```

It runs:

- **Positive:** every synthetic fixture under each example workspace in `examples/` (currently `synthetic_20_page_business_review` — the upper-range stress-test fixture — and `synthetic_8_page_product_brief` — a lower-range fixture) validates against its schema (`deck_brief`, `deck_plan`, `design_system`, each `slide_plan`, `image_manifest`, and — when the workspace ships any — each `render_models/*.json` against `render_model.schema.json`). The runner discovers workspaces by inspecting `examples/`; no example name is hardcoded.
- **Negative:** removing a required field from each artifact in memory must fail validation.
- **Path-safety:** workspace-relative paths only. Rejects the empty string, POSIX-absolute (`/...`), leading-backslash, protocol-relative (`//host/...`), **any URI-like scheme prefix** matching `^[A-Za-z][A-Za-z0-9+.\-]*:` (covers `http://`, `https://`, `file://`, `s3://`, `ftp://`, `data:`, `mailto:`, `javascript:`, and anything else of that shape — which also subsumes Windows drive prefixes like `C:\...`, `D:/...`, `c:foo`), and any path containing a `..` segment. Applied to both `image_manifest.local_path` and `template.theme_ref`.
- **Media resolution (fail-closed):** every `image_manifest` `local_path` must resolve to a real file under the workspace, and every `slide_plan.image_refs` entry must point to an id declared in the manifest. Enforces `references/security-policy.md` item 6.
- **Layout-aware slide plans:** every required slot declared by a template layout is covered by a matching block in the slide plan (`block.id == slot.id`, `block.kind == slot.type`). Negative cases prove that a dropped required block or a wrong block kind is detected.
- **Planner semantics:** the same machine-enforced planner contract as the workspace runner — `deck_brief.source_refs` required and non-empty, `planning.planned_slide_count` matches `len(slides)`, `sections[]` partition the deck's slide indices, every `slides[].section_id` resolves to an existing section that lists this index, and every `slides[].source_refs` value is declared in `deck_brief.source_refs`. The check runs against every discovered workspace; no slide-count or example name is hardcoded.
- **Stage-1 (Intake) bridge:** `scripts/validate_scaffold.py` invokes the same `check_source_manifest_bridge` helper the workspace validator uses (lazy-imported from `validate_workspace`). When a discovered workspace ships `source_manifest.json`, the bridge validates it against `schemas/source_manifest.schema.json`, cross-checks `source.local_path` against the on-disk `input/source.md` (byte count / line count / sha256 / UTF-8 validity), confirms `source.kind` is one of the schema-documented values (`markdown` / `text` — `init_workspace.py` normalizes the filename for both `.md` and `.txt` originals, so both kinds are legitimate against the canonical `input/source.md`), and — when `deck_brief.json` is present — requires `deck_brief.source_refs` to declare `source_manifest.source.id`. Workspaces without `source_manifest.json` are a no-op so the existing prepared examples remain valid.
- **Template / theme / layout cross-check:** `template.json`, `theme.json`, and every layout file validate against their schemas; the template's `name` matches its containing directory; `theme_ref` passes a string-only path-safety check **and then** resolves to a path under the template directory; layout filenames match the declared `name`; and the template's `layouts` list matches the files on disk. The `theme_ref` guards run in two stages and are **fail-closed**: stage 1 (`local_path_is_safe`) is string-only and short-circuits before any filesystem call; only a stage-1 pass allows stage 2 (which calls `Path.resolve()` to catch symlink escape), and only a stage-2 pass allows the theme file to be opened. The same gate shape is mirrored in the negative loop, so the stage-2 helper is never called on a string-unsafe `theme_ref`. A meta-check iterates every unsafe `theme_ref` in the negative list through the loader under recorders for `_resolves_within` and `_load`, and asserts both recorders stayed empty and the loader emitted its "NOT opened (fail-closed)" result every time. Other negative cases prove that unsafe `theme_ref` values and a `template.name` mismatch are rejected.
- **Render model schema-level negatives:** a clean in-memory baseline `render_model` validates, and every required negative mutation is rejected by the schema: unsupported `kind` (`svg`, `foreignObject`), missing `bounds`, invalid token refs (no prefix, wrong domain, unknown typography role), `image_ref` carrying an `http://` / `https://` / `file://` / `data:` URI / POSIX-absolute / path-traversal / Windows drive prefix, and arbitrary SVG-like fields on a primitive or at the root (`transform`, `viewBox`, `href`, `xlink_href`, `foreignObject`, `filter`, `xmlns`, `defs`, `path`). The schema's `additionalProperties: false` at every nested object plus the pattern restrictions on ids and token refs are what enforce this. The runtime cross-checks (kind/payload alignment, bounds inside canvas, slot resolution, image_ref vs manifest, palette token resolution) are exercised by `validate_workspace.py`'s tempfixtures.

Exit code is `0` only when every check (positives **and** negatives) behaves as expected.

### Workspace validator

`scripts/validate_workspace.py` validates an **arbitrary** workspace directory (the workspace path is caller-supplied; nothing is hardcoded). Pair it with a template-root directory containing the templates. The validator is agnostic to deck length — it works on a 6-slide deck or a 25-slide deck:

```
python3 scripts/validate_workspace.py \
  --workspace examples/synthetic_20_page_business_review \
  --template-root templates/layouts

python3 scripts/validate_workspace.py \
  --workspace examples/synthetic_8_page_product_brief \
  --template-root templates/layouts
```

It runs (every check is fail-closed; exit 1 on any failure):

- **Schemas:** every artifact in the workspace (`deck_brief.json`, `deck_plan.json`, `design_system.json`, `image_manifest.json`, and every `slide_plans/*.json`) loads and validates against its schema. Missing or malformed artifacts produce a `[FAIL]` line — never a Python traceback.
- **Stage-1 (Intake) bridge (when `source_manifest.json` is present):** the manifest must be a **regular in-workspace file** — a symlink at that path (broken or resolvable) is refused outright by a symlink preflight that runs BEFORE `_try_load` / `read_text`, so neither a dangling link silently demoting the workspace to the legacy "no manifest" no-op nor a resolvable link reading manifest bytes from outside the workspace can slip through; the manifest validates against `schemas/source_manifest.schema.json`; `source.local_path` (enum-locked to `input/source.md`) resolves inside the workspace and the file exists as a regular UTF-8 file (symlinks refused) whose `byte_count`, `line_count`, and `sha256` match the manifest, and `source.kind` is one of the schema-documented values (`markdown` for a .md-original source, `text` for a .txt-original source — `init_workspace.py` normalizes the on-disk filename to `input/source.md` regardless of the original extension, so both kinds are legitimate against the canonical filename). When `deck_brief.json` is also present, `deck_brief.source_refs` must declare `source_manifest.source.id`. Workspaces without `source_manifest.json` are a no-op (the prepared examples below pre-date `init_workspace.py` and remain valid). The bridge is verification-only — it does not plan a deck, mutate the workspace, or call any external service. The same `check_source_manifest_bridge` is invoked per-workspace by `scripts/validate_scaffold.py`. Tempfixture negatives cover the positive match, missing `input/source.md`, sha256 mismatch, bad `local_path` (schema enum lock), invalid UTF-8, schema-invalid `kind` rejected at the schema layer, a `.txt`-intake positive (`kind="text"` against `input/source.md` is accepted), a deck_brief whose `source_refs` omits the manifest's `source.id`, **malformed `source_manifest.json` (non-JSON bytes) reported as a structured loadability FAIL without traceback**, **list-rooted `source_manifest.json` failing closed on the shape gate without traceback**, a **Stage-1-only workspace (manifest + source.md present, `deck_brief.json` absent) passing the bridge with the deck_brief cross-check row skipped rather than emitted as a FAIL**, a **broken symlink at `source_manifest.json` failing closed at the symlink preflight (dangling target never created — proves `Path.is_file()`'s false-False does not demote the workspace to the legacy no-op)**, and a **resolvable symlink at `source_manifest.json` failing closed at the same preflight BEFORE `read_text` can follow the link (outside target bytes preserved byte-identical, no schema-validate row emitted)** — these five cases are what `scripts/init_deck_brief.py` relies on to fail closed before writing anything.
- **Template chain:** `deck_plan.template` is path-safe **and** resolves inside `--template-root`; every `deck_plan` slide layout is declared by the template's `layouts` list.
- **Template files (full):** the selected template's `template.json` validates against its schema; `template.name` matches its directory; `theme_ref` passes the two-stage path-safety + within-template-dir gate, the theme file exists, and `theme.json` validates against `schemas/theme.schema.json`; every declared layout has a `layouts/<name>.json` file that validates against `schemas/layout.schema.json` and whose file stem matches `layout["name"]`.
- **Slide-plan coverage (strict 1:1):** `deck_plan` slide indices must be unique (duplicates in `deck_plan.slides[].index` `[FAIL]` before any further analysis); every `deck_plan` slide has exactly one matching `slide_plan` file (matched by JSON `index`); duplicate `slide_plan` indices, orphan `slide_plan` indices (no deck_plan entry), and missing indices (deck_plan slide with no `slide_plan`) all `[FAIL]`. Every `slide_plan` agrees with its deck_plan entry on `index`/`layout`/`title`, and every required layout slot is covered by a matching `slide_plan` block (`block.id == slot.id`, `block.kind == slot.type`).
- **Planner semantics (machine-enforced contract):** `deck_brief.source_refs` is required and non-empty; `deck_plan.planning.planned_slide_count` equals `len(deck_plan.slides)`; `deck_plan.sections[].slide_indices` partition the set of `slides[].index` values (no duplicates across sections, no missing deck indices, no orphan section indices); every `slides[].section_id` resolves to an existing section that lists this slide's index; and every `slides[].source_refs` value is declared in `deck_brief.source_refs`. No maximum slide count and no required layout sequence is imposed.
- **Image manifest:** every `local_path` passes the same path-safety rule as `validate_scaffold.py` (any URI-like scheme prefix, absolute paths, leading-backslash, protocol-relative, `..` segments, and the empty string are rejected); every safe path resolves to a real file inside the workspace; every `slide_plan.image_refs` id is declared in `image_manifest`.
- **Render models (when present):** if the workspace ships any `render_models/*.json`, each validates against `schemas/render_model.schema.json` and then cross-checks against the rest of the workspace — index matches a `deck_plan` slide, layout matches that slide's layout, canvas equals `design_system.grid`, `source_refs` are declared in `deck_brief.source_refs`, primitive ids are unique, bounds fit inside the canvas, exactly one kind-specific payload field is present and matches `kind`, `slot_id` (when set) resolves to a layout slot whose `primitive_kind` (or the default `slot.type` → primitive mapping) matches and whose `bounds` (when declared) contain the primitive's bounds, `image_ref` is declared in `image_manifest`, palette tokens resolve to a `design_system.palette` key, and no reference-field string carries a URI-scheme prefix. The `render_models/` directory is optional today — workspaces that don't ship one are silently skipped.
- **Render-model coverage (anti-drift):** once a workspace ships at least one `render_models/*.json`, every deck_plan slide whose `layout` is in the render-model generator's `SUPPORTED_LAYOUTS` set must have a matching render_model on disk. A committed workspace cannot quietly ship a partial subset (e.g. `01_cover.json` only while the deck declares twenty supported slides). The check imports `SUPPORTED_LAYOUTS` from `scripts/generate_render_models.py` lazily so the two always agree, and is fully **adaptive** — no example name, no slide count, and no template name is hardcoded. Slides whose layout is **not** in the supported set are explicitly allowed to have no render_model; every layout declared by the business_review template skeleton is now in `SUPPORTED_LAYOUTS`. An empty `render_models/` directory still means "not yet generated" and does not trigger this check.
- **Render-model filename (canonical name):** every `render_models/*.json` file must be named `<index:02d>_<layout>.json`, where `index` and `layout` come from inside the JSON (zero-padded two-digit index, layout token verbatim — e.g. `02_agenda.json`, `09_two_column.json`). The PPTX exporter (`scripts/export_pptx.py`) iterates `deck_plan.slides[]` in declared order and resolves each entry to its canonical render_model filename, so a mis-named `99_agenda.json` whose JSON `index` is 2 fails closed at the deck_plan/render_models 1:1 coverage gate (the canonical `02_agenda.json` is missing, the mis-named file is orphan) before any output is produced. The validator fails closed with FAIL lines that name both the missing and the orphan filename. Schema-invalid files (no integer `index`, no string `layout`) are not double-flagged here — `check_render_models` already surfaces those. The generator always writes canonical names; this on-disk gate catches hand-edited or copied workspaces.
- **Slide-plan filename (canonical name):** every `slide_plans/*.json` file must be named `<index:02d>_<layout>.json`, where `index` and `layout` come from inside the JSON (same zero-padded two-digit shape as the render-model gate — e.g. `02_agenda.json`, `04_two_column.json`). `scripts/init_slide_plans.py` always writes the canonical name (Stage-5 producer-side commitment); the workspace validator's `slide_plans filename` gate is the on-disk side that catches a hand-edited or migrated workspace whose name drifts from its JSON `index`/`layout`. Misnamed files, filename/index mismatches, filename/layout mismatches, and a duplicate canonical/non-canonical pair for the same JSON index (canonical sibling passes, non-canonical sibling fails) all fail closed with FAIL lines naming the expected canonical filename. Schema-invalid slide_plans (missing `index` / `layout`, wrong types, empty layout) emit zero rows from this gate — `check_schemas` and `check_slide_plan_coverage` already surface those, so the maintainer sees the schema error once rather than confusing duplicate noise. Tempfixture negatives prove each behavior (canonical pass, misnamed, index mismatch, layout mismatch, duplicate canonical/non-canonical, schema-invalid file silently skipped).
- **SVG previews (when render_models ship):** each `render_models/<stem>.json` must have a matching `svg_previews/<stem>.svg`. The SVG must parse, the root `<svg>` must declare a `viewBox` of `0 0 <width> <height>` matching the render_model canvas, no `<foreignObject>` may appear anywhere in the tree (the element-name comparison is **case-folded** so `<foreignobject>` / `<FOREIGNOBJECT>` / `<ForEignObJecT>` are all detected — the HTML5 parser's SVG element-name adjustment table case-corrects every variant back to canonical camelCase before the inline SVG renders, so a case-sensitive validator would miss the bypass while the browser still rendered any `<script>` the foreign object hosts; the geometry walk and the reference-attribute walk apply the **same case-fold rule to BOTH element tags AND attribute keys** — `<RECT X="3000" Y="3000" WIDTH="5000" HEIGHT="5000"/>` still trips the out-of-canvas FAIL because HTML5 also lowercases attribute names, so a case-sensitive `attrib["x"]` would otherwise `KeyError` and silently skip the element; AND any element in the tree — **including the root `<svg>`** — carrying duplicate case-folded attribute keys is refused outright (e.g. `<rect X="3000" ... x="0" ...>` on a geometry element OR `<svg VIEWBOX="0 0 99999 99999" viewBox="0 0 1920 1080">` on the root, where an attacker could otherwise case-fold-collide the viewBox attribute and redefine the canvas to absorb out-of-canvas geometry), because HTML5's first-wins resolution after case-folding would diverge from a naive last-wins dict-build), every reference-bearing attribute (`href`, `xlink:href`, `src`, anything ending in `href`) must pass the same path-safety rule as `image_manifest.local_path` (no URI scheme, no POSIX-absolute, no leading backslash, no protocol-relative, no `..` segment, no empty string), every `<image>` href value must match a `local_path` declared in `image_manifest`, every `<rect>` / `<image>` / `<ellipse>` / `<circle>` / `<line>` with explicit numeric geometry must stay inside the canvas, and every `<text>` with numeric `x`/`y` must have its anchor point inside the canvas. The validator does **not** enforce a `<text>` width / height / wrapping box — that needs font metrics this stdlib-only validator does not carry, and is recorded as a TODO in `references/svg-design-rules.md`. Tempfixture negatives prove the validator catches each forbidden mutation (missing svg, malformed XML, wrong root, viewBox mismatch, `<foreignObject>` present in canonical camelCase **and** every case variant — lowercase, UPPERCASE, wacky-case — all detected, unsafe `href` of every shape, undeclared `<image>` href, `<rect>` outside canvas, `<text>` anchor outside canvas — both negative and beyond-edge).
- **Negative tests (in-memory + tempfixture):** the runner explicitly proves the validator detects unsafe schemes (`s3://`, `ftp://`, `data:`, `mailto:`, `javascript:`), absolute paths (POSIX, Windows drive, UNC), path traversal (`..` segments), missing media, unknown layouts, missing slide_plans, orphan slide_plans, duplicate `slide_plan` indices, **duplicate `deck_plan` indices** (no false-green), required-slot mismatches, wrong block kinds, missing theme files, invalid theme schemas, invalid layout schemas (extra fields), missing declared layout files, missing `deck_plan.json` (no traceback), every planner-semantics rule (planned-count mismatch, missing / duplicate-across-sections / orphan section indices, unknown / mis-listed slide `section_id`, undeclared slide `source_refs`), and every render-model rule (unsupported `kind`, missing `bounds`, invalid token refs, external URL / `file://` / absolute path / path traversal in `image_ref`, arbitrary SVG-like fields, kind / payload mismatch, bounds outside canvas, unknown `slot_id`, `slot.primitive_kind` mismatch, `image_ref` not in manifest, unknown palette token, duplicate primitive ids). Tempfixture cases build their bad fixtures under `tempfile.TemporaryDirectory()` so nothing synthetic leaks into the repo.

### Render-model generation

`scripts/generate_render_models.py` produces `render_models/<index:02d>_<layout>.json` for the **supported layouts only** (`cover`, `section_divider`, `executive_summary`, `key_message`, `two_column`, `kpi_dashboard`, `timeline`, `agenda`, `conclusion`, `comparison_table` — every emit uses only the controlled `text` / `line` / `shape` / `image_slot` / `kpi` / `table` primitive kinds). The canonical filename is `<index:02d>_<layout>.json` (zero-padded two-digit `index` + `layout` token from the JSON, e.g. `02_agenda.json`); the workspace validator's `render_models filename` gate enforces this on disk so the exporter's deck_plan-driven 1:1 coverage gate doesn't see a mis-named file as orphan. It reads `deck_brief.json`, `deck_plan.json`, `design_system.json`, `image_manifest.json`, and the matching `slide_plans/*.json`, plus the chosen template's layout slot definitions. Layouts that map to the `chart_placeholder` primitive_kind (anything chart-bearing) are listed as `[SKIP]` and reported as not implemented — they are **not** counted as success.

```
python3 scripts/generate_render_models.py \
  --workspace examples/synthetic_20_page_business_review \
  --template-root templates/layouts

python3 scripts/generate_render_models.py \
  --workspace examples/synthetic_8_page_product_brief \
  --template-root templates/layouts
```

Determinism: the generator never invents source content. Text comes from `slide_plan.blocks[].content`; KPI rows come from `slide_plan.blocks[id="kpis"].content`; image refs come from `slide_plan.blocks[id="accent"].content` and must be declared in `image_manifest`. Bounds use the layout slot's `bounds` when set, otherwise the deterministic fallback table at the top of the script. The script fails closed on missing / malformed inputs, an unsafe `deck_plan.template`, an unknown image_ref, malformed kpi entries, a missing required slide_plan block, a stale / mismatched slide_plan (matched by JSON index but whose `layout` or `title` disagrees with the deck_plan slide), or an output that fails `render_model.schema.json` or the workspace cross-checks. Before generating, the script also removes **every** pre-existing `render_models/*.json` file — the workspace validator schema-validates every `*.json` in that directory as a render_model, so the `*.json` namespace is generator-owned. This means a stale render_model from a previous good run cannot survive a current-run fail-closed mismatch, a deck_plan change that moves the slide to an unsupported layout, **or a slide being removed from `deck_plan` entirely (orphan)**. Non-JSON files (READMEs, `.md` / `.txt` notes) are preserved. After writing, the script re-runs the same `check_render_models` gate the workspace validator uses, so output drift fails immediately.

### SVG preview generation

`scripts/generate_svg_previews.py` reads `render_models/*.json` and writes `svg_previews/<stem>.svg` for each. It consumes `render_model.json` only — never `slide_plan.json` — so the controlled primitive contract cannot be bypassed (see `references/svg-design-rules.md`).

```
python3 scripts/generate_svg_previews.py \
  --workspace examples/synthetic_20_page_business_review \
  --template-root templates/layouts

python3 scripts/generate_svg_previews.py \
  --workspace examples/synthetic_8_page_product_brief \
  --template-root templates/layouts
```

The renderer supports the primitive kinds the generator emits today — `text`, `line`, `shape`, `image_slot`, `kpi`, `table`. Any other kind (`chart_placeholder`, or any future kind) fails closed on that slide. The `table` primitive is rendered as a composite `<g>` with a bold header row, grid `<rect>` outline, and one `<text>` per cell — a visual preview of the native PPTX table the exporter emits. Token resolution: `palette.*` resolves to a raw hex via `design_system.palette.X` (the resolved value must match `^#[0-9A-Fa-f]{6}$`); `typography.heading|body` resolves to a `(font_family, size_pt)` pair via `design_system.typography.X` (`font_family` must match the CSS-style fallback-chain pattern); an unresolved or schema-violating token fails closed at the resolver, including for the background `<rect>` (no direct dict bypass). Image references resolve through `image_manifest.images[].id -> local_path`; the resolved path must pass the same path-safety rule the workspace validator enforces (no URI scheme, no POSIX-absolute, no leading backslash, no protocol-relative, no `..` segment, no empty). **Preflight** (runs before any cleanup): `design_system.json` and `image_manifest.json` are schema-validated, and every `image_manifest.images[].local_path` is run through `local_path_is_safe`. A preflight failure exits non-zero, prints no `OK:`, and does **not** delete any pre-existing `svg_previews/*.svg`. Before generating, every existing `svg_previews/*.svg` is removed (the `*.svg` namespace is generator-owned), so a stale preview cannot survive a fail-closed mismatch or a render_model deletion; non-SVG files (READMEs, `.md` / `.txt` notes) are preserved. After writing, the script re-runs the same `check_svg_previews` gate the workspace validator uses, so output drift fails immediately.

### PPTX export

`scripts/export_pptx.py` is driven by `<workspace>/deck_plan.json`: it iterates `deck_plan.slides[]` in declared order, resolves each entry to its canonical `<workspace>/render_models/<idx:02d>_<layout>.json`, and writes one native editable `.pptx` to `--output`. The exporter does NOT just glob `render_models/*.json` — a slide declared by `deck_plan` whose render_model is missing on disk fails closed before any output is written, as does an orphan render_model not declared by `deck_plan`. It is **stdlib-only**, deterministic (fixed ZIP timestamp, deterministic shape ids), and intentionally narrow.

```
python3 scripts/export_pptx.py \
  --workspace examples/synthetic_8_page_product_brief \
  --output /tmp/szh-ppt-master-smoke/synthetic_8_page_product_brief.pptx
```

Supported subset:

- layouts: `cover`, `kpi_dashboard`, `agenda`, `section_divider`, `executive_summary`, `key_message`, `two_column`, `timeline`, `conclusion`, `comparison_table`. The slide-body emitter is layout-agnostic — it walks each render_model's `primitives` list — so widening the layout allow-list does not change how any single shape is rendered. Every layout declared by the business_review template skeleton is currently in this allow-list;
- primitive kinds: `text` (editable text frame), `line` (native `<p:cxnSp>` connector), `shape` (native `<p:sp>` with prstGeom rect / roundRect / ellipse), `kpi` (editable `<p:sp>` text frame with stacked label / value / optional delta paragraphs), `image_slot` (native `<p:pic>` with `<p:blipFill>` + `<a:blip r:embed="rIdN"/>` for PNG / JPG / JPEG manifest entries — bytes copied into `ppt/media/imageN.<ext>`, deterministic by manifest order — and a native placeholder rectangle whose `<a:txBody>` carries the `image_manifest` alt_text for every other extension; SVG / GIF / WebP embedding remains TODO), and `table` (native `<p:graphicFrame>` wrapping `<a:tbl>` with one `<a:gridCol>` per column, a bold header row, and an editable `<a:txBody>` per cell — every cell is directly editable in PowerPoint, no media embedding required).

Fail-closed gates:

- output extension must be `.pptx` (case-insensitive);
- `design_system.json` and `image_manifest.json` schema-validate, and every manifest `local_path` passes `local_path_is_safe`;
- `deck_plan.json` schema-validates, `planning.planned_slide_count` equals `len(slides)`, every `slides[]` entry has an integer `index` and a non-empty string `layout`, and indices are unique;
- `render_models/` exists and contains exactly the canonical filenames named by `deck_plan.slides[]` (every planned slide present, no orphan file);
- every consumed render_model schema-validates against `render_model.schema.json`;
- the render_model `canvas` matches `design_system.grid`;
- every primitive's `kind` is in the supported set (`chart_placeholder` fails closed with a clear per-slide error);
- palette / typography tokens resolve to a 6-digit hex / CSS-style font chain (defense-in-depth gate mirroring the SVG renderer);
- `image_slot.image_ref` is declared in `image_manifest`;
- for embeddable PNG / JPG / JPEG manifest entries: the resolved path stays inside the workspace, is NOT a symlink (broken or resolvable), is a regular file, is under the 10 MiB embed cap, and starts with the matching magic bytes (`\x89PNG\r\n\x1a\n` or `\xff\xd8\xff`). Any failure aborts the run before any output is written. Manifest entries with a non-PNG/JPG extension are intentionally NOT errors — they demote to the placeholder rectangle;
- a render_model whose layout is outside the SUPPORTED_LAYOUTS allow-list above fails closed with a per-slide error and aborts the whole run; no partial `.pptx` is written. Every layout declared by the business_review template skeleton is currently in scope; new layouts must add a paired generator branch before they may appear here. (Earlier wording said unsupported layouts were `[SKIP]`-ed and a partial deck was still produced — that is no longer the case.)

`--self-test` builds a synthetic workspace under `tempfile.TemporaryDirectory()` and exercises the happy path plus every fail-closed gate:

```
python3 scripts/export_pptx.py --self-test
```

A `--render-model <path>` debug entry exports a single render_model file (paired with `--design-system` and `--image-manifest`); the workspace mode is the primary entry point.

### PPTX contract + minimal-evidence validator

`scripts/validate_pptx_contract.py` is a stdlib-only, fail-closed validator that grows alongside the exporter. See `references/pptx-conversion-rules.md` for the contract.

```
# Skeleton mode — reports the contract / TODO surface only. Does not
# open or fabricate any file.
python3 scripts/validate_pptx_contract.py

# Container + minimal-evidence mode — when a .pptx is supplied,
# runs:
#   container.exists / container.extension / container.zip /
#   container.parts — basic OOXML container shape; AND
#   slide_count.inspectable / relationships.no_external /
#   relationships.no_file_uri / relationships.allow_list /
#   package.no_macros / package.no_ole / package.no_activex /
#   minimal_evidence.editable_text /
#   minimal_evidence.not_all_image_slide /
#   minimal_evidence.no_blank_slide /
#   minimal_evidence.every_slide_has_native_shape —
#   minimal-evidence safety / editability gates.
python3 scripts/validate_pptx_contract.py --pptx \
  /tmp/szh-ppt-master-smoke/synthetic_8_page_product_brief.pptx

# Self-test — exercises tempfixture negatives for every gate
# (missing file, wrong extension, non-zip content, empty ZIP, ZIP
# missing ppt/presentation.xml, external rel, file:// rel,
# unexpected relationship Type, vbaProject.bin, oleObject1.bin,
# activeX1.xml, all-image slide (fails both not_all_image_slide and
# every_slide_has_native_shape), no editable text, blank slide
# alongside an editable one) plus two positives (minimal valid
# container, minimal editable PPTX).
python3 scripts/validate_pptx_contract.py --self-test
```

The four `minimal_evidence.*` checks are explicitly named MINIMAL EVIDENCE in the per-check output: they prove *one* slide has an editable `<a:t>` run, they rule out the obvious one-big-PNG failure mode, they reject any slide whose `<p:spTree>` is structurally empty, and they require every slide to carry at least one native editable `<p:sp>` / `<p:cxnSp>` structure. They do not constitute a full per-shape editability inventory. The `relationships.allow_list` gate restricts every package `Relationship` `Type` URL to the canonical OOXML set (`officeDocument`, `slide`, `slideMaster`, `slideLayout`, `theme`, `image`). The `image` URL is reserved for per-slide PNG / JPG / JPEG embedded media; the `media.targets_internal` gate fails closed on an `image` rel that uses `TargetMode="External"`, a URI scheme, or `file://`; the `media.inventory` gate fails closed on a dangling Target, an orphan `ppt/media/` part, or a media extension outside the `{png, jpg, jpeg}` allow-list (and verifies each shipped extension is registered with the matching `<Default ContentType="image/png|image/jpeg"/>` entry); and the `media.embedded_only` gate fails closed on any `<a:blip r:link="..."/>` linked-image reference anywhere in the package's content XML (scope is every `*.xml` part under `ppt/` that is not a `_rels/` file — slides, slideMasters, slideLayouts, theme, presentation — so the gate catches the linked-image form even when the rels file is left untouched). Full-inventory editability, theme palette mapping, determinism, and validator-side layout / primitive scope all remain TODO and are reported in every run.

### PPTX readback inventory (structured JSON)

`scripts/inspect_pptx_inventory.py` is a stdlib-only, fail-closed readback inventory that complements the validator above. Where `validate_pptx_contract.py` emits pass/fail lines, this script reads the same `.pptx` ZIP and produces a **deterministic JSON document** a CI / review surface can consume:

```
# Print the inventory to stdout.
python3 scripts/inspect_pptx_inventory.py --pptx \
  /tmp/szh-ppt-master-smoke/synthetic_8_page_product_brief.pptx

# Write the inventory to a file (the --out path may not be a symlink,
# and an existing non-regular file at the path is refused).
python3 scripts/inspect_pptx_inventory.py --pptx deck.pptx \
  --out /tmp/inventory.json

# Self-test — 18 tempfixture scenarios: happy path, deterministic
# repeated inventory, image-only slide failure, chart-shaped
# full-slide <p:pic> raster regression guard (the same
# `slide.image_only` finding fires when the picture is sized to
# the whole 16:9 canvas — gate is size-blind, so a chart silently
# lowered to a full-bleed rasterization cannot pass), missing
# media, image rel with NO Target attribute (without this explicit
# check the rel would fall off every downstream media gate — the
# path-traversal block guards on target truthiness), orphan media,
# external rel, file:// rel, absolute-path rel, path-traversal
# rel, bad XML at a slide part, unsupported media extension,
# linked-blip `<a:blip r:link/>` in slide content (the same
# surface validate_pptx_contract.py's media.embedded_only gate
# refuses), unparseable XML in a non-slide ppt/ part (no
# fail-open continue), unreadable ZIP, nonexistent file, and an
# external image rel placed at the PACKAGE-level rels file
# (`ppt/_rels/presentation.xml.rels`) — defense in depth proving
# the rels walker iterates every `*.rels` member in the package,
# not only slide-level rels.
python3 scripts/inspect_pptx_inventory.py --self-test
```

The JSON document carries: top-level `ok` (True iff `findings[]` is empty), an `evidence_basis` field that commits to the literal string `"OOXML structure only; not proof of full PowerPoint editability"`, a `container` block (size_bytes, sha256, exists), `slide_count`, a `slides[]` array (each entry: `index`, `part`, `native_object_counts` for `shapes_sp` / `connectors_cxnSp` / `pictures_pic` / `graphic_frames` / `tables` / `text_bodies` / `text_runs`, `text_run_count`, `media_refs[]` with rId / Target / resolved package path / `used_by_slide_blip` flag, `image_only_evidence` boolean), a `media_parts[]` array (each entry: part name, size_bytes, sha256, extension, declared content_type, sorted `referencing_slides[]` list of slide indices), a `relationships[]` array sorted by `(rels_part, id)`, and a `findings[]` array (each entry: `severity="error"`, stable `code`, optional `part`, human-readable `detail`). The finding codes are: `relationships.external` / `relationships.file_uri` / `relationships.absolute` / `relationships.path_traversal` / `media.missing` / `media.orphan` / `media.unsupported_extension` / `slide.image_only` / `media.linked_blip` / `package.unreadable_zip` / `package.unreadable_xml`. The `media.linked_blip` code fires on any `<a:blip r:link="..."/>` element anywhere in slides / slideMasters / slideLayouts / theme / presentation — closing the same surface `validate_pptx_contract.py`'s `media.embedded_only` gate refuses, so a tampered package whose rels file points at an internal Target while a slide consumes it through `r:link` still fails closed at the readback layer. Determinism rule: `json.dumps(..., indent=2, sort_keys=True)` + sorted findings / slides / media parts / relationships, so two runs against byte-identical input produce byte-identical JSON output. Exit 0 iff `findings[]` is empty; otherwise exit 1 (the JSON is still emitted so the caller can read what failed). The script is **inspection-only**: it never modifies the package, never writes outside `--out`, never calls any network, and never overclaims full PowerPoint editability — every count is "evidence from the OOXML structure", not proof a slide opens cleanly in PowerPoint.

### Local end-to-end pipeline runner (prepared workspaces)

`scripts/run_pipeline.py` is a stdlib-only orchestrator that chains the five existing scripts above into a single command for a workspace that **already** ships `deck_brief.json`, `deck_plan.json`, `slide_plans/*.json`, `design_system.json`, and `image_manifest.json`, with an optional sixth inventory-readback stage that runs only when `--report-dir` is supplied. The runner does **not** plan a deck from a raw prompt — intake → brief → plan from source is still TODO. It runs each stage as a subprocess against the same `python3` that started it (so each stage's existing fail-closed gates and self-tests are the source of truth — no logic is duplicated):

1. `scripts/validate_workspace.py` — contract gate on the inputs;
2. `scripts/generate_render_models.py` — regenerate `render_models/*.json`;
3. `scripts/generate_svg_previews.py` — regenerate `svg_previews/*.svg`;
4. `scripts/export_pptx.py` — write the native editable `.pptx`;
5. `scripts/validate_pptx_contract.py --pptx <output> --expected-slide-count <len(deck_plan.slides)>` — container + minimal-evidence gates on the exported file, including the slide-count gate the per-stage validator exposes;
6. (only when `--report-dir` is supplied) `scripts/inspect_pptx_inventory.py --pptx <output> --out <report-dir>/inventory.json` — deterministic OOXML structure readback after the validator has passed. Evidence only: OOXML structure counts, media / relationship report, and findings — NOT proof of full PowerPoint editability (the readback carries the literal `"OOXML structure only; not proof of full PowerPoint editability"` framing string). A non-zero exit from the inventory subprocess (invocation error OR findings present) fails the pipeline closed.

A non-zero exit code from any stage stops the pipeline; downstream stages are skipped with a `[SKIP] earlier stage failed` note rather than silently passing. Workspace mutability: the runner **regenerates** `<workspace>/render_models/*.json` and `<workspace>/svg_previews/*.svg` in place (each generator owns its subdirectory and sweeps stale files before regenerating). The prepared-input artifacts (`deck_brief.json`, `deck_plan.json`, `slide_plans/`, `design_system.json`, `image_manifest.json`) and any other files under the workspace are read-only. The final `--output` PPTX **must live OUTSIDE** the workspace, and **so must `--report-dir`** when supplied — a stray `.pptx` or report directory under the workspace tree would otherwise be picked up by `validate_workspace` on a later run.

The runner fails closed before invoking any stage if `--workspace` or `--template-root` is not a directory, if `--output` does not end in `.pptx`, if `--output` would land inside the workspace tree, if `--report-dir` (when supplied) would land inside the workspace tree, if `--report-dir` (when supplied) already exists as a symlink (refused — not silently followed, same rule as `--output`), if `--report-dir` (when supplied) already exists and is not a directory (e.g. a regular file at the report path; refused so the pipeline never runs validate / generate / export only to crash inside `_write_reports()` afterwards with a `FileExistsError`), if `--output` already exists as a symlink (refused — not silently followed), if `--output` already exists and is not a regular file (e.g. a directory named `Documents.pptx/`; refused so the runner never recursively touches arbitrary content), or if `--output`'s parent directory cannot be created (e.g., a regular file blocks the parent path; the runner catches the resulting `OSError` and returns rc=2 with a clean FAIL diagnostic instead of a Python traceback). When `--report-dir` is supplied, the runner also pre-creates it (`mkdir(parents=True, exist_ok=True)`) and **then probes writability** by opening + closing + deleting a `NamedTemporaryFile` inside it — `mkdir(exist_ok=True)` alone is not sufficient because it returns success on an existing read-only directory (one the runner does not own, a read-only mount, an ACL block, a full filesystem), leaving `_write_reports()` to crash with `PermissionError` only AFTER the PPTX has already been written. The probe closes that hole: any unrelated permission / parent-not-a-directory / read-only-filesystem / full-disk error surfaces BEFORE any stage runs. The directory probe is not enough on its own either — `_write_reports()` writes two specific filenames (`pipeline_report.json` / `pipeline_report.txt`) via `Path.write_text()`, which follows symlinks and fails on directories or read-only regular files — so the runner ALSO refuses, before any stage runs, a pre-existing symlink at either report-file path (would silently redirect the write to an unrelated target — same anti-pattern `--output` forbids), a non-regular-file at either path (a directory would raise `IsADirectoryError`), and an existing regular file at either path that lacks user write permission (`os.access(..., os.W_OK)` is False; `write_text` would raise `PermissionError`). A pre-existing regular `.pptx` at `--output` is **not** deleted up-front — that prior artifact is overwritten in place by the export stage only when (and only when) the pipeline reaches that stage. An earlier-stage failure therefore leaves the caller's prior good `.pptx` untouched on disk, and the runner's report records the failure surface explicitly. A partial `.pptx` left by a crash inside the export stage itself is not cleaned up by this runner; full atomic-write semantics for the export stage remain a TODO on `scripts/export_pptx.py`, not on this runner.

`scripts/run_pipeline.py --self-test` builds tempfile-based synthetic workspaces (copied from `examples/synthetic_8_page_product_brief/`) and asserts twenty-one scenarios: happy-path export+validation, wrong `--output` extension, `--output` inside the workspace, `--report-dir` inside the workspace, pre-existing regular file at `--report-dir` (refused before any stage runs; no PPTX is written and the prior file is preserved byte-identical), pre-existing symlink at `--report-dir` (refused; the symlink's target directory is untouched and no `pipeline_report.{json,txt}` is written into it), pre-existing read-only directory at `--report-dir` (writability probe; refused before any stage runs; auto-skipped under root because `chmod` cannot meaningfully restrict root), pre-existing symlink at `<report-dir>/pipeline_report.json` (refused; no PPTX is written and the symlink's unrelated target is preserved byte-identical), pre-existing read-only `<report-dir>/pipeline_report.txt` (refused; no PPTX is written and the prior file content is preserved; auto-skipped under root), happy-path `--report-dir` outside the workspace writes `pipeline_report.json` + `pipeline_report.txt` with `overall_ok=true`, happy-path `--report-dir` writes `inventory.json` with `ok=true` and the `evidence_basis` framing line (the `inspect_pptx_inventory` stage reports `[PASS]` and the runner prints the inventory path with the evidence-only caveat), happy path WITHOUT `--report-dir` produces no `inventory.json` anywhere under a dedicated scenario root and the `inspect_pptx_inventory` stage is not added to the stage list, pre-existing symlink at `<report-dir>/inventory.json` refused before any stage runs (the symlink's unrelated target is preserved byte-identical), `validate_workspace` failure with `--report-dir` cascading `[SKIP]` to `inspect_pptx_inventory` too (no `inventory.json` is created), stale `inventory.json` from a prior successful `--report-dir` run is removed BEFORE a later failing rerun's stages execute (so the caller never sees a fresh FAIL `pipeline_report` next to a stale `inventory.json` from a different run), a LATE preflight failure (`--output` is a symlink) with the same `--report-dir` preserves the prior `inventory.json` byte-identical (the destructive stale-inventory unlink only runs AFTER every preflight gate, so a late preflight refusal does not strand the caller without their prior evidence), `--output` parent path blocked by a regular file returns rc=2 with a clean FAIL diagnostic naming the parent path and the `OSError` detail (no Python traceback) and the blocker file and any prior `inventory.json` are both preserved byte-identical, pre-existing symlink at `--output`, pre-existing directory at `--output`, `validate_workspace` failure cascading `[SKIP]` across every downstream stage, and a pre-existing regular `.pptx` preserved (byte-identical) when `validate_workspace` fails before export. Exits non-zero if any scenario does not behave as expected.

```
python3 scripts/run_pipeline.py \
  --workspace examples/synthetic_8_page_product_brief \
  --template-root templates/layouts \
  --output /tmp/szh-ppt-master-smoke/8_page.pptx \
  --report-dir /tmp/szh-ppt-master-smoke/reports_8

python3 scripts/run_pipeline.py \
  --workspace examples/synthetic_20_page_business_review \
  --template-root templates/layouts \
  --output /tmp/szh-ppt-master-smoke/20_page.pptx \
  --report-dir /tmp/szh-ppt-master-smoke/reports_20
```

When `--report-dir` is supplied, the runner writes:

- `pipeline_report.json` — machine-readable: workspace / template_root / output / expected_slide_count / overall_ok / per-stage `{name, command, ok, skipped, exit_code, duration_s, stdout, stderr}` (stdout / stderr are truncated to 4 KB with an elision marker so the JSON stays bounded);
- `pipeline_report.txt` — short human-readable summary: workspace inputs, overall status, and per-stage PASS / FAIL / SKIP lines with the failing stage's stderr / stdout tail;
- `inventory.json` — the deterministic OOXML structure readback produced by `scripts/inspect_pptx_inventory.py` against the just-exported `.pptx`. Same JSON contract as the standalone script (sorted keys / findings / slides / media_parts / relationships, `evidence_basis = "OOXML structure only; not proof of full PowerPoint editability"`). Written only after `validate_pptx_contract` passes; if the inventory subprocess itself fails (invocation error OR any finding), the pipeline fails closed and the JSON the inventory step did emit is left on disk so the caller can read what failed. The pre-existing-path gates that apply to `pipeline_report.{json,txt}` (no symlink / non-regular file / read-only regular file at the path) also apply to `inventory.json`. **Stale-inventory hygiene**: before any stage runs, any pre-existing regular `<report-dir>/inventory.json` is unlinked. The two `pipeline_report.{json,txt}` files are always re-written at end of run by `_write_reports()` (so they cannot go stale), but `inventory.json` is only written when the `inspect_pptx_inventory` stage actually runs — so a prior successful run's inventory would otherwise survive a later failing rerun that aborts before the inventory stage, leaving the caller with a fresh `FAIL` pipeline_report next to a stale inventory from a previous run. The pre-stage unlink removes that mismatch: after every `--report-dir` invocation, `inventory.json` is either freshly written by the current run (when the inventory stage ran) or absent (when an upstream stage failed). The unlink is intentionally placed AFTER every preflight gate, never before — so a late preflight failure (`--output` is a symlink / a directory, or the output-parent `mkdir` fails) returns rc=2 without invoking `run_pipeline()` AND without destroying the prior good `inventory.json`. When `--report-dir` is omitted, the `inspect_pptx_inventory` stage is not added to the stage list at all — no inventory file is produced anywhere.

This is **not** the end-to-end "prompt → editable PPTX" experience. The runner only composes the existing prepared-workspace stages; everything outside that scope (raw-source ingestion, brief / plan generation from a prompt, `chart_placeholder` rendering, SVG / GIF / WebP media embedding, full PPTX inventory, D-One image assets, Qoder CLI) remains TODO. Callers who already have explicit source + spec inputs can chain Stage 1-6 (`scripts/prepare_workspace.py`) with this runner in one command via `scripts/run_explicit_pipeline.py` (see next section).

### Local end-to-end pipeline runner (explicit-input source/spec → PPTX)

`scripts/run_explicit_pipeline.py` is a stdlib-only orchestrator that chains `scripts/prepare_workspace.py` (Stage 1-6 explicit-input orchestration; invoked in-process so the structured per-stage result is preserved for reporting) and `scripts/run_pipeline.py` (Stage 7-10 prepared-workspace pipeline; invoked as a subprocess so its existing fail-closed gates, `[PASS]/[SKIP]/[FAIL]` cascade, and optional `--report-dir` writes are reused verbatim — no stage logic is duplicated) into one local, deterministic command that takes explicit caller-supplied source + spec files in and produces a validated editable `.pptx` out. **This is explicit-input end-to-end orchestration only — it is NOT a full prompt/report/Markdown-to-PPTX automation.** Every Stage-1-to-6 artifact's content comes from the caller (CLI flags or JSON spec files); Stage 7-10 consumes only what Stage 1-6 wrote. The orchestrator never parses `input/source.md` for business content, never invents `deck_brief` / `deck_plan` / `design_system` / `slide_plans` / `image_manifest` content, never generates any image / spec / slide body, never calls D-One / Qoder / any public network / image generation / telemetry / external service, never duplicates per-stage logic, and never modifies files outside `--workspace` apart from the final `--output` PPTX and (if supplied) `--report-dir`.

Fail-closed semantics: if Stage-1-to-6 preparation fails at any stage (non-zero exit), the Stage-7-to-10 pipeline is NOT invoked at all — no `render_models/`, `svg_previews/`, `.pptx`, or `pipeline_report.{json,txt}` is produced; if Stage-7-to-10 fails, the failing stage is reported clearly via the pipeline subprocess's own `[FAIL] <stage>` / `[SKIP] <stage>` cascade plus stderr tail. The orchestrator owns three up-front gates only — required-args check, the Stage-4 mode-selection gate (`--design-system-spec` XOR `--theme-from-template`), and the `--output` extension check (`.pptx`) — to fail fast on obvious user errors before a six-stage prep run is wasted; every other `--output` / `--report-dir` safety gate (inside-workspace, symlink, non-regular-file, pre-existing report-file paths) is delegated to `scripts/run_pipeline.py` for defense in depth, so a caller who passes `--output <workspace>/foo.pptx`, a symlink, or a directory at the output path sees exactly the same diagnostic `scripts/run_pipeline.py` would have produced on its own.

`scripts/run_explicit_pipeline.py --self-test` exercises tempfixture scenarios under `tempfile.TemporaryDirectory()`: happy path (explicit source/spec inputs produce a validated PPTX with every Stage-1-to-6 stage PASS and every Stage-7-to-10 stage PASS), preparation failure short-circuiting the pipeline (empty source → `init_workspace` fails → no `render_models/` / `svg_previews/` / PPTX produced, every downstream prep stage SKIP, pipeline never invoked), pipeline failure reported clearly (a pre-existing directory at `--output` is caught by `run_pipeline`'s gate after prep succeeds; the orchestrator surfaces the diagnostic and the directory's canary file is untouched), wrong `--output` extension failing fast in the orchestrator before any prep stage runs (no workspace is created), `--output` inside the workspace refused by `run_pipeline`'s delegated gate (no `.pptx` written inside the workspace tree), the source-body marker phrase never copied into any artifact beyond `<workspace>/input/source.md` (source_manifest / deck_brief / deck_plan / design_system / slide_plans / image_manifest / render_models / svg_previews / the PPTX bytes are all checked), deterministic repeated runs from byte-identical inputs both passing validation with byte-identical Stage-1-through-6 artifacts across the two runs, and a `--report-dir` happy-path scenario that asserts `<report-dir>/inventory.json` is written with `ok=true` and the `evidence_basis` framing line (the orchestrator forwards `--report-dir` so the `inspect_pptx_inventory` stage runs as the last stage in the chain) and the source-body marker phrase never appears in the inventory.

```
python3 scripts/run_explicit_pipeline.py \
  --workspace /tmp/szh-explicit-ws \
  --source /path/to/source.md \
  --title "Quarterly Review" \
  --audience "Executive team" \
  --objective "Summarize Q3 results and decisions" \
  --plan-spec /path/to/plan_spec.json \
  --theme-from-template \
  --template-root templates/layouts \
  --slide-specs-dir /path/to/slide_specs/ \
  --image-manifest-spec /path/to/image_manifest_spec.json \
  --output /tmp/szh-explicit-out/quarterly.pptx \
  --report-dir /tmp/szh-explicit-out/reports

python3 scripts/run_explicit_pipeline.py --self-test
```

### Mock D-One image-to-editable-PPT runner (NOT real D-One integration)

`scripts/run_mock_image_pipeline.py` is a stdlib-only, deterministic wrapper that packages the local image chain (`done_image_adapter.py` → `run_d_one_generation.py --allow-synthetic-bytes` → `materialize_image_assets.py` → `run_explicit_pipeline.py --assets-dir <staging>`) behind one explicit-input command. It accepts the same caller-authored inputs as `run_explicit_pipeline.py` (`--workspace` / `--source` / `--title` / `--audience` / `--objective` / `--plan-spec` / `--design-system-spec` OR `--theme-from-template` / `--template-root` / `--slide-specs-dir` / `--image-manifest-spec` / `--output`, plus the optional `--source-id` / `--tone` / `--language` / `--approximate-slide-count` / `--report-dir`) AND adds two image-chain inputs: `--d-one-spec <path>` (the caller-authored D-One request spec forwarded to `done_image_adapter --spec`) and `--descriptor-vocabulary <path>` (optional; required iff `--d-one-spec` carries any of the seven taxonomy fields `rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain`). The runner opens one `tempfile.TemporaryDirectory()` for staging, seeds the staging `image_manifest.json` from `--image-manifest-spec` (the direct-author workflow documented at `scripts/materialize_image_assets.py`), drives the d-one chain on the staging workspace, then invokes `scripts/run_explicit_pipeline.py --assets-dir <staging>` so the orchestrator's materialize_image_assets step copies the synthetic bytes into the production workspace between Stage 5 and Stage 6. **MOCK / STUB only — NOT real D-One integration**: nothing calls D-One, MCP, Qoder, a public network, telemetry, any model API, an image search, or any external service. `--allow-synthetic-bytes` is REQUIRED on the runner — omitting it fails closed at the runner boundary rather than implying a real-D-One / provider mode this repo does not support today. For taxonomy-bearing specs the runner re-parses the produced `<staging>/d_one_adapter_plan.json` BEFORE invoking the production pipeline and refuses to proceed unless `schema_version == 2` AND every taxonomy field/value supplied on the spec request appears byte-identical on the matching plan request — a downgrade or value drift aborts the run before any PPTX is written. The only files the runner writes outside the staging tempdir are the caller's `--workspace` (created by `init_workspace.py` inside `run_explicit_pipeline.py`), the `.pptx` at `--output`, and (when supplied) `pipeline_report.{json,txt}` + `inventory.json` under `--report-dir`; no committed example or repo directory is touched.

```
python3 scripts/run_mock_image_pipeline.py \
  --workspace /tmp/szh-mock-image-ws \
  --source /path/to/source.md \
  --title "Synthetic Mock" \
  --audience "Internal reviewers" \
  --objective "Exercise the local mock D-One chain end-to-end" \
  --plan-spec /path/to/plan_spec.json \
  --design-system-spec /path/to/design_system_spec.json \
  --template-root templates/layouts \
  --slide-specs-dir /path/to/slide_specs/ \
  --image-manifest-spec /path/to/image_manifest_spec.json \
  --d-one-spec /path/to/d_one_spec.json \
  --descriptor-vocabulary /path/to/descriptor_vocabulary.json \
  --allow-synthetic-bytes \
  --output /tmp/szh-mock-image-out/deck.pptx \
  --report-dir /tmp/szh-mock-image-out/reports

python3 scripts/run_mock_image_pipeline.py --self-test
```

`scripts/run_mock_image_pipeline.py --self-test` exercises 16 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()`: the happy path (a 2-slide bundle with one `d_one_local` image carrying all 7 taxonomy dimensions produces a validated `.pptx` whose `ppt/media/` carries a native PNG/JPG/JPEG with no external/`file://`/`data:`/scheme-shaped relationships AND the `[PASS] taxonomy preservation check` marker fires); negative probes for missing `--allow-synthetic-bytes`, taxonomy-bearing `--d-one-spec` without `--descriptor-vocabulary`, regex-shape-valid-but-out-of-vocabulary `text_policy`, regex-shape-valid-but-out-of-vocabulary `subject_domain`, **editable-text wording** (`slide title`) embedded in the prompt refused by `done_image_adapter` before any production workspace or PPTX is created, **in-image-text wording** (`include text`) under `text_policy='no_text'` refused before any production workspace or PPTX is created, **boundary-safe phrases** (`image texture` / `textured paper` / `context lighting` / `texture pattern`) under `text_policy='no_text'` still pass the chain end-to-end and produce a PPTX whose `ppt/media/` carries a native PNG/JPG/JPEG with no external relationships, the `schema_version == 2` invariant on the produced plan (indirect — the runner always invokes `done_image_adapter`, which writes `schema_version=2`; the probe confirms the assertion line lands in stdout), `--d-one-spec` request id not declared in the image-manifest spec, image-manifest `local_path` carrying a URI scheme (`http://...`), symlinked `--output` / `--workspace` / `--report-dir` (each refused at the runner boundary before any subprocess fires); a `no-repo-write` snapshot probe that strips `PYTHONDONTWRITEBYTECODE` from the subprocess env and asserts a flat-bytes snapshot of `scripts/__pycache__/` is byte-identical before and after the run — proves the in-script `sys.dont_write_bytecode = True` flip (set BEFORE the first-party `validate_scaffold` import) closes the `.pyc`-leak hole even when the caller forgets the env-var prefix; and a downstream-failure cleanup probe asserting no `.pptx` lands at `--output` when the run aborts.

Raw-source-to-PPTX automation — extracting `deck_brief` / `deck_plan` / `design_system` / `slide_plans` / `image_manifest` content from `input/source.md` directly, without explicit caller-supplied spec files — remains TODO. The implemented explicit-input runtime pipeline (the six per-stage helpers `init_workspace.py` / `init_deck_brief.py` / `init_deck_plan.py` / `init_design_system.py` / `init_slide_plans.py` / `init_image_manifest.py`, the Stage-1-to-6 orchestrator `scripts/prepare_workspace.py`, the Stage-7-to-10 runner `scripts/run_pipeline.py`, and the Stage-1-to-10 end-to-end orchestrator `scripts/run_explicit_pipeline.py`) does **not** implement it: every script in that pipeline is intentionally explicit-input. The work of reading `input/source.md` plus the user's request and **authoring** the five spec inputs the runtime pipeline consumes is the agent's responsibility; the authoring contract (per-stage fields, source-fidelity rules, image-safety rules, and the pre-pipeline quality loop) lives in [`references/authoring-workflow.md`](references/authoring-workflow.md), kept as a separate document so the authoring workflow is never read as runtime code. A passing `scripts/run_explicit_pipeline.py` (or per-stage chain) run is evidence that the authored specs round-trip to a validated editable PPTX, **not** evidence of automatic prompt-to-PPTX behavior, of D-One image generation, of Qoder runtime packaging, or of any model API call — none of those are implemented anywhere in this repo.

### Pre-pipeline authoring quality gate

`scripts/validate_authoring_bundle.py` is a stdlib-only, **non-mutating** structural gate that takes the same explicit caller-supplied inputs `scripts/run_explicit_pipeline.py` accepts (`--source` plus `--title` / `--audience` / `--objective` plus `--plan-spec` plus `--design-system-spec` OR `--theme-from-template` plus `--template-root` plus `--slide-specs-dir` plus `--image-manifest-spec`) — OR, equivalently, a single `--bundle <dir>` shortcut that resolves the same inputs from a canonical directory layout (`<dir>/source.md` or `source.txt`, `<dir>/brief.json` carrying `title` / `audience` / `objective` plus optional `tone` / `language` / `approximate_slide_count` / `source_id` / `brand_preset_ref` (an OPTIONAL `{id, path}` object that triggers a READ-ONLY authoring-time check against `scripts/validate_brand_preset.py` — NEVER applied to `design_system` / `slide_plans` / `render_models` / SVG / PPTX), `<dir>/plan_spec.json`, `<dir>/design_system_spec.json` (omit only with `--theme-from-template`), `<dir>/slide_specs/`, `<dir>/image_manifest_spec.json`; `--template-root` still required, every entry must be a non-symlink, and mixing `--bundle` with the explicit per-input flags or with an unknown / missing `brief.json` field fails closed with exit 2) — and validates them **before any workspace is created**. This is the **narrow bridge contract** between authoring time and the Stage-1 workspace — it validates an authoring bundle the caller has already written (brief metadata + deck_plan + design_system input + slide_plans + image_manifest, with `slide.source_refs` linking back to the single-element `[source_manifest.source.id]` the Stage-1 helper will write) and is intentionally NOT a full prompt/report/Markdown-to-PPTX automation. The intent is to let an agent-authored bundle fail at authoring time rather than after a six-stage prep run. The gate does **not** create a workspace, write `deck_brief.json` / `deck_plan.json` / `design_system.json` / `slide_plans/*.json` / `image_manifest.json`, generate any image / render_model / SVG / PPTX, parse `input/source.md` for business content (it reads source bytes only for byte-level integrity and a narrow raw-source-leakage scan against `image_manifest.alt_text` / `intended_use`), or call D-One / Qoder / any public network / image generation / telemetry / external service / model API. It is stdlib-only and offline.

Checks (each emits one or more findings; `ERROR` exits non-zero, `WARN` is informational unless `--strict` is passed):

- mode-selection: exactly one of `--design-system-spec` / `--theme-from-template`;
- source: regular `.md` / `.txt` file, not a symlink, non-empty, valid UTF-8 (mirrors `init_workspace`);
- derived `source_id`: `--source-id` (or the source filename stem) matches `^[A-Za-z0-9][A-Za-z0-9_.\-]*$` and passes `local_path_is_safe` (mirrors `init_workspace`);
- brief metadata: `--title` / `--audience` / `--objective` are strings, non-empty **after `.strip()`** (so a whitespace-only value like `"   "` / `"\t"` is refused — the schema's `minLength: 1` alone would silently accept it), and free of embedded newlines / carriage returns (mirrors `init_deck_brief._validate_single_line_string`); when supplied, `--tone` / `--language` go through the same rule;
- plan: schema-validates against `schemas/deck_plan.schema.json`; the planner-semantics cross-checks `init_deck_plan.py` applies (`planned_slide_count == len(slides)`, contiguous 1..N indices, section coverage 1:1, `slide.section_id` resolution, `slide.source_refs` subset of the single-element derived `deck_brief.source_refs = [source_id]`);
- template chain: `deck_plan.template` resolves under `--template-root` to a schema-valid `template.json`; every `slide.layout` is declared by the template, schema-validates as a `layout.json`, and lives in the render-model generator's `SUPPORTED_LAYOUTS` allow-list (imported lazily from `scripts/generate_render_models.py` so the gate and the generator stay in sync);
- slide specs: every `*.json` in `--slide-specs-dir` is a regular non-symlink JSON object that schema-validates against `schemas/slide_plan.schema.json`; 1:1 coverage with `deck_plan.slides[]` (no duplicates, no missing, no orphan); `layout` / `title` agree with the deck_plan slide; every required layout slot is covered (via the shared `_slide_plan_against_layout` helper); a non-canonical filename (`<index:02d>_<layout>.json`) is a `WARN`, not an `ERROR`;
- image manifest spec: schema-validates against `schemas/image_manifest.schema.json`; `images[].id` is unique; every `local_path` passes `local_path_is_safe`; every `slide_plan.image_refs[*]` value across the slide specs is declared in `images[].id`; an empty `images[]` is accepted only when no slide_plan references an image;
- full-slide raster intent: `image_manifest.intended_use` / `alt_text` must not match a curated phrase list (`"full-slide background"`, `"screenshot of slide"`, `"image as background"`, ...; mirrors `references/d-one-image-policy.md`);
- forbidden-token scan: every spec JSON body is scanned for URL schemes (`http://`, `https://`, `file://`, `s3://`, `ftp://`, `data:`, `mailto:`), model/API/runtime tokens (`d-one` / `d_one`, `qoder`, `openai`, `anthropic`, `api_key`, `telemetry`), and overclaim-of-automation tokens (`ai-generated`, `model-generated`, `prompt-to-pptx`, `prompt-to-ppt`, `prompt-to-deck`, `automatic prompt-to`) that contradict the bridge-contract framing — this gate validates an already-authored bundle and is NOT a full prompt/report/Markdown-to-PPTX automation; a match emits `ERROR`;
- raw-source leakage: long phrases (≥ 32 collapsed-whitespace characters, skipping `#`-prefixed headings) from `--source` are looked for inside `image_manifest.alt_text` / `intended_use`; a match emits `ERROR` (catches obvious copy/paste from the source body into a prompt-like field, not every possible leakage).

`scripts/validate_authoring_bundle.py --self-test` exercises 109 tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: happy path (clean bundle → 0 errors / 0 warnings), planned-count mismatch, non-contiguous slide indices, layout not declared by the template, `slide.source_refs` not in derived `deck_brief.source_refs`, missing slide spec, orphan slide spec, layout/title disagreement, missing required layout slot, `chart_ref` block (ERROR — no SUPPORTED layout maps to the `chart_placeholder` primitive today), undeclared `image_ref`, unsafe `local_path`, duplicate manifest id, full-slide raster intent, D-One mention, Qoder claim, `https://` URL in slide content, raw-source leakage into `alt_text`, both / neither design-system mode, missing / empty source, empty title, **whitespace-only `--title` / `--audience` / `--objective` / `--tone` / `--language` (each flagged after `.strip()` so a value like `"   "` / `"\t"` / `" \t  "` cannot false-pass the schema's `minLength: 1` — mirrors `init_deck_brief._validate_single_line_string`)**, non-canonical spec filename surfaced as `WARN` (no `ERROR`), a **non-mutating** assertion that a happy-path run leaves every spec file byte-identical and creates no workspace artifact, **4 overclaim-of-automation scenarios** (an `AI-generated` claim in `slide_plan.notes`, a `prompt-to-pptx` claim in `image_manifest.alt_text`, a `model-generated` claim in `plan_spec.planning.rationale`, and an `automatic prompt-to-*` claim in `slide_plan.notes` — each refused at authoring time because the repo is a narrow bridge contract, not a prompt/report/Markdown-to-PPTX automation), **14 fail-closed shape-preflight scenarios** that prove a malformed `--plan-spec` produces clear `ERROR` findings without tracebacking: 9 cover top-level shape (malformed JSON body; JSON-list root; `plan["slides"]` is a string; `plan["slides"]` contains a non-object entry; `plan["sections"]` is a string; `plan["sections"]` contains a non-object entry; `plan["planning"]` is a string; `section.slide_indices` is a string; `slide.source_refs` is a string), and 5 cover nested-value shape that would otherwise crash `set()` / `in` / `sorted()` (`slide.index` is an unhashable JSON array; `slide.section_id` is an unhashable JSON array; `section.slide_indices` contains an unhashable item; `slide.source_refs` contains an unhashable item; `slide.index` mixes unorderable types like `str` alongside `int`), **21 generator-grounded per-block scenarios** that mirror the per-kind block-shape guards in `scripts/generate_render_models.py` (NOT full generator parity — `_kpi_tile_bounds`, palette / typography token resolution, and generator-hardcoded slot-presence checks remain runtime-only): text / callout block with empty content (2), text block with numeric content (1), list block with an empty content list (1), list block with an empty-string entry (1), kpi entry missing required `value` (1), kpi entry with an empty `delta` string (1), kpi entry with an extra `chart_payload` key flagged as payload smuggling (1), comparison_table row count mismatch with `headers` (1), table cell with empty-string content (1), table content with an extra `chart_config` key flagged as payload smuggling (1), duplicate block id within a spec (1), `image_ref` block smuggled onto a non-cover layout flagged as ERROR (1), `table` block smuggled onto a non-comparison_table layout flagged as ERROR (1), kind drift on the optional `cover.subtitle` slot flagged as ERROR (1), anonymous block (no `id`) flagged as ERROR (1 — the render-model generator silently drops anonymous blocks, so the authoring bundle is structurally incompatible with the generator), unknown text slot id flagged as ERROR (1 — the generator silently drops blocks whose id is not declared by the resolved layout, leaving the agent's content invisible), `image_ref` block with empty content (1), `comparison_table` missing the required `table` block (regression coverage for the required-slot gate, 1), and **list-slot capacity overflow** scenarios that mirror the generator's `_list_item_bounds` `LIST_ITEM_MIN_H` gate (`executive_summary.key_points` with 21 items against the declared slot bounds, and `agenda.agenda_items` with 26 items against the generator's `LAYOUT_FALLBACK_BOUNDS` table for bound-less slots; 2), a **regression scenario** that runs the gate directly against the committed `examples/synthetic_authoring_trial/` authoring bundle and asserts it still validates cleanly (no ERROR findings) under the promoted-to-ERROR anonymous-block / unknown-slot-id semantics, and **24 `--bundle` dispatch scenarios** that exercise the bundle-shortcut end-to-end through `main()`: happy path on the committed trial directory, `--bundle` pointing at a missing / non-directory / URI-shaped / symlinked path (each refused with exit 2 before any read), bundle missing `source.md` or carrying both `source.md` and `source.txt` (refused), bundle missing `brief.json` / `plan_spec.json` / `slide_specs/` / `image_manifest_spec.json` (each refused with the per-entry diagnostic), `brief.json` malformed JSON / missing the required `objective` field / carrying an unknown field (typo defense), bundle `source.md` that is itself a symlink (refused), bundle missing `design_system_spec.json` without `--theme-from-template` (refused), bundle shipping `design_system_spec.json` WITH `--theme-from-template` (mode conflict refused), `--bundle` alongside an explicit per-input flag like `--title` (mutually-exclusive refused), `--bundle` + `--theme-from-template` with no `design_system_spec.json` in the bundle (valid theme-mode composition — validates cleanly), `brief.source_id` propagating into the derived `deck_brief.source_refs` (regression: when `brief.json` declares a `source_id` that does not match `source.md` stem, the gate must NOT silently fall back to the stem), **three `brief.json` field-type fail-closed scenarios** that close a hole where the bundle path used to accept untyped JSON values and crash downstream with a `TypeError` (the explicit-flag path is already argparse-typed — `--source-id type=str`, `--approximate-slide-count type=int` — so the gate must mirror that on the bundle shortcut): a non-string `source_id` like `7` is refused with the single-line diagnostic `--bundle entry brief.json field source_id must be a string if present; got int at <path>` (exit 2, no traceback), a non-integer `approximate_slide_count` like `"seven"` is refused with the equivalent `... must be an integer if present; got str ...` diagnostic, and `approximate_slide_count: true` is refused with `... got bool ...` (the `bool` branch is explicit because `isinstance(True, int)` is True in Python — a literal JSON `true` is not a slide count); and `--self-test` rejecting `--bundle` alongside it; and **15 `brand_preset_ref` scenarios (BR1–BR15)** that exercise the optional READ-ONLY authoring-time reference check end-to-end via `--bundle`: positive baseline against the committed `examples/brand_preset_template.json` (BR1), every id-side denylist (real-brand wording / missing `synthetic_` prefix / credential-shape token / `public` + propagation-verb; BR2–BR5), every path-safety gate (URI scheme / absolute path / parent traversal; BR6–BR8), unknown first-segment registry root refused at the registry-lookup step (BR9 — proves the registry list is consulted exhaustively, not first-root-wins), referenced preset file failing `validate_brand_preset` P4 (real-brand) surfaces as a bundle ERROR finding (BR10 — fixture lives entirely under a `tempfile.TemporaryDirectory()` and is reachable via a temporary extension of `_BRAND_PRESET_REGISTRY_ROOTS` that is reverted in `finally`, so the self-test NEVER writes to the live `examples/` or `templates/` trees), `ref.id != file.preset_id` registry-lookup drift (BR11), optional field omitted is a no-op (BR12), unknown sub-field refused (BR13 — typo defense AND a reminder that the ref is read-only and never applied), scalar value refused (BR14), and path missing the `/` separator refused at the `<registry-root>/<file>` form check (BR15).

```
python3 scripts/validate_authoring_bundle.py \
  --source /path/to/source.md \
  --title "Quarterly Review" \
  --audience "Executive team" \
  --objective "Summarize Q3 results and decisions" \
  --plan-spec /path/to/plan_spec.json \
  --theme-from-template \
  --template-root templates/layouts \
  --slide-specs-dir /path/to/slide_specs/ \
  --image-manifest-spec /path/to/image_manifest_spec.json \
  [--strict]                    # promote every WARN to ERROR

python3 scripts/validate_authoring_bundle.py --self-test
```

The gate is intentionally a pre-flight: every check it makes is also re-enforced by the runtime pipeline (`init_*.py` helpers, `validate_workspace`, `validate_pptx_contract`) — **with one exception**: the optional `brand_preset_ref` check has **NO downstream re-enforcer** because no script in this repo applies a preset to `design_system` / `slide_plans` / `render_models` / SVG / PPTX (the ref is authoring-time-only and read-only). A bundle this gate accepts can still fail downstream on rules the gate cannot evaluate without filesystem state (e.g. image-asset existence in the workspace). It does **not** stand in for the agent-managed checks listed in `references/authoring-workflow.md` (source fidelity, sensitive-data handling, clean-room discipline).

### Post-generation visual quality review

`scripts/validate_visual_quality.py` is a stdlib-only, **non-mutating**, **inspection-only** visual quality review gate over a workspace's `render_models/*.json` + `svg_previews/*.svg` pair. The intent is to let an agent (or the user) eyeball a generated deck structurally — a per-slide health report plus an optional local contact sheet — without re-running the six-stage prep or the four-stage render/export pipeline. The gate never plans a deck, never reads `input/source.md`, never rebuilds render_models / SVG / PPTX, never calls D-One / Qoder / any public network / image generation / telemetry / external service / model API. Repeated runs leave every workspace file byte-identical.

Per-slide checks emit one or more findings (`ERROR` exits non-zero; `WARN` is informational unless `--strict` is passed):

- `svg_missing` (ERROR) — render_model exists but `svg_previews/<stem>.svg` does not;
- `svg_unreadable` / `svg_unparseable` / `render_model_unreadable` / `render_model_unparseable` (ERROR) — load/parse errors on either file;
- `blank_slide` (ERROR) — render_model has zero primitives;
- `near_empty_slide` (WARN) — primitive count below 2 AND total text characters below 32;
- `image_only_slide` (WARN) — every primitive is an `image_slot`;
- `all_placeholder_slide` (WARN) — every text primitive matches one of the placeholder needles (`placeholder`, `tbd`, `todo`, `lorem ipsum`, `<…-placeholder>`, ...);
- `no_native_editable_primitive` (ERROR) — render_model has zero primitives in the editable-intent set (`text` / `shape` / `line` / `kpi` / `table`); mirrors `pptx.minimal_evidence.every_slide_has_native_shape` at the render_model layer;
- `out_of_canvas_render_model` (ERROR) — at least one primitive bounds box exceeds the canvas;
- `out_of_canvas_svg` (ERROR) — at least one explicit-numeric SVG element (`rect` / `line` / `image` / `ellipse` / `circle` / `text`) has geometry escaping the canvas;
- `text_density_low` (WARN) — total text chars below 16;
- `text_density_high` (WARN) — total text chars above 1200.

Aggregate counters: slide count vs `deck_plan.slides[]` (when `deck_plan.json` exists), layout distribution (Counter), missing-preview count, per-slide primitive count, per-slide primitive-kind histogram, per-slide text density. A `deck_plan` vs render_model count mismatch surfaces as a workspace-level `deck_plan_render_count_mismatch` ERROR. A workspace symlink, a `render_models/` symlink, an `svg_previews/` symlink, a missing `render_models/`, or an empty `render_models/` all surface as workspace-level ERROR findings.

`--output` writes either a JSON report (`.json`) or a self-contained HTML contact sheet (`.html`) chosen by the file extension; any other extension is refused. The HTML page embeds each SVG **inline with sanitize-by-rejection**: every SVG is parsed and refused for inline embedding when it carries any of (a) `<script>` / `<foreignObject>` / `<use>` / `<a>` / `<style>` elements, or any SMIL/timing/filter-image element (`<animate>` / `<animateMotion>` / `<animateTransform>` / `<set>` / `<discard>` / `<mpath>` / `<feImage>`) — these can either fetch external resources directly (`<feImage>`) or mutate another element's `href` attribute at runtime (`<set attributeName="href" to="https://attacker/x"/>`) after parse-time inspection is over; (b) any `on*` event-handler attribute; (c) any `href` / `src` / `xlink:href` / `*href` attribute whose value EITHER has surrounding whitespace (`" https://attacker/x"` — browsers strip leading/trailing whitespace per the URL-parser spec, which would otherwise let the URI-scheme regex bypass be smuggled past the path-safety check by a leading space/tab/newline) OR does NOT pass `validate_scaffold.local_path_is_safe` — the same path-safety rule the rest of the repo enforces, which refuses URI schemes (`http:` / `https:` / `file:` / `s3:` / `ftp:` / `data:` / `mailto:` / `javascript:` / etc.), POSIX-absolute paths (`/etc/passwd`), leading-backslash (`\\share\…`), **protocol-relative URLs (`//attacker/x.png` — would inherit the contact sheet's protocol and fetch from a remote host)**, `..` traversal segments, and empty values; (d) any `style="..."` attribute on any element; (e) **any attribute value containing a CSS `url(...)` reference** — these appear in presentation attributes like `fill` / `stroke` / `mask` / `clip-path` / `filter` / `cursor` / `marker-*` and would fetch the referenced external paint server / filter / mask / cursor when the contact sheet is rendered; (f) **the `xml:base` attribute on any element** — would rewrite the document's base URL so an otherwise-safe relative href like `"assets/cover.svg"` resolves against the attacker-controlled base. A refused SVG is NOT spliced into the page — the contact sheet shows a textual `SVG not embedded (unsafe for inline): <reason>` notice in that slide's card instead. A safe SVG is re-serialized from the parsed tree (so XML declarations, DOCTYPEs, processing instructions, and comments cannot leak into the HTML host page). **Second layer (strip-by-removal):** even after the safety check accepts the SVG, every `href` / `src` / `xlink:href` / `*href` attribute is stripped from the re-serialized tree before inlining — including legitimate workspace-relative paths like `<image href="generated_assets/cover.png">` that pass `local_path_is_safe`. The reason: such a path resolves against the HTML host directory when the contact sheet is opened in a browser, and the output gate requires that host directory to live OUTSIDE the workspace by design — so the path 404s on disk anyway. The strip layer removes the fetchable surface entirely so the contact sheet never reaches the network OR the local filesystem when opened. Element geometry (`x` / `y` / `width` / `height` / `viewBox`) survives — only the reference attributes are removed. The contact sheet contains no `file://` URLs, no scheme-relative references, no `href` / `src` / `xlink:href` attribute of any value, no `url(...)` CSS references, no `xml:base`, no SMIL/animation elements, and no JavaScript — it never fetches anything when opened in a browser. `--output` must live OUTSIDE `--workspace`, must not be a symlink, must not exist as a non-regular file, and must not carry a URI scheme — same anti-patterns the rest of the repo's output paths refuse.

```
# Human-readable summary, no file written
python3 scripts/validate_visual_quality.py --workspace path/to/workspace

# JSON report alongside the human summary
python3 scripts/validate_visual_quality.py \
  --workspace path/to/workspace \
  --output path/to/report.json

# Local HTML contact sheet with inline SVG (open it in a browser locally)
python3 scripts/validate_visual_quality.py \
  --workspace path/to/workspace \
  --output path/to/contact_sheet.html

python3 scripts/validate_visual_quality.py --self-test
```

`scripts/validate_visual_quality.py --self-test` exercises 47 tempfixture scenarios under `tempfile.TemporaryDirectory()`: happy path, missing SVG, blank slide, image-only slide (paired with the no-editable-primitive ERROR), all-placeholder slide, out-of-canvas render_model bounds, out-of-canvas SVG geometry, malformed SVG, deck_plan vs render_model count mismatch, empty `render_models/`, missing `render_models/`, JSON output round-trip, HTML output self-containment (no `href` / `src` / `xlink:href` attribute of any value survives in the body), `--output` extension gate, `--output` inside the workspace refused, **non-mutating** assertion that two repeated inspections leave every workspace file byte-identical, out-of-canvas `<text>` anchor, workspace symlink refused, `--strict` promotes WARN to non-zero exit, **HTML sanitize-by-rejection scenarios** — workspace SVG with a remote `<image href="https://attacker...">` (URL never lands in a fetchable position; fallback notice shown), `<script>` element (no `<script` substring in produced HTML), `onload=` event-handler attribute, `<style>` element with `@import url(...)`, `style="background-image:url(...)"` attribute, **protocol-relative `<image href="//attacker/...">`**, **POSIX-absolute `<image href="/etc/passwd">`**, **leading-backslash `<image href="\\share\…">`**, **`..` traversal `<image href="../../etc/passwd">`**, **empty `href=""`** refused at the safety check, **leading-whitespace URI bypass `<image href=" https://attacker/x">`**, **CSS `url(...)` in `fill` attribute** (no `fill="url(..."` in the produced HTML; the fallback notice carries the CSS-url diagnostic), **`xml:base="https://attacker/"` on the `<svg>` root** (no `xml:base=` in the produced HTML; would otherwise rewrite the base URL for every relative href), **SMIL `<animate attributeName="href" to="https://attacker/x">`** inside an `<image>` (no `<animate` element in the produced HTML; would otherwise mutate the static href at runtime) — plus direct unit-tests of `_svg_safety_check` against a clean fixture (accepted), a non-SVG root (refused), a bundled negative enumeration over every shape `local_path_is_safe` rejects (URI schemes, protocol-relative, POSIX-absolute, leading backslash, path traversal, empty), a bundled negative enumeration over the whitespace-prefix bypass surface (leading/trailing space / tab / newline / CR / NBSP before/after URI scheme / `//` / `/` / `\\` / safe path), a bundled negative enumeration over **CSS `url(...)` on `fill` / `stroke` / `mask` / `clip-path` / `filter` / `cursor` / `marker-start`, `xml:base` on the root, and every SMIL/timing/filter element (`<animate>`, `<set>`, `<animateMotion>`, `<animateTransform>`, `<discard>`, `<mpath>`, `<feImage>`)** (every variant refused; a clean hex-only `<rect fill="#FFFFFF">` still accepted), **strip-layer end-to-end scenario — a workspace SVG with legitimate workspace-relative `<image href="generated_assets/cover.png">` + `xlink:href` + `src` attributes (the exact shape `generate_svg_previews.py` emits for an image_slot) has every reference attribute STRIPPED from the inlined HTML body while the `<image>` geometry (`x` / `y` / `width` / `height`) survives** — confirming the contact sheet never resolves a fetchable local path against the HTML host directory, **plus a direct `_strip_fetchable_attrs` unit-test that removes every `href` / `src` / `xlink:href` / `data-href` / case-variant `FormHref` on every element while non-reference attributes survive**, and a defensive scenario proving a `<?...?>` processing instruction inside the element tree does not crash the safety check (non-string `ET.Element.tag` handled).

The gate is a downstream review pass: every check it makes is informational and does NOT replace `validate_workspace.py` (which validates schemas, coverage, layout, bounds, and SVG safety with stricter rules) or `validate_pptx_contract.py` (which validates the produced PPTX). It is meant to be run AFTER `generate_render_models.py` + `generate_svg_previews.py` and BEFORE PPTX export, when a quick visual / structural sanity check on a prepared workspace is useful. (The synthetic authoring trial under `examples/synthetic_authoring_trial/` is an *authoring bundle*, not a prepared workspace — it is validated via `scripts/validate_authoring_bundle.py` and becomes a prepared workspace only after `scripts/run_explicit_pipeline.py` materializes it.)

### Image-asset trial evidence (mock chain only, human-reviewable)

`scripts/image_asset_trial_evidence.py --self-test` is a focused, tempdir-only evidence emitter for the same mock/local image asset → editable PPTX path that `scripts/image_asset_acceptance_smoke.py` already exercises. It drives the existing chain (`done_image_adapter` → `run_d_one_generation --allow-synthetic-bytes` → `materialize_image_assets` → `run_explicit_pipeline --assets-dir <staging>` → `validate_pptx_contract --expected-slide-count N` → `inspect_pptx_inventory` → `validate_visual_quality`) under a single `tempfile.TemporaryDirectory()` and writes a small structured JSON file at `<tempdir>/report/image_asset_trial_evidence.json` recording exactly what landed: the produced `.pptx` path / size, the sorted `ppt/media/*.{png,jpg,jpeg}` parts the exporter embedded, whether at least one OOXML `relationships/image` Relationship resolves inside the package, the list of external / `file://` / `data:` / URI-scheme relationships (must be empty), the rc + ok of `validate_pptx_contract` / `inspect_pptx_inventory` / `validate_visual_quality`, the subset of `<report-dir>/inventory.json` fields the trial reports on (`ok` / `findings` / `slide_count` / `media_parts_count` / `evidence_basis`), the contract validator's `minimal_evidence.{editable_text,not_all_image,no_blank_slide,every_slide_has_native_shape}` PASS/FAIL flags (proves the deck still carries native editable structure and is NOT all-image), whether `<report-dir>/visual_quality.json` landed and parses as JSON, the expected slide count derived from the synthetic plan, the temp report dir, a fixed `notes.real_d_one_status` sentence stating real D-One is UNVERIFIED and uncalled by this script, and a single `summary.ok` boolean that ANDs every observed gate. The JSON contents are also dumped to stdout so a reviewer never has to recover the tempdir. Snapshot check: takes flat-bytes snapshots of `REPO_ROOT/examples/` and `REPO_ROOT/scripts/` before the run and re-snapshots after; any drift aborts the script non-zero. **MOCK / local only — NOT real D-One integration**: nothing calls D-One, MCP, Qoder, a public network, telemetry, any model API, an image search, or any external service. The script has no production CLI surface today; `--self-test` is required (running without it exits 2). The intent is reviewability — a human can read one PASS line plus one small JSON blob to confirm the basic mock image-asset → editable PPTX capability still holds end-to-end, without scrolling many self-test logs.

### Image-request taxonomy acceptance smoke (mock chain only)

`scripts/image_taxonomy_acceptance_smoke.py --self-test` is a tempdir-only acceptance smoke that proves the seven-dimensional D-One prompt-intent taxonomy (`rendering_style` / `palette_family` / `image_role` / `layout_pattern` / `modifier` / `text_policy` / `subject_domain`) survives the existing local mock D-One chain end-to-end. It drives the chain `done_image_adapter --descriptor-vocabulary <vocab>` → `run_d_one_generation --descriptor-vocabulary <vocab> --allow-synthetic-bytes` → `materialize_image_assets` → `run_explicit_pipeline --assets-dir <staging>` under a single `tempfile.TemporaryDirectory()` against a synthetic spec request that carries one canonical value per dimension. After the chain succeeds the smoke asserts the produced `d_one_adapter_plan.json` carries `schema_version == 2` (the locked value after the 1 → 2 bump for taxonomy support), exposes exactly one request, and preserves every taxonomy field/value byte-identically — proof the taxonomy projection is lossless through the deterministic writer. The final `.pptx` is then verified to embed at least one `ppt/media/<file>.{png,jpg,jpeg}` part with no external / `file://` / `data:` / URI-scheme relationships. Negative probes (20 total, in-process via `done_image_adapter`): **N1** spec carries any taxonomy field but `--descriptor-vocabulary` is omitted → write path refuses; **N2** spec `text_policy` is regex-shape valid but outside `image_taxonomy.text_policy.allowed_values` → write path refuses; **N3** spec `subject_domain` is regex-shape valid but outside `image_taxonomy.subject_domain.allowed_values` → write path refuses; **N4** hand-written plan whose `rendering_style` drifted from the vocab's `allowed_values` → `--validate-plan` refuses (stale taxonomy drift after the vocab moved); **N5** plan `schema_version` set to 1 (pre-taxonomy shape) AND 3 (a future unknown shape) → both refused by the plan schema enum locked to `[2]`; **N6** vocab descriptor carrying any of the nine bounded forbidden tokens (`public`, `upload`, `raw`, `customer`, `confidential`, `screenshot`, `credential`, `password`, `secret`) → descriptor-vocabulary schema pattern refuses; **N7** vocab descriptor carrying any of the five compound forbidden phrases (`full_slide`, `image_search`, `web_generation`, `page_generation`, `slide_generation`) → same schema pattern refuses. Snapshot check: takes flat-bytes snapshots of `REPO_ROOT/examples/` before the run and re-snapshots after; any drift aborts the script non-zero. **MOCK / stub acceptance only — NOT real D-One integration**: nothing calls D-One, MCP, Qoder, a public network, telemetry, any model API, an image search, or any external service. The smoke has no production CLI surface today; `--self-test` is required.

### Image-asset negative-probes smoke (mock chain only)

`scripts/image_asset_negative_probes_smoke.py --self-test` is the **fail-closed companion** of `scripts/image_asset_acceptance_smoke.py` — that smoke proves the mockable D-One-stub chain SUCCEEDS end-to-end and emits a native editable PPTX with an internal `ppt/media/*` image; this smoke proves every documented fail-closed gate FIRES on its documented perturbation of that baseline, so a regression that silently demotes a gate (or accepts attacker-shaped input) is loud. Each probe runs under its own `tempfile.TemporaryDirectory()`; the smoke writes nothing under `REPO_ROOT/` and confirms `REPO_ROOT/examples/` + `REPO_ROOT/scripts/` byte-snapshots are unchanged on success. Probes (every probe expects rc != 0 from its target tool AND every named expected diagnostic substring to appear in stdout/stderr; if the gate is also documented as writing nothing, the smoke verifies that too): **P1** missing image file → `materialize_image_assets` refuses (workspace + assets-dir both empty); **P2** wrong magic bytes → `materialize_image_assets` refuses (a `.png`-named plain-text file under assets-dir trips the magic-byte check); **P3** `image_manifest` id mismatched with `slide_plan.image_refs` → `init_image_manifest` refuses (Stage-5 setup is the actual `init_workspace` → `init_deck_brief` → `init_deck_plan` → `init_design_system` → `init_slide_plans` subprocess chain so the cross-check fires through the same code path a live caller would hit); **P4** unsafe `local_path` × 6 shapes (`http://`, `https://`, `file://`, `data:`, `../`, `/abs`) → `materialize_image_assets` refuses each (the schema permits any non-empty string, so the helper's `local_path_is_safe` scan is the durable gate); **P5a** symlinked target file (`<workspace>/<local_path>` is a symlink) → `materialize_image_assets` refuses with the symlink-specific diagnostic (leaf-symlink check fires before `_resolves_within`); **P5b** symlinked parent directory (intermediate segment between `<workspace>` and the target is a symlink) → `materialize_image_assets` refuses via the parent-walk gate; **P6** public-distribution wording × 4 phrases (`upload to public hosting`, `public upload`, `share publicly`, `publish to web`) → `done_image_adapter` refuses via `_PROMPT_PUBLIC_DISTRIBUTION_LITERALS`, and the workspace never gains a `d_one_adapter_plan.json`; **P7** credential-shaped strings × 5 (AWS access key, JWT, PEM marker, bearer token, `password:` literal) → `done_image_adapter` refuses via `_PROMPT_CREDENTIAL_PATTERNS` + `_PROMPT_CREDENTIAL_LITERALS`. 20 probes total; the smoke exits non-zero with a per-probe diagnostic the moment any documented gate stops firing as expected. **MOCK / stub acceptance only — NOT real D-One integration**: nothing calls D-One, MCP, Qoder, a public network, telemetry, any model API, an image search, or any external service. The smoke has no production CLI surface today; `--self-test` is required.

### Visual-review contract smoke (static)

`scripts/visual_review_acceptance_smoke.py --self-test` is a local-only static readiness smoke for the synthetic per-slide visual-review contract. It validates `examples/synthetic_visual_review.json` through both `scripts/validate_visual_review.py` and `scripts/validate_artifacts.py`, then uses temp fixtures to prove the contract refuses missing / duplicate slide coverage, summary drift, unknown enums, non-synthetic ids, URL / unsafe path / public-upload / credential / raw-source / confidential wording, and screenshot / browser / D-One / MCP / Qoder / model API / external-service claims. It also preserves the negative-positive boundary that legitimate `render_model ...` artifact wording passes while bare `model was used` / `called the model` / `model was invoked` / `model was prompted` fail closed. The smoke writes only under `tempfile.TemporaryDirectory()` and asserts `examples/` and `scripts/` are byte-identical after the run; it does not capture screenshots, open a browser, call a model, call D-One, render slides, export PPTX, or generate artifacts.

### MVP acceptance smoke (synthetic authoring trial)

`scripts/acceptance_smoke.py` is a stdlib-only, **narrow** end-to-end acceptance smoke for the implemented explicit-input pipeline. It composes the existing scripts — `validate_authoring_bundle.py` (preflight) → `run_explicit_pipeline.py` with `--report-dir` (Stage 1-to-10 orchestration; internally runs `validate_workspace`, `generate_render_models`, `generate_svg_previews`, `export_pptx`, `validate_pptx_contract`, and `inspect_pptx_inventory`) → `validate_pptx_contract.py --pptx ... --expected-slide-count N` (belt-and-braces slide-count gate) → `validate_visual_quality.py --workspace ... --output <report-dir>/visual_quality.json` (post-pipeline visual review; the deterministic JSON report is persisted alongside the pipeline reports so the smoke leaves a reviewable artifact, not just stdout) — against the synthetic authoring trial bundle (`examples/synthetic_authoring_trial/`), into a temporary output directory that lives OUTSIDE the committed example tree. After every stage passes, the smoke asserts the final PPTX exists as a regular non-symlink file and is non-empty, that PPTX passes the contract validator with `--expected-slide-count = len(deck_plan.slides)`, that `<report-dir>/inventory.json` carries `ok=true`, `findings=[]`, the `evidence_basis` line `"OOXML structure only; not proof of full PowerPoint editability"`, and `slide_count == N`, AND that `<report-dir>/visual_quality.json` exists as a regular non-symlink JSON file. **This is MVP acceptance smoke only — it is NOT a full prompt/report/Markdown-to-PPTX automation.** Every Stage-1-to-6 artifact's content still comes from the bundle's already-authored JSON spec files; in live mode (the smoke against `--bundle`) it never extracts business content from `input/source.md`, never generates any image / spec / slide body, never calls D-One / Qoder / any public network / image generation / telemetry / external service / model API. The `--self-test` image-asset chain scenario (described below) is the one exception: it delegates to `scripts/image_asset_acceptance_smoke.py --self-test`, which emits a single fixed magic-byte synthetic PNG payload (the delegated smoke's synthetic bundle declares exactly one `cover_accent` image at `media/cover_accent.png`; `run_d_one_generation --allow-synthetic-bytes` supports both PNG and JPEG, but this smoke only exercises PNG today) under its own `tempfile.TemporaryDirectory()` — strictly local: no real D-One call, no MCP, no public network, no model API, no image search, no Qoder, no external service. Fail-closed: any stage exiting non-zero, or any post-condition failing, aborts the smoke immediately. The smoke refuses an `--output-root` that resolves inside `examples/`, `references/`, `schemas/`, `scripts/`, `templates/`, `.git/`, or `.claude/` so generated PPTX / reports / render_models / SVG never land in the committed tree, the git metadata directory (writing into `.git/` can corrupt the object database / refs / hooks / index), or the user's Claude Code project state (transcripts, memory, hook config). Both the repo-prefix comparison AND the refused-name comparison are **case-insensitive** (every path component is `.casefold()`d on both sides before comparison) so two distinct case-variant bypasses are closed on case-insensitive filesystems (macOS APFS, Windows NTFS): (1) **case-variant of a refused first component** — `--output-root=.GIT/foo` resolves to `.git/foo` on disk but `Path.resolve()` preserves the input case, so a naive `relative_to(repo / '.git')` would say "not a match" while the FS would still route the write into the real `.git/`; (2) **case-variant of the REPO prefix itself** — `--output-root=/Users/Robert/.../szh-ppt-master/.git/foo` (capital R when the on-disk path is `/Users/robert/...`) where `Path.resolve()` again preserves the input case, a naive `relative_to(repo_resolved)` raises ValueError on the prefix case mismatch, the gate concludes "outside the repo entirely" and skips the refusal, yet the underlying FS still routes the write into the real `.git/`. Casefolding every path component before any comparison closes both. The repo-root-itself check uses the same case-folded comparison so `--output-root=/Users/Robert/.../szh-ppt-master` is also refused. Without `--output-root` the smoke creates a temporary directory via `tempfile.mkdtemp()` (outside the repo by virtue of `$TMPDIR`) and removes it on exit unless `--keep` is supplied. `scripts/acceptance_smoke.py --self-test` exercises 22 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` covering: the real-pipeline happy path (deck.pptx + pipeline_report.{json,txt} + inventory.json + visual_quality.json all land under output/report; inventory contract holds); missing bundle directory; invalid bundle directory (missing `source.md`); **malformed `plan_spec.json` aborts cleanly BEFORE the preflight stage** (no Python traceback; no workspace / PPTX / report files written — proves the smoke's up-front shape gate around `_expected_slide_count`); preflight failure (corrupt slide_spec JSON aborts at stage 1); explicit-pipeline failure (pre-existing non-empty `<output_root>/workspace/` aborts at stage 2); PPTX-contract failure (injected stage-3 exit-1); inventory-post-condition failure (manufactured bad inventory.json with `ok=false` / non-empty `findings` / wrong basis / wrong `slide_count` — `_check_post_conditions` catches every violation); visual-quality failure (injected stage-4 exit-1); **the image-asset chain smoke** (delegates to `scripts/image_asset_acceptance_smoke.py --self-test`: a d_one chain — `done_image_adapter` → `run_d_one_generation --allow-synthetic-bytes` → `materialize_image_assets` — runs in a staging workspace that materializes the declared PNG bytes at `<staging>/<local_path>`, then `scripts/run_explicit_pipeline.py --assets-dir <staging>` is invoked once against a fresh production workspace + the same `d_one_local` bundle and is expected to return rc=0: the orchestrator's `materialize_image_assets` step (Stage 5.5, only present when `--assets-dir` is passed) copies the staged bytes into `<workspace>/<local_path>` so `init_image_manifest` (Stage 6) finds them and the cascade completes Stages 1-10 in one shot — no expected failure, no manual recovery, no fallback to the Stage-7-to-10 prepared-workspace runner; the delegated smoke then runs `validate_pptx_contract --expected-slide-count N` + `validate_visual_quality --output <report-dir>/visual_quality.json` as belt-and-braces validators and asserts the final PPTX embeds at least one `ppt/media/<file>.{png,jpg,jpeg}`, carries no external / `file://` / `data:` relationships, `<report-dir>/inventory.json` is `ok=true` / `findings=[]` / `slide_count == len(deck_plan.slides)` / `media_parts` non-empty / exact `evidence_basis` line, `<report-dir>/visual_quality.json` exists as a regular non-symlink file under the temp report dir and parses as JSON, the explicit-pipeline output carries every documented success marker (`OK: explicit-input end-to-end run succeeded`, `[PASS] materialize_image_assets`, `[PASS] init_image_manifest`) and NO expected-failure / recovery wording, and the smoke's own source body carries no literal reference to the Stage-7-to-10 prepared-workspace runner; **MOCK / stub acceptance only — NOT real D-One integration; no MCP, no public network, no model API, no image search, no Qoder, no external service**); inventory determinism (re-running `inspect_pptx_inventory.py` on the happy-path PPTX produces byte-identical inventory bytes); a snapshot-diff that proves no file under `REPO_ROOT/examples/` was mutated by any scenario; **eight refused-top-level-directory probes** that exercise `_validate_output_root` directly with probe paths the gate must reject without ever creating on disk — three for `.git/` (lower-case `.git`, upper-case `.GIT`, title-case `.Git`), two for `.claude/` (`.claude`, `.CLAUDE`), and three for `examples/` (`examples`, `EXAMPLES`, `Examples`) — proving the case-insensitive first-component comparison; and **two case-variant repo-prefix probes** that flip the case of every alphabetic character in the REPO prefix and exercise both "descend into `.git/` via case-variant prefix" AND "repo-root-itself via case-variant prefix" — proving the case-insensitive repo-prefix and repo-root comparisons close attack (2).

```
python3 scripts/acceptance_smoke.py                                          # default: bundle=examples/synthetic_authoring_trial; outputs under tempdir
python3 scripts/acceptance_smoke.py --output-root /tmp/szh-acceptance --keep # preserve outputs for inspection
python3 scripts/acceptance_smoke.py --self-test                              # 22 in-script scenarios under tempfile.TemporaryDirectory()
```

### Packaging readiness check

`scripts/verify_skill_package.py` is a stdlib-only, **verify-only** static check that the repo can ship as an internal skill package without leaking generated artifacts or breaking doc-referenced commands. It enumerates the package surface as `git ls-files` (tracked) + `git ls-files --others --exclude-standard` (untracked-not-ignored) and runs five fail-closed gates: (1) `no_generated_artifacts` — no `__pycache__`, no `*.pyc`/`*.pyo`/`*.pyd`, no PPTX-family binaries (`*.pptx`/`*.potx`/`*.ppsx`/`*.ppt`/`*.pot`/`*.pps`), no pipeline reports (`pipeline_report.json`/`pipeline_report.txt`/`inventory.json`/`visual_quality.json`), no `*.log`/`*.tmp`/`.DS_Store`, no `.env`/`.env.*`/`*.pem`/`*.key`, no generated-workspace directories anywhere in the path (`dist`/`build`/`out`/`output`/`previews`/`renders`/`exports`/`projects`/`.git`/`.claude`); (2) `doc_referenced_scripts_exist` — every `scripts/<name>.py` reference in `SKILL.md` / `README.md` / `CLAUDE.md` / `AGENTS.md` / `references/*.md` must (a) resolve to a regular file on disk AND (b) be tracked or staged for tracking (present in `git ls-files`); an untracked-or-gitignored file would survive `--root` enumeration but drop out of a clean `git archive` / fresh checkout, so the gate fails closed with an explicit `not tracked or staged for tracking` diagnostic; (3) `doc_referenced_schemas_exist` — same two-part check for every `schemas/<name>.schema.json` reference; (4) `no_credential_shapes` — no fully-formed credential shape (AWS key `AKIA[A-Z0-9]{16}`, PEM `-----BEGIN ... KEY-----` block, JWT-shaped triple, bearer literal, inline secret literal like `password='...'` / `api_key='...'`) in any runtime DATA file (`schemas/`, `templates/`, `examples/`) — Python scripts under `scripts/` are EXEMPT because they legitimately ship adversarial deny-list fixtures; (5) `no_external_urls` — no external URL substring (`https?://`, `s3://`, `ftp://`, `file://`, `data:<lower>`) in any runtime DATA file except the well-known allow-list (the JSON-Schema draft-07 `$schema` URL in `schemas/*.json`; the W3C SVG/xlink namespace URIs in `examples/**/*.svg`). The JSON-aware doc-key skip (strings under JSON-Schema documentation keys `description` / `title` / `$comment` / `default` / `examples` are documentation, not runtime references) is RESTRICTED to `schemas/*.json`. Template / example artifacts (`templates/` / `examples/`) legitimately use those same key names as REAL content fields (e.g. `deck_brief.title`, `deck_plan.sections[].title`, `template.description`), so applying the skip there would let an attacker hide a runtime URL in a content field; non-schema runtime data files are therefore text-scanned end-to-end. Python scripts are EXEMPT because they legitimately ship OOXML namespace identifiers (`http://schemas.openxmlformats.org/...`) and adversarial URL fixtures inside self-tests. A hostile URL that STARTS WITH an allow-listed prefix but extends past it (e.g. `http://www.w3.org/2000/svg/EVIL`) is still flagged — the allow-list compares full equality. **Out of scope (deliberately):** live Qoder runtime import / runtime packaging (UNVERIFIED here, and TODO across the wider repo); zip writing (the .gitignore permits a local `dist/` directory but this helper is verify-only today); changes to PPTX export behavior. The script does NOT call D-One / MCP / public network / telemetry / model API / image search / external service. `--self-test` exercises 40 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` — each fixture is its own ephemeral git repo (`git init --quiet --initial-branch=main` + `git add -A` of the baseline) so the tracked-or-staged check has a real `git ls-files` to consult: a positive baseline; G1 negatives for every forbidden directory and every forbidden filename / suffix / prefix; a G1 `.git/` path-component defense-in-depth test exercised by calling the gate function directly with a synthetic file list (`git ls-files` filters out `.git/` so injection through the normal pipeline cannot reach the gate, but the entry stays in the forbidden-component set as belt-and-braces against a future non-git enumerator); G2 / G3 negatives (docs reference missing scripts / schemas); G2 / G3 tracked-or-staged coverage — a docs-referenced script that exists on disk but is NEVER `git add`-ed fails G2, the same script after `git add scripts/<name>.py` passes every gate, a gitignored docs-referenced script (file on disk + tracked `.gitignore` entry) fails G2, plus the matched untracked / staged pair for `schemas/<name>.schema.json` under G3; G4 negatives for each credential shape; G5 negatives for `https://` / `file://` / `data:` / `s3://` in templates/examples; a G5 allow-list positive (W3C SVG + xlink namespaces); a G5 tail-attack negative (extended-tail URL must still fire); a G5 JSON-doc-key positive — `description` / `examples` URLs in a schema fixture under `schemas/` are skipped — plus two regressions that prove the skip is correctly scoped: a URL placed under `title` in a non-schema example JSON, AND a URL placed under `description` in a non-schema template JSON, both still fire; a regression that proves the same URL value under a non-doc KEY in a schema still fires; and an empty-surface scenario.

```
python3 scripts/verify_skill_package.py             # verify the current repo
python3 scripts/verify_skill_package.py --root R    # verify a specific repo root
python3 scripts/verify_skill_package.py --self-test # 40 in-script scenarios under tempfile.TemporaryDirectory()
```

### Packaging build

`scripts/package_skill.py` is a stdlib-only, **narrow** deterministic builder that emits exactly one `.zip` of the allow-listed package surface under `dist/` (or a caller-supplied `--output PATH`) after the pre-flight `verify_skill_package` gates pass, and re-inspects the produced archive against the same forbidden-member, doc-reference, and allow-list gates. The shipped surface is an **explicit allow-list** of top-level package roots/files intersected with `git ls-files` (tracked only) — root files are `SKILL.md` / `README.md` / `SECURITY.md`; root dirs are `references` / `schemas` / `scripts` / `templates` / `examples`. Anything else — VCS/control metadata (`.gitignore`, `.gitattributes`, `.gitmodules`, any future root-level `.git*` control file), local-development docs whose audience is in-repo AI agents (`CLAUDE.md` / `AGENTS.md`), editor / OS junk — is intentionally excluded from the archive even when `git ls-files` would report it. Preferring an explicit allow-list over "all tracked files" means a future stray root-level tracked file does not silently ship in the next build — it has to be named in the allow-list, and named in the docs, before it reaches the archive. Untracked-not-ignored files are also NOT shipped even when the verifier accepts them in the wider surface, because they would not survive a clean checkout / `git archive` and would silently break the package on re-derive. The pre-flight still scans the wider (tracked + untracked-not-ignored) surface so a stray credential / external URL / forbidden artifact in the working tree blocks the build before any zip is written. The archive is **deterministic**: members are written in sorted POSIX-string order; per-member `date_time` is pinned to `(1980, 1, 1, 0, 0, 0)` (the zip-format minimum) instead of the on-disk mtime so a rebuild on the same tree produces byte-identical bytes regardless of file modification times; per-member `create_system` is pinned to 3 (UNIX) so the host OS does not vary the recorded system identifier; per-member `external_attr` is pinned to `0o100644 << 16` (regular file, rw-r--r--) so executable bits / umask do not leak into the archive; compression is `ZIP_DEFLATED` at compresslevel 6 (Python stdlib default; the same input bytes through the same deflate codec are byte-equal across runs); no extra fields, no archive comment, no per-member comment. Post-write, the produced `.zip` is opened and three extra gates run over the archive member set: (A1) `archive_no_forbidden` re-runs the verifier's G1 over the member name list as defense-in-depth against a future enumerator drift, (A2) `archive_doc_references_present` requires every `scripts/<name>.py` and `schemas/<name>.schema.json` referenced by any shipped doc file (`SKILL.md` / `README.md` / `CLAUDE.md` / `AGENTS.md` / `references/*.md`) to also be a member of the archive — so a downstream consumer who only sees the zip can rely on the docs not referencing missing files — and (A3) `archive_members_allow_listed` requires every archive member's top-level path component to be in the explicit package allow-list, so VCS/control metadata and local-development docs cannot silently ship even if a future `select_package_members` refactor drifts. The build writes to `<output>.partial` first and renames on success; any pre-flight, mid-write, or post-write gate failure removes the partial / final archive so a half-written or contract-violating zip is never left at the final path. The mid-write cleanup lives inside `write_deterministic_zip` itself as a `try/except (Exception, SystemExit)` wrapper around the entire write + replace block — if any source file turns out to be a symlink, vanishes between pre-flight and write, or any OSError fires during read / write / replace, the leftover `<output>.partial` is unlinked defensively (symlink-safe; best-effort) before the original exception propagates. The post-write gate failure path additionally calls `_cleanup_partial` from `build_package` to cover any leftover partial after the rename has happened. **Stale-archive hygiene**: the output-path cleanup runs BEFORE pre-flight, so a prior successful build's zip at `--output` does not survive a later failing pre-flight attempt and mislead the caller into trusting outdated bytes as "the current package" — the on-disk state always reflects the latest build outcome (a fresh archive after success; nothing at the output path after any failure). A symlink at the output path itself is refused outright by the same up-front cleanup so the cleanup cannot be tricked into following a planted symlink and deleting an unrelated target, and a directory at the output path is also refused so the builder never silently destroys arbitrary content. Parent-symlink policy is split by mode. For a user-supplied `--output`, a symlink at the parent directory is INTENTIONALLY NOT refused — legitimate OS layouts use symlinked path components (on macOS `/tmp` is a symlink to `/private/tmp`) and refusing those would make the canonical `TMPDIR=/tmp` invocation unusable; the output-FILE symlink check is the actual attack surface there, not the parent. For the **default-path mode** (no `--output`, the builder derives `<root>/dist/szh-ppt-skill.zip` from the repo root), the up-front cleanup additionally refuses any symlinked path component STRICTLY UNDER `<root>` on the way to the output's parent — a symlinked `<root>/dist/` (or any future intermediate component) resolving to an unrelated directory is NOT canonical filesystem geometry; it can only have been planted by the user or an attacker, and silently following it would let the cleanup unlink + overwrite a file the symlink resolves to before the build had even started. The default-path refusal pins the documented invariant that the cleanup never silently redirects writes through a symlinked default `dist/` parent (`<root>` itself may still be a symlink — a repo checkout under macOS `/tmp/checkout` → `/private/tmp/checkout` is normal — only components STRICTLY UNDER `<root>` are refused). **Out of scope (deliberately):** live Qoder runtime import / runtime packaging (UNVERIFIED here, and TODO across the wider repo — the produced zip is a static archive whose loadability inside a Qoder runtime is not proven by this builder); changes to PPTX export behavior. The script does NOT call D-One / MCP / public network / telemetry / model API / image search / external service. `--self-test` exercises 16 in-script tempfixture scenarios under `tempfile.TemporaryDirectory()` only — outputs land in a sibling tempdir so the verifier never sees the freshly-written zip: a baseline build (the archive members equal the **allow-listed subset** of the tracked surface in sorted POSIX order — the fixture deliberately tracks `.gitignore` / `CLAUDE.md` / `AGENTS.md` so the filter has concrete inputs to drop, and the test asserts those names are tracked but NOT in the archive while every allow-listed root file IS in the archive; every member carries the fixed timestamp / external_attr / create_system); a two-build determinism scenario (the same fixture built twice yields byte-identical archives); a pre-flight block (staging a tracked `examples/.../deck.pptx` makes the build fail on `no_generated_artifacts` BEFORE any zip is written, and the output path does not exist after the failure); a direct archive-level G1 negative (`scripts/__pycache__/x.pyc` in a synthetic member list fires `archive_no_forbidden`); a direct A3 `.gitignore` negative (`.gitignore` in a synthetic member list fires `archive_members_allow_listed`); a direct A3 `.git*` family negative (`.gitattributes` / `.gitmodules` / `.git-blame-ignore-revs` each fire); a direct A3 local-development-doc negative (`CLAUDE.md` / `AGENTS.md` each fire); a direct A3 unknown-root-dir negative (`tools/probe.py` fires); a `select_package_members` unit test (synthetic mixed-input list is filtered to exactly the allow-listed subset, in input order); a direct archive-level A2 negative (a docs file references a fictional `scripts/<missing-name>.py` path not in the member set — the stem is NEVER spelled out as a real-looking script name and is NEVER spelled out in this row so the verifier's own G2 scan over `references/*.md` does not pick up an unresolvable reference and self-trip; `archive_doc_references_present` fires with a diagnostic naming the missing target); a custom-output-path scenario (two parallel builds at different output paths yield byte-identical archives, proving the output path does not influence archive contents); a direct-call symlink-refusal scenario (a symlink at the output path passed straight to `build_package()` is refused with a clear `symlink` diagnostic and the decoy target is left intact); a **stale-archive-hygiene** scenario that pins the fix for the prior bug — a baseline build succeeds and writes a zip at `output`, then a forbidden tracked file is injected so the next pre-flight fails, and after the second `build_package()` call returns FAIL the prior zip at `output` MUST be gone (and no `<output>.partial` survives either), proving the output-path cleanup runs BEFORE pre-flight so a stale archive does not survive a failing build; a **CLI-path symlink-refusal** scenario that exercises `main(["--root", R, "--output", L])` end-to-end where `L` is a symlink — pins the fix for a prior CLI-path bypass where `_resolve_output` called `Path.resolve()` (which FOLLOWS symlinks) so the symlink was rewritten to its target before `_prepare_output_path` ever saw it, silently letting the builder unlink + overwrite an unrelated file the symlink pointed at. `_resolve_output` now uses `Path.absolute()` (does NOT follow symlinks) so the symlink path stays structurally intact and `_prepare_output_path` refuses it; a **default-path symlinked-parent refusal** scenario that exercises `main(["--root", R])` end-to-end (no `--output`, so the builder derives the default `<root>/dist/szh-ppt-skill.zip`) with a symlink planted at `<root>/dist/` pointing at a sibling decoy directory containing a regular `szh-ppt-skill.zip` file — pins the fix for a prior default-path cleanup bug where `_prepare_output_path` would follow the symlinked parent and unlink the decoy file via the symlink before the build had started. The fix refuses any symlinked path component STRICTLY UNDER the repo root in default-path mode; the test asserts the SystemExit message contains `symlink`, the decoy file at the symlink target is preserved byte-identical, and the symlinked parent dir itself is left in place untouched; and a **write-failure `.partial` cleanup** scenario that calls `write_deterministic_zip` directly with a curated file list whose first entry is a regular source file (so the partial accumulates real bytes before the failure fires) and whose second entry is a symlinked source file (so the per-source-file `_refuse_symlink` check fires mid-write) — pins the fix for a prior write-failure cleanup bug where the half-written `<output>.partial` would otherwise survive on disk indefinitely because `tmp_path.replace(out_path)` never runs after a mid-write exception. The fix wraps the entire write + replace in `try/except (Exception, SystemExit)` inside `write_deterministic_zip`; on any failure it unlinks the leftover partial (symlink-safe; best-effort) and re-raises the original exception so the caller still sees the actual error. The test asserts the SystemExit message names the symlink, the leftover `.partial` does NOT exist after the cleanup, and the final `output` also does not exist (the rename never ran).

```
python3 scripts/package_skill.py                                # build dist/szh-ppt-skill.zip from this repo
python3 scripts/package_skill.py --output /tmp/skill.zip        # build into a caller-supplied path
python3 scripts/package_skill.py --root /path/to/checkout       # build a different checkout
python3 scripts/package_skill.py --self-test                    # 16 in-script scenarios under tempfile.TemporaryDirectory() only
```

All other tools — full PPTX coverage (every layout, every primitive, full editability inventory, `chart_placeholder`), SVG / GIF / WebP media embedding, security scan, visual regression, automatic `image_manifest.json` generation from a directory of real assets (the manifest itself still has to be authored — `init_image_manifest.py` validates a caller-supplied spec; `materialize_image_assets.py` is the pre-D-One asset-byte materialization gate, not a manifest auto-populator), D-One integration, Qoder CLI, live Qoder runtime import / runtime packaging (the packaging-readiness check above is verify-only and does NOT prove the package can be loaded by Qoder) — are still **not implemented**. Do not document them as available. Render-model generation for layouts mapped to the `chart_placeholder` primitive kind, and SVG rendering / PPTX emission of primitive kinds outside `text` / `line` / `shape` / `image_slot` / `kpi` / `table`, are also not implemented.

## Engineering rules

See `CLAUDE.md` for the durable engineering and privacy rules that apply to all work here. Highlights:

- Keep changes narrow and surgical.
- Prefer structured schemas, templates, and deterministic validators.
- Preserve pipeline invariants (see `SKILL.md`).
- Mark missing decisions as TODOs, not invented defaults.
- Do not commit real customer data, credentials, endpoints, or sensitive report text.
- Do not copy or paraphrase any code or wording from `ppt-master`. This repo is clean-room.

## How to extend the scaffold

When adding the next pipeline stage:

1. Update or add the relevant schema under `schemas/`.
2. Update the matching reference doc under `references/`.
3. Add a synthetic fixture under `examples/` and confirm it validates.
4. Add or extend a deterministic script under `scripts/` that fails clearly when behavior is not yet implemented.
5. Update `SKILL.md` "What exists" / "What is NOT implemented" sections.
