# Synthetic 8-Page Product Brief (Example Source Input)

> **This document is synthetic.** It contains no real company, product, customer, or financial data. Every name, figure, and date is a placeholder. Use this file only to exercise the pipeline scaffold at the lower end of the deck-length range.

## Context

A fictional internal team (referred to here as the "Synthetic Product Team") is circulating an internal product brief for an early-stage proposal. The brief is short on purpose — it exists to demonstrate that the pipeline does not assume any single deck length and can produce shorter decks when the source material warrants it.

## Audience

Internal product review (synthetic).

## Objective

Communicate the proposal in eight slides, request approval of two synthetic next steps, and avoid any speculative financial guidance.

## Tone

Neutral, business-professional.

## Approximate length

8 slides. This is intentionally shorter than the upper-range stress-test fixture and demonstrates the adaptive deck-length behavior described in `references/workflow.md`.

## Source outline

1. Cover — title, presenter (synthetic), date (placeholder).
2. Executive summary — qualitative product overview (synthetic).
3. Key message — core proposition (qualitative).
4. Two-column — problem vs. solution (placeholder bullets).
5. KPI dashboard — three placeholder target metrics ("metric_x", "metric_y", "metric_z") with placeholder values and deltas.
6. Timeline — three rollout milestones (placeholder events).
7. Two-column — risks vs. mitigations (placeholder bullets).
8. Conclusion — two synthetic asks (placeholder approvals).

Note: this outline deliberately omits agenda, section dividers, and a comparison table. Those layouts are available in the `business_review` template family but are not required by every deck.

## Key messages (synthetic)

- Synthetic Product Team can deliver the proposal within the proposed scope.
- Two placeholder asks require approval to start.
- Three placeholder risks have placeholder mitigations.

## Constraints

- Must include three placeholder target metrics.
- Must avoid speculative financial guidance.
- Must avoid any real customer reference.

## Source references

- `synthetic_source_p` (placeholder identifier; no real file).
- `synthetic_source_q` (placeholder identifier; no real file).
