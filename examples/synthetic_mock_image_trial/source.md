# Synthetic Mock Image Trial Source

> **This document is synthetic.** It contains no real company, product,
> customer, employee, or financial data. Every label below is a
> placeholder authored only to exercise the mock image-to-editable-PPT
> bundle shortcut.

## Context

A fictional internal team is reviewing the local mock D-One image chain
on a tiny two-slide deck. The chain never reaches the public network,
never calls a model API, never reads this body into a prompt, and never
produces a real image; it lays down a fixed synthetic PNG byte payload
so the downstream PPTX exporter has something to embed under
`ppt/media/`.

## Sections in the source

- Cover (synthetic placeholder)
- Conclusion (synthetic placeholder)

## Synthetic notes

This synthetic body is intentionally short and qualitative. It contains
no file path, network address, credential, or model API reference. Slide
bodies in the spec bundle are authored separately and do not import
paragraphs from this file.
