{
  "name": "kpi_dashboard",
  "status": "skeleton",
  "purpose": "Grid of KPI tiles, each with a label, value, and optional delta.",
  "slots": [
    {
      "id": "title",
      "type": "text",
      "required": true,
      "primitive_kind": "text",
      "bounds": { "x": 64, "y": 80, "w": 1792, "h": 120 }
    },
    {
      "id": "kpis",
      "type": "kpi",
      "required": true,
      "primitive_kind": "kpi",
      "bounds": { "x": 64, "y": 280, "w": 1792, "h": 200 }
    }
  ],
  "todo": "Slot sized to wrap the label + value (+ optional delta) lines; the generator paints a rounded-rect card per tile."
}
